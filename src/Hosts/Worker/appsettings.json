{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "_ConnectionStrings_note": "Password injected at runtime (env / user-secrets); never committed. The worker connects as pol_worker: cross-tenant outbox drain, RLS-scoped consumer writes, no bypass role.",
  "ConnectionStrings": {
    "Worker": "Server=localhost,11433;Database=PaymentOrchestration;User Id=pol_worker;Password=;Encrypt=True;TrustServerCertificate=True"
  },
  "Vault": {
    "MasterKeyBase64": ""
  }
}
