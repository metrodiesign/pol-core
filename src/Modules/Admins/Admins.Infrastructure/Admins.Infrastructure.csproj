<Project Sdk="Microsoft.NET.Sdk">

  <ItemGroup>
    <ProjectReference Include="..\Admins.Application\Admins.Application.csproj" />
    <ProjectReference Include="..\..\..\BuildingBlocks\BuildingBlocks.Infrastructure\BuildingBlocks.Infrastructure.csproj" />
    <!-- Iam.Domain is a published language (rf2) — this repo queries iam.Roles/iam.RolePermissions directly
         for assignment resolution; only Iam.Domain, never Iam.Application/Infrastructure. -->
    <ProjectReference Include="..\..\Iam\Iam.Domain\Iam.Domain.csproj" />
    <!-- masterdata-split: the ONLY Admins layer that names the four reference entity types — the User->cfg.*
         FK configs (HasOne<Position/Office/Level/Division>) live here. Only each module's Domain, never its
         Application/Infrastructure. -->
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.EntityFrameworkCore.SqlServer" />
  </ItemGroup>

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
  </PropertyGroup>

</Project>
