# Implementation Tasks: System Completion
> Status: approved 2026-06-23 (autonomous, AFK). Branch feat/system-completion (off develop). Commit per task; PR not merged.

- [x] 1. Cart management — `Cart.SetItemQuantity` (domain) + `GetCart` query (CartView), `RemoveItemCommand`, `SetItemQuantityCommand`, `ClearCartCommand` handlers + host endpoints (GET /carts/{id}, DELETE/PUT /carts/{id}/items/{productId}, POST /carts/{id}/clear, RequireAuthorization("tenant")). Done = domain + handler unit tests green; not-open -> 409, qty<=0 -> 400.
     Satisfies: REQ-1.1/1.2/1.3/1.4/1.5/1.6/1.7. Verify: `dotnet test tests/Cart.Tests`.
     Evidence: `dotnet test tests/Cart.Tests` -> Passed 15 (domain: AddItem-merge/Subtotal, SetItemQuantity set/reject<=0/reject-unknown-product/reject-not-open, Remove, Clear; handlers: GetCart own-tenant view / null wrong-tenant+missing, Remove+save, SetQuantity update/reject<=0, Clear, edit-on-missing rejected). Api build + Hosts.Tests 39 green (container boots with new handlers/endpoints). Endpoints added: POST /carts, POST /carts/{id}/items, GET /carts/{id} (404 if null), DELETE/PUT /carts/{id}/items/{productId}, POST /carts/{id}/clear (all RequireAuthorization("tenant")). Cart/Checkout/Orders had NO host endpoints on develop — Cart now reachable. Viewports: n/a. Deviations: edit on missing cart -> InvalidOperationException (409), mirroring existing AddItemToCartHandler for module consistency (not 404). New Cart.Tests project + slnx entry.

- [x] 2. Order summary link + TTL — `Order` gains SummaryToken + expiry (Create issues, default 72h; ReissueSummary rotates+extends), `GetOrderSummaryByTokenQuery` (404 unknown / 410 expired / view), `ResendOrderSummaryCommand`; new `GoneException` -> 410 in ProblemDetailsExceptionHandler; host GET /orders/{token}/summary (public, token = capability) + POST /orders/{id}/summary/resend (tenant). Done = domain + handler tests green.
     Satisfies: REQ-2.1/2.2/2.3/2.4/2.5. Depends on: -. Verify: `dotnet test tests/Orders.Tests`.
     Evidence: `dotnet test tests/Orders.Tests` -> Passed 11 (5 existing + domain: Create issues token+future expiry / IsSummaryExpired, ReissueSummary rotates+extends / rejects non-awaiting; handler: resend rotates+extends+saves / unknown->NotFound / non-awaiting->InvalidOperationException). Solution build + Hosts.Tests 39 boot green. Migration `20260622183508_AddOrderSummaryToken`: SummaryToken (per-row unique defaultValueSql NEWID -> UNIQUE index holds for existing rows) + expiry + `usp_resolve_order_summary` (WITH EXECUTE AS 'pol_webhook_resolver', mirrors webhook resolver) + grants (SELECT Orders to resolver, EXECUTE to pol_app) + Down revoke/drop; `ef migrations script` emit verified. GoneException -> 410 arm added. Public read uses IOrderSummaryReader (bypass proc via fresh scope, no raw SqlConnection) returning a customer-safe view (no TenantId). Viewports: n/a. Deviations: query realized as a reader port over the bypass proc (not a Mediator query) because the customer is anonymous (no tenant binding) — matches the webhook resolver pattern; host computes 410 from expiry. Apply migration -> CI.

- [x] 3. Customer notification via outbox — `Contracts.CustomerOrderNotification`, enqueue in CreateOrder + ResendOrderSummary (same UoW), `INotificationSender` port + `LoggingNotificationSender` default (no PII), Worker consumer `INotificationHandler<CustomerOrderNotification>` building the link + calling the sender; relies on existing OutboxDispatcher retry/DLQ. CreateOrderCommand gains optional Recipient. Done = consumer test (calls sender; failure propagates for retry) + enqueue test.
     Satisfies: REQ-3.1/3.2/3.3/3.4/3.5. Depends on: 2. Verify: `dotnet test tests/Orders.Tests`.
     Evidence: `dotnet test tests/Orders.Tests` -> Passed 15 (+4: CreateOrder enqueues CustomerOrderNotification when Recipient present / nothing when absent; consumer sends via port / propagates a delivery failure for outbox retry). Built: Contracts.CustomerOrderNotification (INotification), INotificationSender + NotificationMessage (BuildingBlocks.Application), LoggingNotificationSender (no PII — logs order id only, registered in AddBuildingBlocksInfrastructure so both hosts + Api ValidateOnBuild resolve it), CustomerOrderNotificationConsumer (Orders.Application; throw -> outbox retry/DLQ), CreateOrderHandler enqueues in the same UoW via IOutbox. Added the `CustomerOrderNotification` case to OutboxDispatcher's publish switch (the second integration event, as its comment anticipated). Solution build + BuildingBlocks.Tests 34 + Hosts.Tests 39 boot green. Viewports: n/a. Deviations: notification fires on ANY order creation carrying a Recipient; the upstream Checkout->Order trigger that would supply the recipient is a PRE-EXISTING deferred gap (ConfirmCheckout is a stub by design, CreateOrderCommand had no caller) — out of this slice's scope, noted for a follow-up. Real email/SMS provider deferred (logging default, swappable via DI). Resend-renotify not built (REQ-3 is create-time; resend rotates the token in task 2).

- [x] 4. Reconciliation reporting — `GetReconciliationSummaryQuery` + handler (group bound tenant's orders by Status+Currency -> count + total minor units), `ReconciliationView`; host GET /reports/reconciliation (tenant). Done = handler test (groups correctly, per-currency) + integration (tenant-scoped).
     Satisfies: REQ-4.1/4.2/4.3. Depends on: -. Verify: `dotnet test tests/Orders.Tests`.
     Evidence: `dotnet test tests/Orders.Tests` -> Passed 16 (handler maps repo totals -> view lines with status names, USD kept separate from THB — never summed across currencies). Full non-integration suite green (Cart 15, Orders 16, Architecture 36, BuildingBlocks 34, Hosts 39, Payments 48, SharedKernel 39). IOrderRepository.GetReconciliationAsync does the GROUP BY (Status, AmountCurrency) -> count + SUM in SQL via EF; handler maps to ReconciliationView. Host GET /reports/reconciliation (RequireAuthorization("tenant")). Integration ([Trait Integration], authored): the aggregate excludes another tenant's orders (marker currency, RLS filters before aggregation) + app cannot insert a foreign-tenant order (BLOCK) + InsertOrderAsync helper. Viewports: n/a. Deviations: none. Per-currency grouping satisfies REQ-4.3 (no cross-currency sum).

## Notes
- No new module; extends Cart + Orders. Domains stay EF-free; ITenantScoped + RLS preserved.
- Notification rides the existing transactional outbox + Worker OutboxDispatcher (retry/backoff/DLQ already there); real email/SMS provider deferred (logging default impl, swappable via DI).
