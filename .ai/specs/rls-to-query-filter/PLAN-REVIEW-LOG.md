# Plan Review Log: RLS → EF Core global query filter migration
Started session (2026-07-18). MAX_ROUNDS=5. Reviewer: Codex (gpt-5.6-sol, read-only). Builder/arbiter: Claude.
Plan artifacts: `.ai/specs/rls-to-query-filter/{PLAN.md, design.md, requirements.md}`.

## Round 1 — Codex (thread 019f75a0-4ccf-72e0-8236-0230e5660815) — VERDICT: REVISE

- **[Critical]** Write guard checks in-memory `MerchantId`, not the DB row owner → forged detached stub (victim PK + attacker MerchantId) passes; UPDATE/DELETE keys on PK only → cross-merchant write without raw SQL/ExecuteUpdate. Fix: MerchantId as concurrency token / part of DML key; reject detached Modified/Deleted; SQL Server test.
- **[High]** OutboxMessage/VaultRevealAudit deliberately unfiltered + 1 principal = any query reads all merchants (permanent bypass). Fix: filter both, suppress only at outbox lease + admin audit gateway (allowlist).
- **[High]** `Guid.Empty` deny-default has no DB invariant (raw SQL/bulk/forgotten interceptor). Fix: `CHECK (MerchantId <> Empty)` on every tenant table + `Merchants.Id` + fresh-DB assert.
- **[High]** Entity list incomplete + REQ-11.5 ("every MerchantId entity implements marker") contradicts design's control-plane exclusions (ProvisioningAudit, RoleAssignment, nullable-merchant Role, admin access/audit). Fix: exhaustive classification (IMerchantFiltered/IPlatformOnly/IUserOwned/IShared), fail model-build on unclassified.
- **[High]** `PolDbContext` filter example references `Product/Cart/Order/PspConnection` directly → BuildingBlocks.Infrastructure has no module refs by design → won't compile / reverse dependency. Fix: build filter from EF metadata + shared marker, no compile-time module reference.
- **[High]** Anonymous registration writes an outbox row under a sentinel merchant with an unbound actor → new write guard (`CanWrite=false`) blocks it; RLS had an explicit carveout (`fn_outbox_predicate`). Fix: scoped system capability for this op/entity/sentinel + integration test.
- **[High]** Merchant root not interceptor-guarded; provision Super-check only at API route → Mediator/worker/alternate caller bypasses. Fix: platform-write authz in handler/pipeline + interceptor guards Merchant by target Id.
- **[High]** CartItems denormalized MerchantId has no parent binding (Item MerchantId=A but CartId of B → guard sees A, allows); seed/test inserts lack column. Fix: composite FK `(CartId, MerchantId)` + alt key on Cart + stamp in ctor only; update seed/test.
- **[High]** Bypass inventory/static bans incomplete (SqlQueryRaw/FromSql*/ExecuteSql*/GetDbConnection/Attach/Update/Remove; arch test partial assemblies). Fix: consolidate raw/suppressed ops in one gateway + ban all bypass primitives across all prod assemblies with allowlist.
- **[High]** 1 principal expands blast radius (SQLi/repo bug in merchant host reaches admin/control-plane/audit). Fix: keep runtime/worker/control-plane principal separation + DB DENY for audit update/delete.
- **[High]** Migration "rewrite/remove" on a possibly-applied migration; SecurityObjects also creates `merch.RegistrationNotices`; prod config references multiple principals. Fix: forward migration preserving schema, backfill CartItems, add constraints, drop security objects explicitly, schema fingerprint check.
- **[Med]** Vault applock stated conceptually; test doesn't prove serialization. Fix: pin the one-transaction algorithm (begin → tx-owned lock → check return → read head → insert → commit) + concurrent-N-writer test.
- **[Med]** Model-cache test ("two sequential scopes") doesn't prove parameterization / same-context rebind; ban misses `AddPooledDbContextFactory`. Fix: assert generated SQL/params for two contexts sharing model cache + ban all pooling APIs.
- **[Med]** Admin accessible-set materialized once at auth (stale-scope race on revoke/demote); keyed admin flow calls tenant-bound repo directly (GetMerchantHandler). Fix: revalidate access/tier in write tx + force all keyed admin reads through IAdminQuery.
- **[Med]** No observability requirements (guard denial, unbound, Empty, bypass, applock timeout, admin cross-merchant, correlation); 1 principal loses DB attribution. Fix: structured audit/metrics (actor kind/id, target merchant, entity, op, reason, correlation) + per-context Application Name, no PII/secret.
- **[Med]** Testing below risk; requirements.md=draft but design.md=approved; test harness may not register interceptor; SQLite proves no SQL Server constraint/DML/applock/migration. Fix: sync spec state + full SQL Server matrix (read, forged detached write, empty sentinel, admin, outbox, Cart FK, bulk/raw, vault concurrency).
- **Alternative:** remove RLS but keep principal/GRANT separation + global filters + DB constraints/composite keys + single escape-hatch gateway.

### Claude's response (arbiter)
ACCEPT (revising artifacts): Critical (concurrency-token) + all [High] EXCEPT principal-separation + all [Med]. These are real, mostly fail-OPEN gaps spec-architect missed.
- Concurrency-token on IMerchantOwned closes the forged-stub write (DB-level `AND MerchantId=@original`) — stronger than banning Attach/Update/Remove.
- Filter application rewritten to metadata+marker (fixes the compile/dependency-cycle blocker) and folded into an exhaustive classification (`IMerchantFiltered`/`IPlatformOnly`/`IUserOwned`/`IShared`) that fails model-build on an unclassified mapped entity — this also resolves the REQ-11.5-vs-design contradiction.
- OutboxMessage/VaultRevealAudit now FILTERED (fail-closed) + suppressed only at drain/audit; registration sentinel gets an explicit scoped carveout; `CHECK(MerchantId<>Empty)` DB invariant added; CartItems composite FK; Merchant root guarded by Id + handler authz; expanded bypass ban + single gateway; applock algorithm pinned + concurrent-N test; observability REQ added; SQL Server test matrix expanded; migration preserves RegistrationNotices + updates prod config.
REJECT (with reason): **keep 1 principal** — the owner explicitly chose this twice (design gate + analyze F-Q3), accepting the blast-radius trade. Codex's merits are noted and WILL be surfaced at human sign-off; the audit-integrity part is partially recovered by the append-only write-guard rule + `CHECK` + concurrency token (a DB trigger was already declined by the owner).

## Round 2 — Codex (resumed session) — VERDICT: REVISE
Round-1 points confirmed closed; principal-separation NOT re-raised (accepted residual risk). Deeper (second-order) findings:
- **[High]** `Merchant` classified `IMerchantFiltered` (needs `MerchantId`) but entity has only `Id`, and REQ-2.7 says it is NOT filtered → self-contradiction (model-build fail / shadow column). Fix: per-entity tenant-key descriptor (`MerchantId` or `Id`).
- **[High]** Classification enforces only `IMerchantFiltered`; the other 3 are inert labels. `merch.RoleAssignments` has a real `MerchantId` but is `IPlatformOnly` → unfiltered; a new caller leaks cross-merchant roles. Fix: enforced policy per category; reclassify tenant-owned entities as filtered.
- **[High]** `CanWrite(Guid)` authorizes by merchant only — Super can write/delete Order/PaymentSession/Idempotency/Outbox/vault; old `pol_admin` was read-only on business tables. App-authz loss (not the principal dispute). Fix: operation-aware `CanWrite(entityType, state, merchant)`, default-deny, explicit per-flow capabilities.
- **[High]** Write guard is an interceptor that a context can be registered without; a missing-interceptor context accepts foreign non-empty `MerchantId` INSERT (CHECK only blocks Empty; token doesn't affect Added). Fix: guard in a sealed `PolDbContext.SaveChanges/Async` override + DI test on every registration.
- **[High]** A single `internal` gateway in BuildingBlocks can't be called cross-module without becoming a generic bypass or a reverse dependency. Fix: narrow per-operation ports per module + central capability/audit + per-impl arch allowlist.
- **[High]** Registration sentinel carveout is narrow at insert but wide at dispatch — the dispatcher binds the sentinel as actor, so `CanWrite(sentinel)` lets the consumer write any filtered entity (CHECK allows the non-empty sentinel). Fix: dispatch registration with a system actor `CurrentMerchant=Empty` that cannot write tenant data + DB CHECK forbidding the sentinel on all filtered tables except Outbox.
- **[High]** 1-principal inventory incomplete: `migrate-entrypoint.sh` (3 passwords), `01-principals.sql` (creates pol_app/admin/worker/resolver + bypass role), `.env.example`, CI, local compose, Worker settings, fresh-db assertion still bind old principals. Fix: complete inventory in REQ-8 + CI assertion of no legacy principal/RLS object post-migration.
- **[Med]** Concurrency token allows changing a row's owner (load original A, set current B → UPDATE SET MerchantId=B WHERE MerchantId=A passes). Fix: `MerchantId` immutable post-insert + guard requires original==current for Modified/Deleted.
- **[Med]** Tx revalidation doesn't close the revoke race under READ COMMITTED; `AdminActorContext` (BuildingBlocks) can't reference `IAdminScope` (Admins.Application). Fix: host-owned authorizer port + authorization version/row-lock to commit.
- **[Med]** Observability misses `DbUpdateConcurrencyException`, CHECK/FK violation, admin-revalidation denial, gateway anomaly, sentinel misuse; no alert/tamper-resistant sink/retention/redaction test. Fix: event taxonomy + external sink + alerts + redaction tests.
- **[Med]** SQL Server matrix not exhaustive per model; no negative tests for ordinary admin write / gateway misuse / omitted interceptor / sentinel consumer writing business table; metadata test doesn't prove the filter uses the correct tenant property (esp. Merchant). Fix: model-driven A/B actor matrix over all filtered entities × Added/Modified/Deleted × original/current tenant + gateway + sentinel denial.
- **[Med]** Spec not gate-ready: requirements=draft vs design=approved; `IMerchantOwned`/`IMerchantFiltered` terminology mixed; no tasks.md. Fix: sync terminology/status + create tasks.md after convergence.

### Claude's response (arbiter)
ACCEPT ALL (no rejections this round; all are genuine second-order fail-open/authz/layering gaps). v4 changes: per-entity tenant-key descriptor (resolves Merchant contradiction); each classification category carries ENFORCED semantics + reclassify `merch.RoleAssignments`→filtered; operation-aware default-deny `CanWrite(entityType,state,merchant)` (admin read-only on business tables); write guard moved into sealed `SaveChanges/Async` override (can't be skipped) + DI test; per-operation module ports replace the single gateway; registration = system actor `CurrentMerchant=Empty` + CHECK forbidding sentinel outside Outbox; complete deployment inventory + CI legacy-object assertion; `MerchantId` immutable + original==current guard; host-owned authorizer port + authz-version/lock for the revoke race; expanded observability taxonomy + tamper-resistant sink; model-driven full SQL Server matrix; terminology synced to `IMerchantFiltered`.

## Round 3 — Codex (resumed session) — VERDICT: REVISE
Round-2 mostly closed, but v4 (single-context + runtime 4-way classification) exposed a new, deeper layer:
- **[High]** `SaveChanges(bool)` / `SaveChangesAsync(bool, ct)` overloads bypass the guard (only 2 of 4 overridden). Fix: seal all 4 via one save-core + reflection test.
- **[High]** 4-way behavior isn't enforced at the runtime chokepoint — read filter + write guard touch only `IMerchantFiltered`; the other 3 categories rely on STATIC arch rules, not runtime deny. A missed path reads platform data under 1 principal. Fix: guard every changed entry + runtime read policy per category.
- **[High]** `Iam.Role` is mixed-scope (Platform + shared + merchant-owned in one table); can't be `IPlatformOnly` (merchant login reads it). Needs a mixed-audience visibility descriptor + parent-scoped filter for `RolePermission`.
- **[High]** Reclassifying `merch.RoleAssignments`→filtered breaks the admin cross-merchant counter + pre-auth merchant role resolution (keyed admin ctx = Empty → 0). Needs narrow ports.
- **[High]** `IUserOwned` has no owner descriptor (owner property varies: PlatformUserId/MerchantUserId/Id/Subject; some audits null) and clashes with auth bootstrap (session read by token hash before owner bind). Needs per-entity owner descriptor + auth-bootstrap port + reclassify audits/notices.
- **[High]** Revoke-race unworkable: `Guard()` runs before EF opens the implicit transaction, so a row lock read there isn't held to commit. Needs one chosen algorithm + explicit UoW tx + in-tx recheck.
- **[High]** Default-deny capability map doesn't cover most write flows (ProvisioningAudit, role CRUD, user approval, sessions, audits, RegistrationNotice, master data, framework-written `DataProtectionKey`). Needs an EXHAUSTIVE model-driven write-policy manifest (every entity × state × actor) + fail-startup on missing policy.
- **[High]** Requirements still hold contradictions (generic "gateway" in 1.6/5.5/7.2/11.2/11.4; MerchantId-on-all in 2.6/11.7; REQ-2.7 says Merchant not filtered; old matrix in 11.2) — not synced to design v4.
- **[Med]** Tenant-key descriptor `Property(key)` auto-creates a shadow property on typo (doesn't fail as claimed); needs `FindProperty` + reject missing/shadow/wrong-type/nullable + typo test.
- **[Med]** Tamper-resistant telemetry has no delivery/failure design (sink/buffer/retry/backpressure/outage).
- **[Med]** Spec gate not passed (requirements draft, no tasks.md, unresolved revoke mechanism).
- **Alternative (Codex):** keep 1 principal but split into `MerchantDbContext` / `IdentityDbContext` / `PlatformDbContext` with limited entity sets → smaller runtime surface than a 4-way generic classifier, clearer auth bootstrap, merchant code can't see control-plane tables at compile time.

### Claude's response (arbiter) — PAUSING THE LOOP FOR HUMAN INPUT
All round-3 findings are valid. But the pattern across v2→v4 is decisive: making ONE shared `PolDbContext` safely serve merchant + platform + user-owned + shared + mixed-audience data under 1 principal requires ever-more machinery — now an EXHAUSTIVE per-entity write-policy manifest + runtime enforcement of all 4 categories + per-entity owner/tenant descriptors + auth-bootstrap ports + mixed-audience descriptors. This is plausibly MORE complex than the RLS it replaces, which contradicts the owner's stated goal (reduce complexity). Codex's split-context alternative makes isolation STRUCTURAL (compile-time) and eliminates most of that machinery. This is a fork in the fundamental approach and it touches the locked "one DbContext" invariant (T1) + the owner's core goal — so it is the owner's call, not mine to decide unilaterally. Loop paused at round 3/5; fork surfaced to the human before spending rounds 4-5 hardening a possibly-wrong-shape approach.
Owner chose the split-context alternative → v5.

## Round 4 — Codex (resumed session) — VERDICT: REVISE (split-context v5)
Principal-separation NOT re-raised. But split-context v5 exposed decisive STRUCTURAL problems:
- **[CRITICAL]** Cross-context transactions are real and MULTIPLE, not "expected none": registration (SubmitRegistration — account+audit+outbox), merchant approval (ApproveReject — Identity User/Audit + Merchant RoleAssignment + Platform Role read), admin assign-merchant (AssignMerchant — Identity Audit + Platform MerchantAccess), provisioning (Merchant rows + Platform ProvisioningAudit), self-provision. Splitting by audience breaks these units of work → partial-commit corruption.
- **[CRITICAL]** Transactional outbox breaks: registration writes `merch.Users` (Identity) + `OutboxMessages` (Merchant) in one UoW → not atomic across contexts.
- **[CRITICAL]** Only MerchantDbContext has a sealed write guard; Identity/Platform have no unskippable write floor → fail-open for foreign user/session/audit/control-plane writes after RLS.
- **[CRITICAL]** Migration lineage: current DB has one `__EFMigrationsHistory` chain; three empty histories → each context's initial migration recreates existing tables; no baseline/bridge.
- **[HIGH]** FK relationships cross audiences (Merchant `RoleAssignment→Iam.Role`; admin `User→cfg.*`) → models can't be cleanly disjoint.
- **[HIGH]** "Compile-time isolation" is only arch-lint today: `Merchants.Infrastructure` holds BOTH merchant+identity persistence and references `Iam.Domain` directly; three contexts in shared assemblies mean merchant code can inject `PlatformDbContext` and compile. True compile isolation needs a real assembly split.
- **[HIGH]** `IProvisioningWriter` under-defined (audit moved to Platform; capability minted pre-tx persists after demotion); `ISessionByTokenHash` misses other pre-bind lookups (OIDC Subject, invited-admin Email); test plan proves only fresh schema, not upgrade/atomicity/compile-negative.
- Codex's refined path: 3 runtime contexts BUT keep 1 migration-only context/history, per-write-owner outbox, and partition tables by TRANSACTION boundary, not audience alone.

### Claude's response (arbiter) — DECISIVE FINDING, ENDING THE LOOP, HANDING TO HUMAN
Both explored approaches have hit fundamental walls: single-context (v4) needs an exhaustive runtime write-policy manifest; split-context (v5) breaks transactional units of work (audience boundaries ≠ transaction boundaries: registration/approval/assign-merchant/provisioning all co-commit across audiences), needs a real assembly refactor for compile isolation, and needs migration-history surgery. The ROOT CAUSE is that pol-core has cross-cutting transactional flows that RLS handles transparently (row-level, one connection, one SESSION_CONTEXT regardless of which tables the tx touches) but any app-layer floor must re-architect. Conclusion after 4 adversarial rounds: **removing RLS here is NOT a simplification — it is a large, not-yet-converged re-architecture of a payment platform's safety floor.** This directly contradicts the owner's stated goal (reduce complexity). Per codex-review Step 3 this is a legitimate "hand to human" outcome — ending the loop at round 4/5 (round 5 would not change this structural conclusion) and returning to the owner with the evidence + a recommendation to reconsider keeping RLS. requirements.md remains draft (NOT approved); no code written.

### Owner decision (human gate, after round 4)
Presented the R4 structural finding + recommendation to reconsider keeping RLS. Owner chose to **proceed with RLS removal as an explicitly LARGE project**, adopting Codex's own R4 refined path (the hybrid): 1 migration-only context/history + runtime contexts partitioned by TRANSACTION boundary + per-write-owner outbox. Not a net simplification vs RLS — owner accepts the trade. Loop reopened for one final validation round (round 5, MAX_ROUNDS=5).

### Claude's revision → design/requirements/PLAN v6 (hybrid — addresses all 10 R4 findings)
- R4 #1 (Identity/Platform no write floor): sealed 4-overload write guard on EVERY runtime context (Merchant/Identity/Platform), one save-core + default-deny `IWriteAuthorizer`; write only via narrow port; reflection test all virtual save entrypoints sealed.
- R4 #2 (registration outbox breaks): Identity-owned outbox table + drain port → registration (`merch.Users`+`RegistrationAudits`+event) commits atomically in the Identity context, no cross-context tx.
- R4 #3 (cross-context tx real+multiple): `ITransactionCoordinator` (1 DbConnection + 1 DbTransaction + `UseTransaction` per context) for registration/approval/assign-merchant/provisioning; transaction inventory + failpoint rollback test.
- R4 #4 (revoke race): in-coordinator-tx `UPDLOCK,HOLDLOCK` / conditional DML on authorization-version held to commit; barrier concurrent-revoke test.
- R4 #5 (`IProvisioningWriter` under-defined): command-level port opens own tx, locks+rechecks Super from DB, mints target IDs, restricts Added set, writes `ProvisioningAudit` atomically, opaque one-shot capability not DI-resolvable.
- R4 #6 (migration lineage): single migration-owner `SchemaDbContext` adopts the EXISTING `__EFMigrationsHistory` chain (no 3 empty histories); runtime contexts declare no migrations; upgrade test from CURRENT migrated DB.
- R4 #7 (cross-audience FK): `SchemaDbContext` owns the real cross-audience FKs; runtime contexts map scalar FK only; catalog assertion FKs still present.
- R4 #8 (compile boundary only arch-lint): real assembly split by audience + `internal` contexts; compile-negative build test (forbidden project reference must fail the build).
- R4 #9 (pre-bind lookups incomplete): enumerate ALL — `ISessionByTokenHash`, `IResolveMerchantLoginBySubject`, `IResolveAdminLoginBySubject`, `IBindInvitedAdminByEmail`; each projection-limited + `AsNoTracking` + audit + arch-allowlist; no generic unfiltered Identity repo.
- R4 #10 (test plan thin): upgrade-from-current fixture + failpoint after each context save + transaction-enlistment assertion (all multi-context writes share one tx id) + compile-negative build + concurrent revoke/provision.
- Honest note carried in design.md Non-Functional: v6 is a large re-architecture, NOT a net simplification vs RLS — owner accepts.

## Round 5 — Codex (resumed session, FINAL round 5/5) — VERDICT: REVISE
Principal separation NOT re-raised (per instruction). "migration-owner direction ดีขึ้น" but coordinator, authorization ownership, and the transaction inventory are still not pinned:
- **[High]** `ITransactionCoordinator` contract wrong-shaped: takes already-created `DbContext[]` (each has its own connection); `UseTransaction` alone is insufficient — all contexts must share ONE `DbConnection` AND `DbTransaction`. Must build the connection first, then create every context via a factory/options bound to that connection; take a typed `ContextSet` not arbitrary `DbContext[]`; coordinator = internal infra, not an Application contract. Caller can still forget a context = fail-open.
- **[High]** Retry + change-tracker semantics break atomic retry: no execution strategy / commit-unknown / `acceptAllChangesOnSuccess` / context recreation. First context `AcceptAllChanges` before the joint commit → on later-context failure + rollback the first tracker thinks it succeeded; retry on the same instance may not restage. Manual tx must be wrapped in an execution-strategy delegate; `SaveChanges(false)` then commit then `AcceptAllChanges`; idempotency key for commit-unknown.
- **[High]** Transaction inventory STILL incomplete — "4 confirmed" is wrong; 5 MORE cross-context flows found: SelfProvisionSuperAdmin (Identity User/Audit + Platform RoleAssignment), SetAdminRoles (Identity + Platform Role/Assignment), UnassignMerchant (Platform MerchantAccess + Identity Audit), Iam role CRUD (Platform Role + Identity admin audit), Merchant SetUserRoles (Identity check + Platform Role + Merchant RoleAssignment). Total >=9. Need a checked transaction table: handler, read/write context set, lock order, coordinator-vs-one-context decision, failure test — for EVERY `ExecuteInTransactionAsync`.
- **[High]** Platform write guard forgets merchant-owned Iam role writes: Iam handlers let a MERCHANT actor create/edit/delete merchant-owned roles (CreateRole/UpdateRole). A broad admin capability leaks cross-merchant role edits; an admin-only capability kills the feature. Need an explicit merchant-role capability scoped `Role.MerchantId == CurrentMerchant` (no shared/null/foreign), immutable+concurrency-token tenant key, RolePermission scope from tracked parent, negative matrix.
- **[High]** Pre-owner-bind ports mislabeled as `AsNoTracking` reads — `IBindInvitedAdminByEmail` is a WRITE (binds Subject); anonymous registration/correction and Super self-provision also write BEFORE owner bind. Identity owner guard would deny them; a generic bypass = privilege escalation. Split read ports from command ports (`IRegistrationWriter`/`IBindInvitedAdminIdentity`/`ISelfProvisionSuperWriter`), each verifying ticket/OIDC/allowlist itself, exact entity/state allowlist, conditional write, opaque capability, atomic tx.
- **[High]** Provisioning recheck reads Identity `Tier` but the provisioning coordinator enlists only Merchant+Platform → "recheck Super from DB" has no mapped context / pinned raw-SQL lease in the tx. Enlist IdentityDbContext too, or an internal `IAuthorizationLease` on the shared connection+tx issuing a non-forgeable lease to both guards.
- **[High]** `SchemaDbContext` does NOT auto-inherit the existing lineage: current designer/snapshot bind `[DbContext(typeof(PolDbContext))]`; renaming the CLR class but keeping the history table does not make EF discover the old chain. Smallest path: keep the CLR name `PolDbContext` as the migration-only context; else rewrite migration-metadata/snapshot and gate on `dotnet ef migrations list` showing the old IDs before adding the forward migration.
- **[Medium]** Compile-time boundary stops at the unified API host: `Api.csproj` references every module; `internal DbContext` blocks direct context access but a merchant endpoint can still call a privileged public read/write port. "forbidden ProjectReference fails build" needs a custom MSBuild rule; a project split alone doesn't do it. Split endpoint adapters per audience or declare the Api composition root a trusted exception + arch test that merchant routes can't resolve privileged ports; privileged ports reauthorize every call.
- **[Medium]** Revoke control still lists multiple algorithms (conditional DML / UPDLOCK,HOLDLOCK / authorization-version) chosen "later" — differing semantics/lock-order/range-lock/rows-affected. Pick one per flow NOW; specify SQL shape, index, isolation, lock order, timeout/deadlock retry, and `rows affected != 1 => deny`.

### Claude's response (arbiter) — DEADLOCK at MAX_ROUNDS (5/5), handing to human
No rejections — every round-5 finding is valid and most are not one-line patches. This is a genuine deadlock, NOT a convergence, and I will not pretend otherwise. The decisive signal is finding #3: round 5 found 5 MORE cross-context transactional flows (total >=9, not the "4 confirmed" the plan asserted). This is the SAME root cause as round 4, now quantified harder: the cross-cutting transactional surface that RLS handles transparently is large and keeps growing each time we inventory it, and the full 5-round review budget was spent without pinning the coordinator mechanics (connection-sharing, execution-strategy retry, commit-unknown), the authorization ownership (leases across contexts), the write-capability surface (merchant Iam roles, pre-owner writes), or the migration-lineage adoption. Direction (migration-owner hybrid) is right per Codex; the design is not done. Per codex-review Step 3 (deadlock outcome), returning to the owner with: the unresolved list above, the new evidence, and a recommendation to complete the full transaction inventory before deciding continue-vs-keep-RLS — because that inventory both is required to finish the design AND quantifies exactly how much RLS is buying. requirements.md remains draft; no code written.

### Owner decision (after round 5 deadlock) + Claude's revision → v7
Owner chose "เดินหน้า design v7" (continue as a large project, reopen a fresh review loop). Before revising, I got GROUND TRUTH: grepped every `ExecuteInTransactionAsync` (21 call sites, not "9") and dispatched a read-only investigation mapping each handler → entities written/read → schema. The inventory proved the decisive v7 insight: **partition runtime contexts by CO-COMMIT CLUSTER, not audience** → 20/21 transactions become single-context; only ProvisionMerchant genuinely crosses. v7 changes vs v6:
- R5 #3 (inventory incomplete / audience cuts transactions): full 21-transaction inventory table in design.md; contexts repartitioned to `ControlPlaneDbContext` (admin.*/iam.*/cfg.*, handlers 1-15), `MerchantUserDbContext` (merch user/role/registration, 17-20), `MerchantRuntimeDbContext` (shop.*/txn.*/provisioning, 16,21). Cross-context write = ProvisionMerchant only.
- R5 #1 (coordinator wrong-shaped): retired the generic `ITransactionCoordinator`; replaced with ONE internal provisioning UoW that builds a single `SqlConnection` and both contexts bound to it (not pre-created `AddDbContext` instances).
- R5 #2 (retry/change-tracker): provisioning UoW wraps in an execution-strategy delegate, fresh conn+contexts per attempt, `SaveChanges(acceptAllChangesOnSuccess:false)` → commit → `AcceptAllChanges`, idempotency key for commit-unknown.
- R5 #4 (merchant Iam role writes): `IMerchantRoleWriter` capability scoped `Role.MerchantId==CurrentMerchant`, no shared/null/foreign, RolePermission via tracked parent, negative matrix.
- R5 #5 (pre-bind writes mislabeled reads): split pre-bind READ ports from WRITE ports (`IRegistrationWriter`/`IBindInvitedAdminIdentity`/`ISelfProvisionSuperWriter`), each verifying its own trust root.
- R5 #6 (provisioning doesn't enlist Identity): provisioning UoW enlists ControlPlane and locks/rechecks Super `Tier` in-tx (UPDLOCK,HOLDLOCK to commit).
- R5 #7 (lineage): migration-owner keeps CLR name `PolDbContext`; gate on `dotnet ef migrations list` showing existing IDs.
- R5 #8 (compile boundary at Api host): assembly split + custom-MSBuild compile-negative + Api trusted composition root + arch test (merchant adapters can't resolve control-plane ports) + privileged ports reauthorize.
- R5 #9 (revoke multi-algorithm): pinned to conditional DML on authorization-version, `rows affected != 1 => deny`.
- R5 #10 (test plan): upgrade-from-current + lineage gate + provisioning failpoint/idempotency/demote-during-provision + transaction-enlistment.

## RESET LOOP (v7) — fresh Codex session, MAX_ROUNDS=5 again

### Round 1 (reset loop, v7) — Codex — VERDICT: REVISE
Codex CONFIRMED the core v7 thesis: the 21-transaction inventory classification matches the real handlers and no listed transaction crosses a cluster boundary undeclared. 10 findings — all implementation-refinement, NO architectural wall (contrast rounds 3-5 of the prior loop):
1. [High] Pre-owner binding unusable for Approve/Reject: pending `merch.Users` has `MerchantId IS NULL`, hidden by the merchant filter from the admin actor, and `AfterSaveBehavior.Throw` forbids the NULL→merchant transition approval needs. Fix: approve/reject write ports (lookup by Subject under suppression, conditional DML `MerchantId IS NULL AND Status=Pending`, one-time NULL→verified-merchant transition).
2. [High] Provisioning idempotency fails on commit-success-but-lost-ACK: the record was in MerchantRuntime (filter-hidden from admin retry) and lacked request hash / persisted result. Fix: `admin.ProvisioningOperations` in ControlPlane (unique key + hash + pre-minted MerchantId + serialized result) + fresh-context verification on commit-unknown.
3. [High] `ExecuteUpdate/Delete` bypasses the sealed guard on ALL runtime entities (not just IMerchantFiltered); SessionStore uses it for bulk revoke. Fix: ban set-based DML on every runtime entity by default; allow only via named op ports with the authz predicate in WHERE + static scan + SQL test.
4. [High] Revoke `rows affected == 1` conflicts with real semantics: session-family revoke is idempotent (0..N) and the target row carries no caller version. Fix: split the authorization LEASE (exactly-one no-op update on the caller's version row) from the business revoke (0..N).
5. [High] iam.Roles capability query policy contradiction (topology unfiltered vs design "own-only" vs requirement "shared+own"). Fix: three distinct predicates, none returning IQueryable — writer own-only, authz reader shared+own, admin unrestricted.
6. [Med] Inventory not truly full: `VaultRevealAuditWriter` uses `BeginTransactionAsync` (22nd site). Fix: add #22 + make the gate scan ALL transaction APIs.
7. [Med] Runtime scalar-only FK not separated from current config: `merch.RoleAssignments` uses `HasOne<Role>()` → importing pulls iam.Role into the model. Fix: separate runtime scalar config from migration-owner relationship config; test disjointness + FK fingerprint.
8. [Med] Outbox topology owner mismatch (table says ControlPlane outbox; migration adds only 2; one CLR OutboxMessage can't span contexts). Fix: per-owner outbox CLR (`MerchantUserOutbox`/`MerchantRuntimeOutbox`); ControlPlane has none.
9. [Med] Api-host compile boundary vs Data Protection: the `IXmlRepository` resolves PolDbContext + SaveChanges in the host → internal context won't compile without `InternalsVisibleTo(Api)` (which opens everything). Fix: move IXmlRepository into ControlPlane persistence, expose via a registration extension; no InternalsVisibleTo(Api).
10. [Med] Migration lineage pin by class name only is insufficient (designer/snapshot bind full namespace/type/assembly). Fix: pin full type identity `BuildingBlocks.Infrastructure.Persistence.PolDbContext` + migrations assembly + snapshot location; gate `dotnet ef migrations list` before AND after the split.

### Claude's response (arbiter) — ACCEPT ALL 10
No rejections — every finding is a real second-order refinement of a design whose core (cluster partition) Codex validated. This is the loop converging, not walling. Applied to design.md + requirements.md v7: approve/reject write ports + NULL→merchant one-time carve-out (2.9); set-based DML banned on all runtime entities (5.2); `admin.ProvisioningOperations` idempotency with commit-unknown verification (10.3); authorization-lease vs business-revoke split; three role predicates (writer own / reader shared+own / admin all); inventory #22 + gate over all transaction APIs (2.12); per-owner outbox CLR + ControlPlane none; runtime scalar-only config separated from migration-owner FKs; Data Protection IXmlRepository relocated (no InternalsVisibleTo(Api)); full-type-identity migration pin + before/after lineage gate.

### Round 2 (reset loop, v7) — Codex — VERDICT: REVISE
Codex confirmed HELD: shared-connection EF ordering, scalar/runtime FK split, full migration type identity, Data Protection move, role predicates. 8 new findings:
1. [High] Authorization lease breaks "only 1 cross-context": if Approve/Reject need an in-tx lease on admin.Users (ControlPlane) while writing MerchantUser, they become cross-context (Provision+Approve+Reject).
2. [High] Provisioning recheck reads only Tier → a SUSPENDED Super still passes. Fix: WHERE Tier=Super AND Status=Active AND AuthorizationVersion=@expected; suspension/demote bump version.
3. [High] Provisioning idempotency: no operationKey in the port signature; two same-key concurrent requests both see absent → loser hits unique violation → 500/409. Fix: required key in signature; catch 2601/2627 → fresh-read/return stored result.
4. [High] Forward migration doesn't add admin.ProvisioningOperations or AuthorizationVersion → compiles but fails at runtime. Fix: add table/column/index/defaults + upgrade/fingerprint asserts.
5. [High] Approve port breaks idempotent replay: current API returns 200 for Active+same-merchant retry; a Pending-only lookup would 404/409. Fix: 0 rows → projection checks Active+same-merchant → return stored success; other-merchant → 409.
6. [High] PLAN/requirements still carry rejected impl: no approve/reject ports; REQ-4.9 still rows-affected==1; REQ-11.4 bulk ban only IMerchantFiltered; REQ-11.7 rejects nullable tenant key (conflicts pending). Fix: sync all.
7. [High] Outbox physical migration vs sentinel conflict: DB has txn.OutboxMessages; design adds 2 tables without pending-row plan; sentinel still points at txn.OutboxMessages. Fix: MerchantRuntimeOutbox reuses txn.OutboxMessages, MerchantUserOutbox = new merch.UserOutbox, sentinel exception on both.
8. [Med] Topology not exhaustive: missing admin.AuthAudits, iam.Permissions, iam.PermissionGroups. Fix: add (AuthAudits append-only; Permissions read-only except seed/migration).

### Claude's response (arbiter) — ACCEPT 7, PARTIAL-REJECT 1 (with rationale)
Accepted 2-8 in full (all real): provisioning full-authz lease (Tier+Status+version) #2; required operationKey + concurrent-dup unique-violation handling #3; migration adds ProvisioningOperations + AuthorizationVersion + outbox physical mapping #4/#7; approve idempotent-replay projection #5; PLAN/requirements synced (4.9 lease-vs-business, 11.4 all-runtime bulk ban, 11.7 nullable pending carve-out) #6; topology exhaustive + entity policies #8.
PARTIAL-REJECT #1 (do NOT make approve/reject cross-context): the in-tx authorization LEASE is for revoke/demote-sensitive ADMIN writes — which all already live in ControlPlane (Reactivate/RevokeAdminSession/Suspend/SetAdminRoles = single-context, lease + business write co-located) — plus provisioning (the one cross-context flow). Approve/Reject grant a merchant USER (not admin privilege), are fully auditable + reversible; their correctness is the registration-status conditional DML (`Status=Pending`), and admin authorization is the request-boundary RBAC check. Forcing a cross-context lease on every admin-on-merchant write would dissolve the cluster partition for a ms-scale race on a reversible action — declined deliberately, documented as §lease scope in design.md. So "only ProvisionMerchant crosses contexts" holds.

### Round 3 (reset loop, v7) — Codex — VERDICT: REVISE
Codex EXPLICITLY ACCEPTED the round-2 partial-rejection #1 ("policy chooses a clear request-boundary linearization point, not 'reversible'"), asking only for a race test proving authorize-before-revoke may commit while start-after-revoke denies. 8 findings, all implementation-consistency (no architecture):
1. [High] Provisioning port can't implement the algo: returns Task<Guid> but replay needs the full result (connection ids); no expectedAuthorizationVersion in port/IAdminScope. Fix: Task<ProvisionMerchantResult> + expectedAuthorizationVersion param, pinned at boundary.
2. [High] SQL table-hint syntax wrong: `WHERE ... WITH(UPDLOCK,HOLDLOCK)` invalid → hint after the table.
3. [High] AuthorizationVersion invalidation graph incomplete (missing UnassignMerchant, RolePermission, RoleAssignment). Fix: full invalidation matrix, bump affected users in-tx, barrier test per source.
4. [High] Provisioning UoW vs internal boundary: needs both internal contexts from one assembly. Fix: dedicated provisioning-integration assembly with narrow InternalsVisibleTo from CP+MR; compile-negative others.
5. [High] Outbox requirements still conflict physical design: REQ-1.6 doesn't filter merch.UserOutbox; sentinel exception only txn.OutboxMessages; legacy sentinel rows not drained. Fix: filter both outboxes, sentinel exception both, drain legacy rows on upgrade.
6. [Med] Requirements still conflict the accepted policy: REQ-4.7 forces revalidate-in-tx for all; bulk rule assumes SQL authz predicate. Fix: scope REQ-4.7 to lease-covered flows; approve/reject linearize at boundary.
7. [Med] Provisioning diagram + tests still the old algorithm (no operationKey, bare Tier, MR idempotency). Fix: sync diagram; add suspended/version/duplicate/payload/replay tests.
8. [Med] Raw-connection CI rule (REQ-11.3) bans the SqlConnection the provisioning UoW needs. Fix: default-ban + allowlist the provisioning UoW.

### Claude's response (arbiter) — ACCEPT ALL 8
All applied to design.md + requirements.md: port → `Task<ProvisionMerchantResult>` + `expectedAuthorizationVersion` (pinned, not re-read in-tx); SQL hint moved after the table; full AuthorizationVersion invalidation matrix (REQ-4.11) incl UnassignMerchant/RolePermission/RoleAssignment + barrier tests; dedicated provisioning-integration assembly (narrow InternalsVisibleTo from CP+MR, compile-negative others); outbox — filter both outboxes (1.6), sentinel exception both (2.8/3.5), drain legacy registration-sentinel rows on upgrade; REQ-4.7 scoped to lease-covered flows + approve/reject request-boundary linearization + race test (authorize-before-revoke commits / start-after denies); provisioning sequence diagram synced (operationKey + full-authz recheck + CP idempotency) + SQL Server tests (suspended/version/duplicate winner-loser/payload/replay); REQ-11.3 default-ban + provisioning-UoW allowlist. The partial-reject held and is now framed as a linearization-point decision with a dedicated race test.

### Round 4 (reset loop, v7) — Codex — VERDICT: REVISE
"รอบนี้แก้แกนหลักได้มาก" — core fixed; 7 last-mile findings around the provisioning idempotency ledger + wording:
1. [High] Replay returns result BEFORE authz + operation not bound to caller: duplicate branch returned stored result (connection ids/hints) before the authz recheck, and the ledger lacked CallerAdminId/snapshot → another caller who guessed the key + payload got the result. Fix: authz FIRST on every attempt; persist CallerAdminId + expectedAuthorizationVersion; duplicate reauthorizes + matches caller; commit-unknown internal-only verification.
2. [High] Unique-INSERT point unclear in EF: DbSet.Add defers the INSERT; 2601/2627 are generic. Fix: immediate parameterized SQL INSERT in a tight try/catch keyed on the SPECIFIC named index.
3. [High] Outbox upgrade self-contradictory ("no pending-row migration" vs "migrate legacy rows"); keeping sentinel exception on txn.OutboxMessages after the move leaves the floor too loose. Fix: atomic move preserving Id/state under quiescence; after move txn.OutboxMessages forbids sentinel, only merch.UserOutbox allows it.
4. [Med] ProvisioningOperations.MerchantId FK vs INSERT order: row written before merch.Merchants → a real FK fails every time. Fix: declare it a NON-FK ledger value.
5. [Med] Inventory not a full WRITE inventory after the invalidation matrix: ProvisionMerchant misses admin.ProvisioningOperations; Assign/Revoke/SetAdminRoles/Unassign/UpdateRole miss the AuthorizationVersion fan-out. Fix: add fan-out writes + gate/failpoint coverage.
6. [Med] set-based-DML rule conflicts with the lease split + approve/reject: forcing the authz predicate inside the business DML WHERE contradicts the separate lease statement. Fix: reword — DML carries tenant/target/state; authorization is a separate lease or boundary-RBAC.
7. [Med] InternalsVisibleTo scopes to the assembly, not one UoW → any type added there gets both contexts. Fix: type-level arch gate — only the named coordinator type may touch the dual-context primitives.

### Claude's response (arbiter) — ACCEPT ALL 7
Applied to design.md + requirements.md: authz-FIRST ordering in the provisioning UoW + diagram (result never precedes authz); ledger row = operationKey(named unique index) + CallerAdminId + expectedAuthorizationVersion + hash + non-FK pre-minted MerchantId + result; immediate parameterized INSERT keyed on the named index (no deferred Add, no bare 2601/2627); duplicate branch reauthorizes + matches caller; atomic outbox move preserving Id/state + post-move sentinel forbidden on txn.OutboxMessages; inventory fan-out note (rows 1/5/7/9/13 → AuthorizationVersion, 16 → ProvisioningOperations, all ControlPlane-single); set-DML rule reworded (DML = tenant/target/state predicate; authz = lease or boundary-RBAC) in REQ-5.2/11.4 + design; type-level arch gate for the sole provisioning coordinator type. Core validated by Codex ("แก้แกนหลักได้มาก"); remaining were precision on the one complex flow.

### Round 5 (reset loop, v7, FINAL 5/5) — Codex — VERDICT: REVISE
Core validated AGAIN: "Cluster partition ยังถือ; ไม่พบ transaction handler ใหม่ข้าม boundary นอกจาก ProvisionMerchant. Settled decisions ทั้งสามไม่ถูกยกใหม่." Only 3 findings — all acceptable refinements, NOT disagreements:
1. [High] Duplicate replay checked only CallerAdminId, not the request hash → same caller + same key + NEW payload would get the old result. Fix: duplicate + commit-unknown must match CallerAdminId + canonical request hash; mismatch → reject before deserializing.
2. [High] Commit-unknown verifier not wired into the execution-strategy control flow → a committed-but-lost-ack op could be retried and then denied by auth-first on a since-bumped snapshot. Fix: run the ledger verifier in the strategy's `verifySucceeded` hook BEFORE any retry.
3. [Med] Doc source-of-truth drift: PLAN still said "authz predicate in WHERE"; REQ-2.8 still allowed sentinel on both outboxes; REQ-8.6 still old ledger schema + no atomic move; REQ-5.5 didn't cover merch.UserOutbox. Fix: sync all.

### Claude's response (arbiter) — ACCEPT ALL 3 (applied)
Applied: duplicate/commit-unknown now match CallerAdminId + canonical request hash (design + diagram + REQ-10.3); commit-unknown wired to the execution strategy's `verifySucceeded` hook before retry (design step 1 + step 4); doc-sync — PLAN item 3 reworded (DML predicate vs authz separated), REQ-2.8 post-move sentinel only on merch.UserOutbox, REQ-5.5 covers merch.UserOutbox, REQ-8.6 ledger schema (CallerAdminId+version+non-FK) + atomic move + post-move sentinel forbid.

## RESET LOOP OUTCOME — CONVERGED ARCHITECTURALLY, MAX_ROUNDS reached (5/5)
Trajectory: R1 10 findings → R2 8 (1 partial-reject, held) → R3 8 (partial-reject ACCEPTED) → R4 7 ("core fixed") → R5 3 (all applied). Severity fell from architectural walls (prior loop) to SQL syntax / ledger precision / doc-sync. Across all 5 rounds Codex re-confirmed the core: the cluster partition holds and ONLY ProvisionMerchant crosses contexts; the 3 settled decisions (1 principal, big-project trade, approve/reject request-boundary linearization) were never re-litigated after acceptance. This is NOT a disagreement deadlock (the prior loop was) — it is convergence that ran out of the 5-round budget with 3 accepted refinements now applied. No open Claude-vs-Codex disagreement remains. Recommendation: human sign-off gate #2 (incl. the 1-principal decision), then a short confirmatory review pass if desired before /spec-tasks. No code written; requirements.md remains draft.

### Human sign-off gate #2 (2026-07-19)
Owner reviewed the v7 convergence and decided: (1) **Approve** requirements + design → proceed to /spec-tasks (design is implementation-grade; remaining is normal implementation risk); (2) **Confirm 1 runtime principal** (the flagged Codex-R1 item) — accepts the wider blast radius for the simpler operational surface, floor stays app-layer + arch guardrails. requirements.md flipped draft→approved 2026-07-19; design.md re-stamped amended 2026-07-19; sign-off item marked RESOLVED. Next: /spec-tasks (tasks.md). No code yet.
