# Implementation Tasks: rf1-schema-reset — Big-bang foundation ของ v5 restructure

> Status: approved 2026-07-11

> Each task is a cohesive, independently verifiable slice. Implement a whole task
> in one pass (it may touch many files). Decompose into sub-steps yourself at
> execution time — do NOT pre-split tasks here.

- [x] 1. Spike กลไก SQL Server 2025 ที่ design พึ่ง — script ทดลองบน fresh DB (:11434): (ก) `sp_set_session_context @read_only=1` สอง key + pooled reuse ผ่าน `sp_reset_connection` ล้าง/re-stamp ได้จริง (ข) cross-schema SCHEMABINDING predicate (sec → admin.PlatformUsers + PlatformMerchantAccess) + ownership chaining dbo โดยผู้ query ไม่มี SELECT (ค) tier-based Super branch ทำงานตามคาด. Done = spike doc ใน `docs/spikes/` + verdict go/no-go; ถ้า no-go หยุดแล้วกลับมาแก้ design ก่อน
     Satisfies: REQ-3.10, REQ-4.1 (spike evidence — implementation จริงอยู่ task 3). Verify: spike script รันผ่านบน container fresh + ผลบันทึกใน doc.
     Evidence:
       - test: `sqlcmd -i probe_bc.sql` (cross-schema SCHEMABINDING predicate, literal จาก design.md, บน throwaway SQL Server 2025 container `pol-spike-rf1-sql`, host port 11435 แยกจาก `pol-sql` :11434 จริง) -> 6/6 scenario ตรงตามคาด: no-context=0 แถว, merchant branch=1 แถว, Super(tier)=2 แถว(all), Scoped assigned=1 แถว, Scoped unassigned(REQ-3.11 fail-closed)=0 แถว, sentinel guard(UserId NULL)=0 แถว; control test ยืนยัน `spike_app` ไม่มี direct GRANT บน `admin.PlatformUsers` (permission denied จริง) แต่ query ผ่าน policy สำเร็จ = ownership chaining ทำงาน; catalog verify จริง (`OBJECT_ID('sec.fn_merchant_predicate')` non-null, `sys.security_policies.is_schema_bound`=1) ไม่ใช่ print เดา
       - test: `dotnet run` (PooledProbe, `Microsoft.Data.SqlClient` 7.0.2 ตรงกับที่ `EntityFrameworkCore.SqlServer` 10.0.8 ดึงจริง, `Max Pool Size=1` บังคับ physical reuse) -> SPID round1=round2=60 (ยืนยัน physical reuse จริง), re-stamp คีย์ `@read_only=1` เดิมซ้ำหลัง reuse สำเร็จ (`RE-STAMP_OK`, ไม่ throw), ไม่พบค่าเก่ารั่วก่อน re-stamp รอบนี้ (SqlClient ล้างให้เองผ่าน `sp_reset_connection`) — ไม่เปลี่ยน design เพราะ T13 ไม่ได้พึ่งพฤติกรรมนี้อยู่แล้ว (ดู spike doc Finding 1)
       - viewports: n/a — logic-only (ไม่มี UI)
       - deviations: none — verdict = **GO**, design ไปต่อได้ไม่ต้องแก้; spike doc: `docs/spikes/2026-07-11-rf1-rls-session-context.md`; throwaway container + scratchpad ลบแล้วหลังจบ (เหลือ `.spike-pw` ใน scratchpad session — ไม่ผูกกับ credential จริงใด ๆ, container ที่มันเคยป้องกันถูกลบไปแล้ว)
- [x] 2. Money DECIMAL(19,4) end-to-end — rewrite `SharedKernel/Money.cs` (decimal Amount + scale≤4 reject + currency semantics เดิม), EF complex type ทุก entity ที่ถือ Money (คอลัมน์ `{Prop}Amount`/`{Prop}Currency` char(3)), `MoneyJsonConverter` ใหม่ (string fixed 4 decimals, reject number) + register HTTP + outbox/worker serializer, rewrite DTO + event contracts (flat `*MinorUnits` → Money object), PSP adapters format จาก decimal ตรง, ปรับ tests/fixtures ทั้งหมด. เพิ่ม **interim migration บน chain เดิม** (AlterColumn — จะถูกลบพร้อม chain ใน task 3, จดใน commit message). Done = ไม่มี `MinorUnits` เหลือใน src/tests + suite เขียว
     Satisfies: REQ-6 (all). Verify: `dotnet build -warnaserror && dotnet test` + grep `MinorUnits` = 0 + model assertion test คอลัมน์ decimal(19,4)/char(3).
     Evidence:
       - test: `dotnet build pol-core.slnx -warnaserror` -> 45 projects, 0 errors, 0 warnings
       - test: `dotnet test pol-core.slnx --filter "Category!=Integration"` -> 748 passed / 0 failed across 12 test projects (SharedKernel 46, Checkout 2, Producer 95, Cart 15, BuildingBlocks 65, Products 25, Payments 55, Orders 25, Tenant 31, Admin 129, Architecture 53, Hosts 207); `Integration.Tests` correctly matched 0 (all `[Trait("Category","Integration")]`, need live `POL_*_PASSWORD` env vars this session does not have — same constraint as task 1's spike, not a regression)
       - test: `grep -rniln minorunits src tests --include=*.cs | grep -v /Migrations/` -> 0 leftover outside one legitimate hit (`PspAdapterBase.FormatMinorUnitAmount`, the new Omise minor-unit wire formatter, correctly named) — case-insensitive sweep also catches camelCase locals a case-sensitive grep would miss
       - test: new `Architecture.Tests/MoneyColumnMappingTests.cs` (5 facts, EF model metadata via Sqlite, no live DB) -> confirms `Product.Price`/`CartItem.UnitPrice`/`CheckoutSession.Amount`/`Order.Amount`/`PaymentSession.Amount` all map `{Prop}Amount` precision=19/scale=4 + `{Prop}Currency` maxlength=3/fixed/non-Unicode (char(3), not nchar(3)) — all 5 pass
       - viewports: n/a — logic-only (no UI)
       - deviations: (1) application-layer commands/queries (not just wire DTOs + event contracts) also switched flat `*MinorUnits+Currency` to one `Money` field — broader than REQ-6.6's literal wording but same spirit, removes redundant `Money.Of` reconstruction since a `Money` value is valid by construction; (2) found + fixed silent semantic drift in 3 PSP adapter tests (`OmiseAdapterTests.cs`, `TwoCTwoPAdapterTests.cs`) where `Money.Of(<int>, ...)` compiled under both the old `long`-minor-units and new `decimal`-major-units overloads but meant a ~100x different real amount — adjusted literals (e.g. `25009`→`250.09m`) to preserve each test's original real-world scenario; (3) interim migration hand-patches `usp_resolve_order_summary` (`ALTER PROCEDURE` in both `Up`/`Down`, mirrors the `RenameDateColumnsDropUtc` precedent) since it raw-SQL-selects `AmountMinorUnits` by name and EF's migration auto-diff has no visibility into stored procs; (4) model-assertion test landed in `Architecture.Tests` (only project already referencing every module needed for a full `ProducerDbContext`), added `Microsoft.EntityFrameworkCore.Sqlite` PackageReference there; (5) `Integration.Tests` (`IntegrationDb.cs`, `OrdersReconciliationIntegrationTests.cs`) updated to compile + target the new column names but NOT executed against a live DB this session (no credential access) — real run is CI's/an operator's job
- [x] 3. Cutover: rename + schema + migration reset + RLS + interceptor + auth — แกน big-bang ทั้งก้อน (atomic โดยธรรมชาติ แตกย่อยแล้วไม่ green): `PolDbContext` + `SchemaNames` + `ToTable(name, schema)` ทุก entity ตาม schema map; rename ทั้งระบบ (Merchant/PlatformUser/PlatformMerchantAccess/MerchantUser + TenantId→MerchantId code/DB/wire/route/event/config ตาม rename map + F4 key `merchant_user.approve|reject`); merge module Tenant+Producer→Merchants + ลบ Identity shells; ลบ migrations 25 ตัว + snapshot → `InitialSchema`/`SecurityObjects` (RegistrationNotices raw + fn 2 ตัว tier-based + procs 3 + policy SCHEMABINDING=ON + GRANT matrix จาก tuple list)/`SeedData` (RBAC 2 catalog key ใหม่ + master data); bootstrap principals (pol_admin ออกจาก bypass + guarded DROP MEMBER, pol_resolver); interceptor 2-key stamp เสมอ + sentinel guard + `IActorContext`/`AdminActorContext` (keyed options ผ่าน DI callback คง model cache) + `AmbientActor`/`MerchantGuardBehavior`; ตัด Bearer + default scheme ใหม่ + policy `merchant-user` single-scheme ครอบ funnel ครบ (รวม summary/resend + reports/reconciliation) + cookie `__Host-mch_session` + `MerchantBoundFilter`; Architecture.Tests schema guard (exception `DataProtectionKey`→dbo) + module list ใหม่; test suite rename ทั้งหมด (Merchants.Tests merge, Hosts.Tests funnel เป็น session auth, IntegrationDb schema/column/key ใหม่). Done = fresh DB migrate จากศูนย์ + suite ทั้งหมดเขียว + funnel E2E เดิมทำงานบนชื่อใหม่
     Satisfies: REQ-1, REQ-2.1-2.6, REQ-3.1-3.9 (core implementation — exhaustive matrix + pooled-reuse proof is task 4), REQ-4, REQ-5, REQ-7.1-7.6, REQ-7.8, REQ-8, REQ-9. Depends on: 1, 2. Verify: `docker compose down -v && up` → bootstrap → `dotnet ef database update` → `dotnet build -warnaserror && dotnet test`.
     Evidence:
       - test: `dotnet build pol-core.slnx -warnaserror` -> 41 projects, 0 errors, 0 warnings
       - test: `dotnet test pol-core.slnx --filter "Category!=Integration"` -> 733 passed / 0 failed across 11 test projects (SharedKernel 46, Orders 25, BuildingBlocks 65, Cart 15, Merchants 128, Products 25, Admin 129, Payments 55, Checkout 2, Architecture 50, Hosts 193)
       - test: live-DB fresh-cutover proof — `docker compose down -v && up` (wipes `pol-db`/:11433) -> `pol-db-init` (`01-principals.sql`) exits 0 -> booted `Api` host (Development) auto-migrates via `ConnectionStrings:Migrator`, log confirms `"Applied pending EF migrations"` then `"Now listening on..."` (all 3 migrations — InitialSchema/SecurityObjects/SeedData — apply clean from zero on real SQL Server 2025); `docker compose exec pol-db sqlcmd` verify (no secrets echoed): 5/5 schemas (`admin/merch/sec/shop/txn`), 40 tables, `sec.MerchantIsolationPolicy` enabled=1, bypass-role membership exactly per T5 (`pol_admin=0, pol_app=0, pol_worker=0, pol_resolver=1, pol_vault_auditor=1`)
       - test: `dotnet test tests/Integration.Tests --filter "Category=Integration"` against that same live, freshly-migrated DB -> 64 passed / 0 failed (stable across 2 consecutive runs), incl. `RlsIsolationTests` (merchant isolation, foreign-merchant BLOCK, unbound-admin-denies + Super-bound-admin-sees-all, sysadmin-denied, webhook-resolve bypass proc, bypass-role membership), `MerchantProvisioningIntegrationTests`/`MerchantProvisioningGrantsTests` (merch.Merchants FILTER+BLOCK, vault INSERT-only, provisioning-audit admin-only), `VaultRevealAuditIntegrationTests`, `Admin*`/`MerchantUser*RbacGrantsTests`/`*SessionStoreIntegrationTests` (control-plane grant posture), `OrdersReconciliationIntegrationTests`
       - test: `bash scripts/spec-trace.sh rf1-schema-reset` -> OK, 68/68 acceptance criteria referenced in design.md+tasks.md, EARS lint clean
       - viewports: n/a — logic-only (no UI)
       - deviations: (1) `sec.fn_outbox_predicate` is a NEW function (not in design.md's literal predicate list) added to fix a genuine T5-induced bug: without it the sentinel-merchant anonymous-registration outbox write would BLOCK once `pol_admin` left `pol_rls_bypass`; carves out only the fixed sentinel merchant id, everything else defers to `sec.fn_merchant_predicate` — see its doc-comment in `SecurityObjects.cs`. (2) `sec.fn_cartitem_predicate` implemented via `CROSS APPLY` into `fn_merchant_predicate(parent.MerchantId)` rather than duplicating the bypass/Super/Scoped branches a second time — a deliberate simplification, not a literal SQL port, so a future predicate change can't drift between the two copies. (3) Found + fixed 2 stale artifacts unrelated to the rename itself but discovered while verifying it live: `BuildingBlocks.Web/HealthChecks.cs` still had `ProducerDbReadinessCheck`/check-name `"producer-db"` (missed by the `Modules/`-scoped sweep since this file lives directly under `BuildingBlocks.Web/`) — renamed to `AppDbReadinessCheck`/`"app-db"`; `Program.cs` had 3 stale doc-comment blocks describing a "dual-scheme Bearer fallback" and cookie name `__Host-prd_session` that no longer exist post-T11 (code is genuinely single-scheme `MerchantUserSession`, cookie is `__Host-mch_session`) — corrected. (4) 12 `Hosts.Tests` `WebApplicationFactory`-based files needed `ConnectionStrings:Migrator` explicitly blanked via `UseSetting` — a pre-existing test-harness hermeticity gap (Program.cs's Dev-convenience auto-migrate reads that key independently of the other pinned connection strings) that a developer machine with a real local dev DB + `appsettings.Development.json` silently exposes; not a rename regression, but genuinely blocked `dotnet test` from being green on this machine until fixed. (5) `merch.MerchantUsers` carries a direct nullable `MerchantId` column (REQ-2.3) replacing the deleted `ProducerTenantAssignments` 1:1 edge table — one now-structurally-moot unique-index integration test was removed rather than adapted (mirrors the Hosts.Tests "bearer wiring" precedent: the table it tested no longer exists). (6) `txn.PspConnections`'s pol_app grant is now SELECT-only (was full CRUD pre-rf1) per design.md's explicit GRANT matrix — an intentional T5 behavior change (PSP connections are admin-provisioned only, matching the real `/api/v1/admins/merchants` provisioning path), not a bug; it broke 3 pre-existing integration tests' setup helpers, fixed by routing merchant/PSP-connection provisioning through a newly-added `IntegrationDb.OpenAsPlatformUserAsync`/`OpenAsNewSuperUserAsync` (T5: `pol_admin` alone is no longer RLS-bypass, so writes to predicate-covered tables now need a bound Super platform user, exactly like production's `AdminActorContext`). (7) `IntegrationDb.InsertOrderAsync` was missing `SummaryToken`/`SummaryTokenExpiresAt` (its own doc-comment incorrectly claimed a migration-level default that never existed — `OrderConfiguration` only marks them required, the `Order` domain entity generates them in C#) — a pre-existing test-helper bug, unrelated to the rename, surfaced only now that this suite ran live; fixed. (8) `OrdersReconciliationIntegrationTests` used the shared fixed `MerchantA`/`MerchantB` constants for an unscoped aggregate SUM assertion, which accumulates across repeat local runs against a persistent DB (already flagged as a known flake in a prior session's memory note) — switched to fresh per-run `Guid`s, eliminating the flake class entirely. (9) `.env.integration` pointed at a completely separate, docker-compose-unmanaged, ~17-day-stale standalone container (`pol-sql`, port 11434, pre-rf1 schema, 12 tables) rather than the `pol-db`/:11433 container this task resets — operator repointed `POL_SQL_SERVER` to `localhost,11433` and synced its 4 password vars to `.env`'s bootstrap values (both done by the operator directly; permission-blocked from `.env*` files here). (10) `.env.example`/`.env.prod.example` (git-tracked templates, REQ-8.4) still reference dead config — `ConnectionStrings__Producer`, `Google__Audiences__tenant/admin` (retired Bearer multi-audience), `TENANT_GOOGLE_CLIENT_ID`, `TENANT_FRONTEND_ORIGIN` — corrected content was handed to the operator to paste in (same `.env*` permission block); **not yet confirmed applied as of this evidence**, operator action item. (11) Leftover harmless `bin`/`obj` build-artifact junk in `tests/Identity.Tests/` (gitignored, untracked, `rm -rf` blocked by the `destructive-guard.sh` hook) — cosmetic only. (12) "Funnel E2E เดิมยังเขียว" (design.md's Requirement Traceability, mapped to test vehicles "Hosts.Tests + Integration.Tests", not one dedicated end-to-end file) is interpreted here as satisfied by the AGGREGATE of both now-green suites — `Products`/`Cart`/`Checkout`/`Orders`/`Payments` domain+application tests, `RouteSchemeAuthPreservationTests`, `MerchantUserWritePermissionsTests`, `WebHardeningTests.WebhookRateLimitTests`/`ExceptionHandlerPipelineTests`, `RlsIsolationTests`, `OrdersReconciliationIntegrationTests` — rather than one new purpose-built click-through test; no such dedicated file existed pre-rename either, and building one (real OIDC session-cookie issuance end-to-end) was judged out of scope for a rename/reset task. Flag for reviewer if a literal single E2E test is still wanted. (13) REQ-4 exhaustive RLS matrix (Scoped-sees-only-assigned via PlatformMerchantAccess, pooled-connection-reuse stale-UserId proof) is task 4's explicit scope per tasks.md, not claimed complete here — task 3's `RlsIsolationTests` covers the CORE predicate branches (own-row, cross-merchant BLOCK, unbound-deny, Super-bound-sees-all, sysadmin-denied, bypass-role membership) but not the full Scoped/pooling matrix.
     **RECONCILED 2026-07-12 (task 4 pre-flight):** the paragraph above was a mid-execution checkpoint
     written 2026-07-11 while task 3 was still in flight (`task-3-handoff.md`, same folder). Work
     continued in the same/a later session past that checkpoint and task 3 was actually finished and
     committed (`0e3c787`, 473 files changed) — the Evidence block above is the real, verified
     completion record (re-confirmed live before starting task 4: `dotnet build` 41 projects/0
     errors/0 warnings, `pol-core.slnx` has no Tenant/Producer/Identity entries, `src/Modules/{Tenant,
     Producer,Identity}` contain no tracked source, migrations = exactly `InitialSchema`/
     `SecurityObjects`/`SeedData`). `task-3-handoff.md` is now a historical record of the mid-flight
     decisions only, not an active blocker — do not treat its "still pending" list as current.
- [x] 4. RLS matrix + pooled-reuse integration proof — fixture ใหม่บน :11434 (pooling เปิด): merchant A/B isolation ทุกตารางใต้ policy, Super (Tier) เห็นหมด, Scoped เห็นเฉพาะ assigned, Scoped ไม่มีแถว PMA = 0 แถว (REQ-3.11), Scoped เขียนข้าม scope + insert Merchants โดน BLOCK / Super ผ่าน, sentinel เปล่า deny, no-context = 0 แถว, `pol_admin` ไม่อยู่ใน bypass role, procs 3 ตัวทำงาน, schema owner = dbo, stale UserId ไม่รอดข้าม pooled reuse. Done = matrix test ครบทุก case เขียว
     Satisfies: REQ-3 (proof ทุก criteria — 3.1-3.11), REQ-4.1 (pooled). Depends on: 3. Verify: `dotnet test tests/Integration.Tests` บน :11433 (ดู deviation 1).
     Evidence:
       - test: `dotnet build pol-core.slnx -warnaserror` -> 41 projects, 0 errors, 0 warnings
       - test: `dotnet test pol-core.slnx --filter "Category!=Integration"` -> 733 passed / 0 failed across 11 projects (unchanged from task 3 — this task only touches `tests/Integration.Tests`)
       - test: `dotnet test tests/Integration.Tests --filter "Category=Integration"` against the live, freshly-migrated `pol-db` (`docker compose`, :11433) -> **86 passed / 0 failed** (64 pre-existing from task 3 + 22 new), stable across 2 consecutive runs. New coverage added this task, mapped to the checklist: merchant A/B isolation on every remaining FILTER+BLOCK table not already covered by task 3 (`Merchant_isolation_holds_across_the_policy_matrix` — a `[Theory]` over shop.Carts/CheckoutSessions/Orders/PaymentSessions/CartItems, 5 cases) + dedicated Facts for `PspConnection_is_invisible_across_merchants`, `IdempotencyRecord_is_invisible_across_merchants`, `VaultSecret_is_invisible_across_merchants`, `App_cannot_forge_a_foreign_merchant_vault_reveal_audit`, `Merchant_self_row_is_visible_only_to_itself`; Super/Scoped platform branches (`Super_can_insert_a_new_merchant`, `Scoped_admin_cannot_insert_a_new_merchant`, `Scoped_admin_sees_only_its_assigned_merchant`, `Scoped_admin_can_write_a_vault_secret_for_its_assigned_merchant_but_not_others`); the REQ-3.11 fail-closed regression guard (`Scoped_admin_with_no_platform_merchant_access_rows_sees_nothing` — zero PlatformMerchantAccess rows sees 0, not everything); the REQ-3.4 sentinel guard tested independently of the app-layer throw (`Explicit_empty_sentinel_with_explicit_null_user_denies`); REQ-3.5's write-side (`No_context_at_all_blocks_insert_on_a_filtered_table`); REQ-3.10 (`Every_rls_schema_is_owned_by_dbo`, all 5 schemas); the remaining 2 of 3 procs (`Order_summary_resolve_proc_finds_the_order_by_token_with_no_caller_context`, `Vault_audit_head_proc_returns_the_chain_head` — proc 1, the webhook resolver, was already covered by task 3). REQ-4.1 pooled-reuse: new `PooledConnectionReuseTests.cs` (`IntegrationDb.PooledAppConn`, `Max Pool Size=1`) — `Stale_actor_context_does_not_survive_a_pooled_connection_reuse` (same-SPID physical reuse confirmed via `@@SPID`, an un-stamped reuse sees 0 rows — physical reuse already scrubs SESSION_CONTEXT before the query runs) + `Re_stamping_after_reuse_binds_the_new_actor_cleanly_with_no_cross_contamination` (re-stamped for a different merchant on the same physical connection, no bleed-through either direction)
       - viewports: n/a — logic-only (no UI)
       - deviations: (1) **task text says ":11434", actual target is :11433** — reconciled against ground truth before writing anything: `.github/workflows/ci.yml`'s Integration-tests job already pins `POL_SQL_SERVER: localhost,11433` (committed in task 3, `0e3c787`), `IntegrationDb.cs`'s own fallback default is `"localhost,11433"`, and the gitignored `.env.integration` (source of truth for local runs) points at `localhost,11433` — all three agree. This matches task 3's own deviation (9): the operator repointed `.env.integration` away from the old, docker-compose-unmanaged, pre-rf1 `:11434` container during that task specifically because it was stale. `docker ps` during this task confirmed both containers still exist (`pol-db` on :11433, freshly reset by task 3 and healthy; `pol-sql` on :11434, still up 13 days = still pre-rf1, untouched) — running literally against :11434 would have failed against a schema that doesn't have any of these objects. Followed the current, real, already-committed config rather than the stale task wording (filesystem/CI/env are ground truth per the skill's own reconciliation rule). (2) Reconciled a second stale artifact before starting: task 3's checkbox carried a leftover mid-execution "IN PROGRESS, not done" paragraph from 2026-07-11 (`task-3-handoff.md` checkpoint) that contradicted its own Evidence block directly above it. Re-verified task 3's completion live before trusting it (task 4 `Depends on: 3`): fresh `dotnet build` → 41 projects/0/0, `pol-core.slnx` has no Tenant/Producer/Identity project entries, `src/Modules/{Tenant,Producer,Identity}` contain no tracked source (bin/obj build-artifact junk only), migrations = exactly `InitialSchema`/`SecurityObjects`/`SeedData`. Confirmed genuinely done; replaced the stale paragraph with a short reconciliation note (see task 3 above) rather than leaving the contradiction for the next reader. (3) Several new probe helpers deliberately use a FRESH `Guid.NewGuid()` merchant id per test rather than the shared `MerchantA`/`MerchantB` constants, mirroring the precedent already set by `Webhook_resolve_proc_returns_merchant_while_direct_read_stays_blocked` (task 3): `PspConnection_is_invisible_across_merchants` (UNIQUE `(MerchantId, Psp)`), `App_cannot_forge_a_foreign_merchant_vault_reveal_audit` and `Vault_audit_head_proc_returns_the_chain_head` (UNIQUE `(MerchantId, Seq)`, both probes always write `Seq=1`) — reusing the shared constants there would collide on a second run against this same persistent dev DB; verified by literally running the suite twice in a row (86/86 both times). (4) `merch.Merchants.Code` turned out to be `HasMaxLength(64)` + a DB-level `IsUnique()` index only — NOT a raw-SQL `CHECK` allowlist as design.md's prose (line 114) suggested; the `vprivilege`/`vcommerce`/`vsouvenir` allowlist is enforced by the `MerchantCode` C# value object one layer up, which these raw-SQL probes intentionally bypass. Confirmed against `MerchantConfiguration.cs` directly rather than trusting the prose; used synthetic `"probe-<guid>"` codes freely since the DB itself only demands uniqueness. (5) `usp_vault_audit_head`'s `sp_getapplock(@LockOwner=N'Transaction')` requires an explicit open transaction on the calling connection — `Vault_audit_head_proc_returns_the_chain_head` wraps the call in `conn.BeginTransaction()`/`tx.Commit()`, mirroring `VaultRevealAuditWriter.AppendViaProcAsync`'s production pattern exactly (read before writing the test, not guessed). (6) `usp_resolve_order_summary`'s first result column is `Id`, not `MerchantId` — the order-summary proc test asserts against the resolved `Id` (proves the token→row resolve works through the bypass principal) rather than trying to pull `MerchantId` out of a scalar read, which would have silently read the wrong column. (7) One pre-existing type bug caught by the live run itself: `SELECT @@SPID` returns SQL `smallint`, and the shared `AsInt` pattern from `RlsIsolationTests` does `(int)o!` (a hard cast) — `PooledConnectionReuseTests` needed `Convert.ToInt32` instead (a numeric conversion, not a cast) since `int`-from-`COUNT(*)` and `smallint`-from-`@@SPID` are different CLR boxed types; caught immediately by the first live run (2 failures, `InvalidCastException`), fixed, reran clean. (8) Did not add an explicit "Super sees every row" case per NEW table in the matrix Theory (only re-confirmed the existing `Admin_bound_as_super_sees_every_merchant` on `shop.Products` from task 3) — the Super branch of `sec.fn_merchant_predicate` has no per-table logic at all (same tier check regardless of which table's policy invoked it), so one representative proof plus the Scoped/fail-closed matrix (which DOES exercise the table-specific `MerchantId`/`Id` predicate wiring end-to-end via `PlatformMerchantAccess`) covers the real risk surface; flag for reviewer if a literal per-table Super assertion is still wanted.
- [x] 5. Fresh-DB gate เข้า CI + dev auto-migrate — CI step (หรือ script ที่ CI เรียก): container ใหม่ → bootstrap → migrate จากศูนย์ → assert raw objects ครบ (RegistrationNotices, fn 2, procs 3, policy, grants, seeds RBAC/master-data) → รัน suite; ยืนยัน dev auto-migrate block ใน Program.cs ยังทำงานกับ chain ใหม่; update `.github/workflows` ตาม conn key ใหม่. Done = CI เขียวบน chain ใหม่ตั้งแต่ commit แรกที่ merge
     Satisfies: REQ-7.7. Depends on: 3. Verify: CI run ผ่าน + assert script output.
     Evidence:
       - test: new `docker/bootstrap/assert-fresh-db.sql` (sqlcmd, `-b` exits non-zero on THROW) run via `sqlcmd -S localhost,11433 -U sa -P $POL_SA_PASSWORD -C -b -v DbName=VCentralPay -i docker/bootstrap/assert-fresh-db.sql` against the live `pol-db` (:11433, fresh-migrated by tasks 3/4, unchanged since) -> `assert-fresh-db: OK` (exit 0): 5/5 schemas owned by dbo, `merch.RegistrationNotices` table, all 3 `sec.fn_*` functions, all 3 `sec.usp_*` procs, `sec.MerchantIsolationPolicy` enabled+schema-bound, bypass-role membership exactly per T5 (pol_admin/pol_app/pol_worker=0, pol_resolver/pol_vault_auditor=1), grant floor (>=1 GRANT per runtime principal) + the 2 security-critical negative grants (pol_app cannot SELECT `merch.VaultRevealAudits`, pol_admin cannot SELECT `merch.VaultSecrets`), RBAC/master-data seed row counts exact (6/16/5 admin catalog, 12/8/10/10 HR master data, 3/7/2 merchant-user catalog)
       - test: negative control — same script with `-v DbName=master` (a real DB with none of these objects) -> `Msg 208 ... Invalid object name 'admin.AdminPermissionGroups'`, exit 1 — proves the assert genuinely fails closed instead of rubber-stamping
       - test: live dev auto-migrate re-proof this session — `dotnet run --project src/Hosts/Api --launch-profile http` (backgrounded ~25s, `timeout`/`gtimeout` unavailable on this machine so used background+sleep+kill instead), boot log confirms `"Applied pending EF migrations (Development, Migrator connection)."` -> `"Now listening on: http://localhost:5100"` -> `"Application started."`, no exceptions; port 5100 confirmed free after stop (clean shutdown) — Program.cs:338-349 unchanged since task 3's own live proof, still reads `PolDbContext` + `ConnectionStrings:Migrator` against the same 3-migration chain
       - test: `.github/workflows` conn-key audit — `git show 0e3c787 -- .github/workflows/ci.yml` shows task 3 already fully updated it (1-line diff, `ProducerDbContext`->`PolDbContext`); `grep -niE "tenant|producer" .github/workflows/*.yml` -> 0 matches in both `ci.yml` and `claude.yml` — nothing stale left for this task to change; new work was wiring the assert step in, not a conn-key fix
       - test: `dotnet build pol-core.slnx -warnaserror` -> 41 projects, 0 errors, 0 warnings
       - test: `dotnet test pol-core.slnx --filter "Category!=Integration"` -> 733 passed / 0 failed across 11 projects (unchanged from tasks 3/4 — this task touches no C#)
       - test: `dotnet test pol-core.slnx --filter "Category=Integration"` against the same live `pol-db` -> 86 passed / 0 failed (unchanged from task 4), confirming the new CI step slots in without disturbing the suite it gates
       - viewports: n/a — logic-only (no UI)
       - deviations: (1) REQ-7.3's prose says "fn 2 ตัว" but the actual `SecurityObjects` migration has 3 functions (`sec.fn_outbox_predicate` was added as task 3's own documented deviation) — the assert script checks the real 3, ground truth over stale doc count, same precedent task 3/4 already set. (2) "assert grants ครบ" is intentionally NOT a line-by-line re-encoding of the ~60-line GRANT matrix into a second hand-maintained SQL copy (that would itself become a drift risk — the exact anti-pattern `ARCHITECTURE.md` warns against); instead it's a floor check (every runtime principal has >=1 GRANT, i.e. migration didn't silently no-op) plus the 2 grants the migration's own comments call out as hard security invariants (vault write-only, no plaintext read-back) — the full behavioral matrix is already exhaustively proven by task 4's 86 Integration.Tests, which this step runs immediately before. (3) `.github/workflows` needed no conn-key change — task 3's big-bang commit already covered it; task 5's real remaining deliverable was the assert step itself. (4) Discovered but out of scope (belongs to task 6's grep gate, which explicitly covers `src/`): `src/Hosts/Api/appsettings.json`'s committed `ConnectionStrings:Producer` key + `_ConnectionStrings_note`/`_Producer_note` comments are stale post-REQ-8.4 (code reads `GetConnectionString("App")`/`("Admin")`/`("Worker")` — confirmed via `grep -rn "GetConnectionString(" src/`; the file's "Producer"/"tenant-facing"/`/api/v1/admins/tenants` wording is dead placeholder text, not a functional break, since real values come from env vars or the gitignored `appsettings.Development.json`) — flagging here so task 6 doesn't miss it.
     Satisfies: REQ-7.7. Depends on: 3. Verify: CI run ผ่าน + assert script output.
- [ ] 6. Grep gate สุดท้าย + exception list — แตกหมวดยกเว้นจาก design เป็น concrete list (ไฟล์:เหตุผล) ใน tasks evidence, รัน `grep -rEn '\b[Tt]enant|[Pp]roducer\b' src/ tests/ docker/ .github/` เก็บ leftover ทุกตัวที่ไม่อยู่ใน list. Done = grep = 0 นอก list
     Satisfies: REQ-2.7. Depends on: 3. Batch: B1. Verify: grep command ใน evidence + build/test เขียวหลังเก็บตก.
- [ ] 7. Canon docs + FE mapping + operator note — update `.ai/shared/ARCHITECTURE.md` + `CODING_STANDARDS.md` (schema layout, actor names, ถอด freeze rule `tenant-user(s)`, Money as-built = DECIMAL), `docs/runbooks/local-dev-run.md` (reset + `ConnectionStrings__App`), stale banner บน `docs/reference/*.md` ที่อ้างชื่อเก่า, สร้าง `FE-MIGRATION.md` ใน spec folder (route เก่า→ใหม่, JSON field, cookie, Money wire), operator note (.env/.env.integration/appsettings.Development.json)
     Satisfies: REQ-10. Depends on: 3. Batch: B1. Verify: อ่านทวน canon ไม่มีชื่อเก่าหลงเหลือ + FE-MIGRATION.md ครบ 4 หมวด.

## Suggested execution batches

> งานนี้ COUPLED หนัก (ทุก task แชร์ rename/model เดียวกัน) — DEFAULT = ALL IN ONE SESSION:
> `scripts/pane-loop.sh rf1-schema-reset all-in-one` (หรือ `/spec-implement all`)
> ข้อยกเว้นเดียวที่ควรพิจารณาแยก: task 1 (spike) จบก่อนเปิด session ใหญ่ — ถูกและตัดสิน go/no-go ให้ design
> Batch B1 = task 6+7 (สรุปงานเก็บกวาด text-only, แชร์ context sweep เดียวกัน) — `scripts/pane-loop.sh rf1-schema-reset 6+7`
