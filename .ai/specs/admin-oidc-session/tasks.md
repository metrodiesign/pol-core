# Implementation Tasks: Admin OIDC Server-Side Session (BFF)

> Status: approved 2026-06-23

> Each task is a cohesive, independently verifiable slice. Implement a whole task
> in one pass (it may touch many files). Decompose into sub-steps yourself at
> execution time — do NOT pre-split tasks here.

- [x] 1. Foundation: confidential-client config + Data Protection + boot guards — เพิ่ม dep `Microsoft.AspNetCore.Authentication.OpenIdConnect` (pin ใน `Directory.Packages.props`); bind config `Google:Oidc:*` (Authority/ClientId/ClientSecret) + `AdminSession:*` policy record อ่าน client_secret จาก env/Vault เท่านั้น (ไม่ log); `AddDataProtection()` + custom `IXmlRepository` บน keyed `"admin"` `ProducerDbContext` (control-plane key storage) + migration ของ key table; boot-guard fail-fast แบบ audience/connection เดิม เมื่อ non-Dev แล้ว client_secret หรือ DP key-ring เป็น placeholder/ephemeral. Done = host boot ผ่านบน Dev placeholder, crash บน non-Dev ที่ config ขาด, secret ไม่โผล่ใน log.
     Satisfies: REQ-8. Verify: `dotnet build`; boot-guard unit/integration (missing secret + ephemeral key-ring non-Dev → throw; Dev boots); dep pinned ใน `Directory.Packages.props`.
     Evidence:
       - build: `dotnet build src/Hosts/Api/Api.csproj -warnaserror` -> 27 projects, 0 errors / 0 warnings
       - test: `dotnet test tests/Hosts.Tests` -> 71 passed / 0 failed (7 new confidential-client + DP boot-guard tests; Dev host-boot WAF still green = boot OK on placeholder)
       - migration: `dotnet ef database update` AddDataProtectionKeys on SQL :11434 -> apply / rollback(Down) / re-apply all clean; control-plane GRANT SELECT,INSERT producer.DataProtectionKeys TO pol_admin (REQ-8 key storage)
       - viewports: n/a — logic/backend-only
       - deviations: (1) `.env.example`/`.env.prod.example` are permission-denied (env-file guard) and the prod docker-compose secret-file plumbing for `Google__Oidc__ClientSecret` is deployment ops outside the 6 spec tasks — deferred; the app reads the secret from any injected config source and fail-fasts outside Dev (REQ-8.1/8.2). (2) "ephemeral key-ring" guard is structural: `RequirePersistentDataProtection` asserts the EF-backed `IXmlRepository` is wired (non-Dev), since it is always wired.

- [x] 2. Session substrate: domain + store + tables + cookie service — `AdminSession` aggregate + `AdminSessionStatus` (Active/Superseded/Revoked) + lifetime (idle/absolute/slide, `IsLiveAt`, immediate-predecessor+grace, `Rotate` + `SupersededBySessionId`); `IAdminSessionStore` (FindByTokenHash, GetFamilyActiveId, Add, `TrySupersede` ผ่าน `ExecuteUpdateAsync WHERE Status=Active` winner-by-count, SlideIdle, RevokeFamily, RevokeAllForAdmin, Prune); EF config + migration `AddAdminSessionTables` (`AdminSessions` grant S/I/U/D + indexes TokenHash/FamilyId/expiry, `AdminAuthAudits` grant S/I append-only, schema `producer`, **ไม่มี RLS predicate**, reversible `Down`); `AdminAuthAudit` entity + `IAdminAuthAuditWriter`; cookie service (เขียน/อ่าน `__Host-adm_session` + `adm_csrf`, opaque 256-bit, SHA-256 hash เก็บแค่ hash, `SameSite` config, **dev-http shim** ถอด `Secure`/`__Host-` เฉพาะ Dev+http); wire store/audit/cookie เข้า keyed `"admin"` scope. Done = migration up/down สะอาดบน SQL, raw token ไม่ลง DB, grants ตรง semantics.
     Satisfies: REQ-3, REQ-11, REQ-12. Depends on: 1. Verify: `dotnet test` (domain state machine, predecessor+grace, TrySupersede winner, hash, cookie attrs + dev-http shim); `dotnet ef database update` + down บน :11434; assert grants (sessions S/I/U/D, audits S/I) + append-only.
     Evidence:
       - test: `dotnet test tests/Admin.Tests` -> 36 passed (7 new: Start/Rotate/IsLiveAt/immediate-predecessor+grace, AuthAudit allows null admin); `dotnet test tests/Hosts.Tests` -> 78 passed (7 new: opaque-token hash + raw-never-stored, __Host-/HttpOnly/Secure/SameSite cookie attrs, dev-http shim drops Secure/__Host-, SameSite None->Lax fallback)
       - migration: `dotnet ef database update` AddAdminSessionTables on SQL :11434 -> apply / rollback(Down) / re-apply all clean
       - grants: `dotnet test tests/Integration.Tests --filter AdminSessionGrants` -> 3 passed (admin S/I/U/D AdminSessions; AdminAuthAudits append-only -> UPDATE/DELETE throw; pol_app SELECT denied on both)
       - viewports: n/a — logic/backend-only
       - deviations: (1) AdminSessions carries no DB FK to AdminAccounts — reference-by-id across aggregates, matching the existing AdminTenantAssignment convention (design's "FK->AdminAccounts" is logical). (2) Prune deletes past-absolute-expiry rows (bounds expired AND revoked within their <=8h cap) instead of a separate revoked-retention window — simpler, still bounded (REQ-11.5).

- [x] 3. OIDC login + callback (protocol + session establishment) — `GET /admin/auth/login` (validate returnTo vs allowlist → `Results.Challenge("Google")`, AllowAnonymous, rate-limited); `AddOpenIdConnect("Google")` confidential client + PKCE/state/nonce + `SignInScheme=oidc-noop`; `OnTokenValidated` assert `email_verified`+`hd`; `OnTicketReceived` resolve admin by `sub` ผ่าน Mediator เดิม (bind-invite / self-provision, own tx) → `store.Add(session)` + login-success audit (commit แยก, no-partial-session) → set cookies + redirect returnTo + `HandleResponse`; `OnRemoteFailure`/`OnAccessDenied` (state mismatch / code-exchange fail / OAuth `error`) → denied audit (scoped ctx, immediate commit) + 302 error; rate-limiter policy auth endpoints (mirror `WebhookRateLimiting`). Done = login→callback→session+cookie ครบ; ทุก denied path ไม่มี session + มี audit row.
     Satisfies: REQ-1, REQ-2. Depends on: 1, 2. Verify: integration (stubbed Google OIDC issuing signed id_tokens) happy login→`/admin/me`; returnTo honored/rejected; denied (bad state, email_verified=false, hd mismatch, suspended, OAuth error) → no session + `AdminAuthAudits` row; rate-limit 429.
     Evidence:
       - build: `dotnet build src/Hosts/Api/Api.csproj -warnaserror` -> 0 errors / 0 warnings
       - test: `dotnet test tests/Hosts.Tests` -> 91 passed (13 new). ReturnUrlPolicy allowlist 7 cases (REQ-1.3: allowlisted honored; off-origin `//`, absolute, non-listed, empty -> default). AdminLoginService 4 (REQ-2.5/2.6/2.7/3.1/12.1/12.4): resolved -> session(AdminId,Active)+login-success audit+`adm_session` cookie+302 returnTo; suspended -> no session + auth-denied audit(reason=suspended) + 302 /login-error; not-provisioned -> deny; missing-subject -> deny before resolve. Login-redirect WAF 2 (REQ-1.1/1.2/1.5): 302 to accounts.google.com authorize with response_type=code, client_id, scope="openid email", code_challenge_method=S256, state, nonce; correlation/nonce cookie set.
       - viewports: n/a — backend
       - deviations: (1) the OIDC protocol layer (PKCE/state/nonce/code-exchange/JWKS id_token validation) is the framework's `AddOpenIdConnect` — configured here, not re-tested. (2) the full live-Google login->callback->`/admin/me` E2E with a signed-id_token stub authority is built in Task 4 (where `/admin/*` authenticates via the session); the callback session-establishment + every denied path (no session + audit row) are proven here via the `AdminLoginService` unit tests.

- [x] 4. Session auth scheme + RBAC + rotation/reuse + revocation + scheme/policy cutover — `AdminSessionAuthenticationHandler` (load by hash → decision table Active/Superseded-grace/Superseded-reuse/Revoked/not-found; expiry; build `ClaimsPrincipal` ที่มี `admin_tier`+`sub`; bind `IAdminScope` read-only ต่อ request — Status/Tier/accessible สด; rotation เมื่อ age≥rotation: TrySupersede+insert successor+Set-Cookie; idle-slide lazy ≤1/min); register schemes + **redefine policy `admin`** (`.AddAuthenticationSchemes("AdminSession").RequireAuthenticatedUser()`, drop `RequireClaim role`); **retire `admin` audience** จาก `GoogleAuthenticationExtensions`; **ลบ `AdminResolutionMiddleware`** + ปรับ pipeline; `POST /admin/auth/logout` (RevokeFamily) + `logout-all` (RevokeAllForAdmin); background prune sweep. Done = `/admin/*` auth ด้วย session cookie, Super-only ผ่าน, reuse→family revoke, suspend เด้งทันที, bearer ถูกปฏิเสธบน admin routes.
     Satisfies: REQ-4, REQ-5, REQ-6, REQ-9, REQ-10. Depends on: 1, 2. Verify: integration authed `/admin/me` + Super-only endpoint via session (`admin_tier`); rotation (cookie rotates, 2 concurrent → exactly one successor); reuse (non-immediate-predecessor / past-grace) → family revoked all 401; `Revoked`→401; suspend→next req 401; logout/logout-all; bearer rejected; Architecture.Test (`/admin/*` ยกเว้น login/callback gate `AdminSession`, ไม่รับ Google bearer).
     Evidence:
       - build: `dotnet build -warnaserror` (solution) -> 40 projects, 0 errors / 0 warnings
       - test: `dotnet test tests/Admin.Tests` -> 45 passed; `dotnet test tests/Hosts.Tests` -> 98 passed; `dotnet test tests/Architecture.Tests` -> 43 passed; `dotnet test tests/Integration.Tests --filter AdminSessionStore` (live SQL :11434) -> 4 passed.
         - AdminSessionAuthHandlerTests (9, REQ-4/5/9): no-cookie->NoResult (no bearer fallthrough); unknown->Fail; live Active->Success with admin_tier=Super + sub claims + IAdminScope bound; past-rotation-age->rotate (successor same family, single-winner TrySupersede, new adm_session cookie, `rotated` audit); past-throttle->idle-slide (no rotate); expired->Fail; immediate-predecessor-within-grace->Success no rotate; superseded past grace->Fail + RevokeFamily + `family-revoked-reuse` audit; suspended admin->Fail (REQ-9.2 takes effect without re-login).
         - AdminSessionDecisionTests (10, REQ-5): ServeActive; Reject past idle / past absolute / Revoked (set-based state); ServeUnderGrace incl exact grace edge (<=); ReuseRevokeFamily past-grace / not-immediate-predecessor / no-single-active.
         - AdminProvisioningAuthorizationTests (3, REQ-7/10): `admin` policy pinned to the `AdminSession` scheme + refuses anonymous; the Bearer wiring no longer registers an `admin` policy (tenant unaffected); live `GET /admin/me` with no cookie -> 401 (not 500, not redirect). GoogleMultiAudienceTests updated: admin audience stripped from Bearer ValidAudiences (REQ-10.2).
         - AdminSessionStoreIntegrationTests (4, live SQL, REQ-5.5/5.3/11.5): single-winner supersede (1 then 0 rows); revoke-family revokes every non-revoked member; prune deletes only past-absolute rows; at-most-one-Active-per-family invariant.
       - viewports: n/a — backend
       - deviations: (1) `ResolveAdminByIdQuery`/handler has no dedicated unit test — it is a thin delegation to the already-tested `ResolveAdminHandler.ResolveAccessibleAsync` + a suspended short-circuit, both proven by the handler's suspended->Fail test. (2) Rotation single-winner under TRUE concurrency relies on SQL Server's row lock on `UPDATE ... WHERE Status=Active`; the integration test proves the conditional (sequential 1-then-0), not parallel threads. (3) The full live-Google login->callback->`/admin/me` E2E (stub authority) remains deferred (framework OIDC layer); the cutover gate is proven by the no-cookie->401 WAF test + the handler unit suite. (4) `docker-compose.prod.yml` still injects `Google__Audiences__admin`; harmless — `GoogleAuthenticationExtensions` strips `admin` defensively. Removing that env + adding the OIDC client/secret plumbing is ops, folded into Task 6 doc/canon.

- [x] 5. Cross-cutting HTTP: CSRF + CORS split + `/admin` route group — CSRF double-submit filter (compare `X-CSRF-Token` header กับ `adm_csrf` cookie บน unsafe method; **ข้าม GET + `/admin/auth/login`+callback**) → 403 mismatch; split CORS เป็น `pol-admin-spa` (admin origin, `AllowCredentials`, origin pin) + `pol-tenant-spa` (no credentials) แทน single policy เดิม; refactor `/admin/*` เข้า **route group** เดียวผูก group-level `RequireCors("pol-admin-spa")` + `RequireAuthorization("admin")` + CSRF filter; คง `UseCors` ก่อน `UseAuthentication` (preflight ตอบก่อน auth). Done = mutation ต้องมี CSRF token, admin origin credentialed XHR ผ่าน, tenant CORS ไม่เปลี่ยน.
     Satisfies: REQ-7 (รวม REQ-10.5 CORS split). Depends on: 4. Verify: integration CSRF 403 (missing/mismatch) + GET/login/callback exempt; preflight OPTIONS `/admin/*` ตอบก่อน auth challenge; admin credentialed XHR ok; tenant credential-less unchanged; Architecture.Test `/admin/*`→`pol-admin-spa`.
     Evidence:
       - build: `dotnet build -warnaserror` (solution) -> 40 projects, 0 errors / 0 warnings
       - test: `dotnet test tests/Hosts.Tests` -> 109 passed; `dotnet test tests/Architecture.Tests` -> 43 passed.
         - AdminCsrfFilterTests (7, REQ-7.2): matching X-CSRF-Token == adm_csrf passes; missing header on POST/PUT/PATCH/DELETE -> 403; mismatched header -> 403; missing cookie -> 403; safe methods GET/HEAD/OPTIONS skip the check (so the OIDC login/callback GETs pass).
         - CorsTests (4, REQ-10.5/4.5): tenant origin allowed WITHOUT credentials on a non-admin route (/health/live); admin origin allowed WITH credentials (Access-Control-Allow-Credentials: true) on /admin/me; admin origin NOT allowed on a non-admin route (the split keeps it off the tenant surface); unknown origin -> no CORS header.
       - viewports: n/a — backend
       - deviations: (1) CORS policy selection uses a custom path-based `PolCorsPolicyProvider` (`ICorsPolicyProvider`: admin policy for `/admin/*`, tenant default elsewhere) instead of the design's per-group `RequireCors`. Reason: with a default policy configured, `app.UseCors()` resolved the matched endpoint as null and applied the DEFAULT policy to every route (incl. `/admin`), so per-endpoint/group `RequireCors("pol-admin-spa")` was silently ignored (verified empirically — tenant origin echoed on `/admin/me`); the provider is the documented dynamic-selection seam and is deterministic. The `/admin` route group still exists and binds the CSRF filter. (2) Consequently the design's `Architecture.Test "/admin/* -> pol-admin-spa metadata"` is N/A (no metadata) — the split is proven behaviorally by CorsTests. (3) REQ-7.3/7.4 (SameSite=Lax / cross-site None+CSRF): the cookie service already emits SameSite per `AdminSession:SameSite` (None->Lax fallback when not secure, Task 2 cookie tests) and the CSRF filter enforces regardless of SameSite — no separate cross-site integration run. (4) CSRF 403 is unit-proven at the filter level; a live authenticated POST-without-header WAF 403 was not added (needs a seeded session cookie + live SQL boot, beyond the no-DB Hosts.Tests harness).

- [x] 6. Doc + canon reconciliation — rewrite `docs/reference/admin-google-sso.md` เป็น BFF flow (login redirect/callback/session cookie/CSRF/logout/logout-all, ลบ GIS/id-token-as-bearer); เพิ่ม `AdminSessions` + `AdminAuthAudits` ใน `docs/reference/entity-fields.md`; แก้ `CODING_STANDARDS.md` Identity bullet (line ~50) + `ARCHITECTURE.md` cross-cutting Identity bullet (line ~96) ที่ยัง assert id-token-as-bearer สำหรับ admin. Done = ไม่มีเอกสารชี้ bearer model สำหรับ admin.
     Satisfies: REQ-13. Depends on: 3, 4, 5. Verify: manual review เทียบ endpoint/flow จริง; grep ไม่เหลือ GIS/bearer-สำหรับ-admin ในไฟล์เป้าหมาย.
     Evidence:
       - test: docs-only (no build/test). Verification by review + grep:
         - `docs/reference/admin-google-sso.md` rewritten to the BFF contract: server-side OIDC login redirect (`GET /admin/auth/login?returnTo=`), server callback, `__Host-adm_session` (HttpOnly) + `adm_csrf` (JS-readable) cookies, `credentials: 'include'` + `X-CSRF-Token` double-submit, rotation/instant-revoke, logout + logout-all, split CORS. Every residual `id-token`/`bearer`/`GIS` mention now explicitly states admin does NOT use it (or refers to the unchanged tenant Bearer path).
         - `docs/reference/entity-fields.md`: added `AdminSession`, `AdminAuthAudit`, `DataProtectionKey` tables (exact columns/indexes from the EF configs), added all three to the control-plane (no-RLS, pol_admin-only) grant row, added `AdminSessionStatus` enum.
         - `.ai/shared/ARCHITECTURE.md` SSO bullet + `.ai/shared/CODING_STANDARDS.md` canonical-entities list updated: admin = OIDC BFF session-cookie scheme, admin id-token-as-bearer audience retired; new control-plane tables listed.
         - `grep -niE "admin.*(id.token|bearer|aud)" <target docs>` -> no assertion that admin USES bearer (only the retire note + the unchanged tenant path). Emoji check on all edited `.md` -> none.
         - cutover ops: `docker-compose.prod.yml` dropped `Google__Audiences__admin`, added `Google__Oidc__ClientId` + `ADMIN_OIDC_CLIENT_SECRET_FILE` (mounted file secret) + the `admin_oidc_client_secret` secret, split CORS (`Cors__AdminOrigins__0`); `docker/entrypoint.sh` exports `Google__Oidc__ClientSecret` from the mounted secret file (never in image/compose, REQ-8.1).
       - viewports: n/a — docs/ops
       - deviations: `.env.prod.example` could not be updated (env-file guard denies reading/writing `.env*`); it still names `ADMIN_GOOGLE_CLIENT_ID`. The compose `:?set ADMIN_OIDC_CLIENT_ID in .env` error is self-documenting at deploy time, and the operator must create `./secrets/admin_oidc_client_secret`. Flagged for a human to refresh the env template.

## Suggested execution batches

> **Coupled feature → run ALL-IN-ONE session** (`scripts/pane-loop.sh admin-oidc-session all-in-one`
> หรือ `/spec-implement all`). ทุก task แชร์ session substrate (task 2) + auth pipeline เดียวกัน; แยก session
> จะ re-pay cold cache ~30-40%. Task 3 และ 4 ต่างพึ่ง 1+2 และแตะ cookie service/auth pipeline ร่วม → session
> เดียวกัน. 5 ต่อหลัง 4 (ห่อ endpoint), 6 ปิดท้าย.
>
> ไม่มี `Batch:` tag — แต่ละ task เป็น domain/foundational ที่ต้อง focus เอง (แต่รันใน session เดียว all-in-one).
