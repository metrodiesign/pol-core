# Implementation Tasks: products-sp-gateway

> Status: approved 2026-07-31

> Each task is a cohesive, independently verifiable slice. Implement a whole task
> in one pass (it may touch many files). Decompose into sub-steps yourself at
> execution time — do NOT pre-split tasks here.
> อ่าน design.md ก่อนเริ่มทุก task — โครง SP 10 ขั้น, error mapping, upsert semantics
> ถูกตัดสินแล้วทั้งหมด ห้าม re-derive

- [x] 1. Database จำลอง hippodb/mammothdb + bootstrap wiring ทุก environment — `docker/bootstrap/02-external-sim.sql` ครบทั้งไฟล์ (CREATE DATABASE x2, `dbo.Documents` + filtered unique DocumentNo, SP 2 ตัวตามโครง 10 ขั้นใน design — validate fixed order + COLLATE BIN2 + window ต่อแถว + materialize `#page` + LIKE escape, principals/GRANT EXECUTE, seed deterministic prefix แยกฝั่ง `77xxx-`/`88xxx-`, self-check) + จุดเสียบ: `docker-compose.yml` `pol-db-init` (คำสั่งใหม่มี `-C -b`), `docker/migrate-entrypoint.sh` (`-N -b`, หลัง 01) + อัปเดต `docker/migrate-entrypoint.test.sh` (assert 02 รันหลัง 01, มี `-N`, ไม่มี `-C`), `.github/workflows/ci.yml` + `.gitlab-ci.yml` job integration รัน 02 ก่อน `dotnet test` — done = fresh `docker compose down -v && up` ผ่าน + sqlcmd smoke EXEC ทั้ง 2 SP ได้ + suite เดิมเขียว ไม่มี app change
     Satisfies: REQ-1 (ทั้งหมด), REQ-2 (ทั้งหมด — ตัว implementation), REQ-3 (ทั้งหมด). Verify: `docker compose down -v && docker compose up -d && bash docker/migrate-entrypoint.test.sh` + sqlcmd EXEC smoke ทั้ง 2 SP ด้วย pol_app.
     Evidence: `docker compose down -v && docker compose up -d` (ลบ volume จริง สร้าง DB ใหม่ทั้งหมด) -> `pol-core-pol-db-init-1` exited 0, log = `02-external-sim: hippodb OK (34 documents, 28 in the default search window).` / `02-external-sim: mammothdb OK (32 documents, 27 in the default search window).` / `02-external-sim: OK.`; รันสคริปต์ซ้ำอีก 2 รอบบน instance เดิม exit 0 ทั้งคู่ ค่าเท่าเดิม (idempotent); `bash docker/migrate-entrypoint.test.sh` -> `pass=34 fail=0` (เพิ่ม 6 assertion: 02 ถูกเรียก, ลำดับหลัง 01, `-N`, `-b`, ไม่มี `-C`); sqlcmd smoke ด้วย login `pol_app` (พิสูจน์ GRANT ไปในตัว) -> `EXEC dbo.usp_Motor_SearchDocument @BranchCode='100',@SaleCode='77001'` คืน RS1 `28|2|1|25|1|0|EXACT|6` + RS2 25 แถว, `EXEC dbo.usp_NonMotor_SearchDocument @BranchCode='100',@SaleCode='S001'` คืน `27|2|1|25|1|0|EXACT|6` + RS2 25 แถว, error case `@CountMode='X'` -> `Msg 50006 ... Invalid CountMode.` และยืนยันแยกว่า `sqlcmd -b` คืน exit 1 เมื่อ THROW (self-check ไม่ silent-fail); เช็คสัญญาเพิ่มด้วยมือ: cap 25 (`@PageSize=100` -> PageSize 25), หน้าเกินท้ายชุด (`@PageNo=99` -> `28|2|99|25|0|1` + RS2 ว่าง), FAST -> `NULL|NULL|...|FAST`, RENEWAL window ทั้ง 2 ฝั่ง, ทะเบียนรถเจอเฉพาะ Motor, LIKE escape `100%`, `@PaidDateFrom` บังคับ PAID, case-sensitivity -> 50007, multi-invalid -> 50004 ตาม fixed order; ระบบกลับสภาพ: `dotnet ef database update` -> `Done.`, `scripts/seed-demo.sh` -> `seed-demo: OK.` (shop.Products = 500), `dotnet test pol-core.slnx --filter Category=Integration` -> `Passed! Failed: 0, Passed: 47`. Viewports 375/768/1440: n/a (ไม่มี UI — SQL + bootstrap wiring ล้วน). Deviations: (1) database สร้างด้วย `COLLATE Thai_CI_AS` เพราะ §5.2 กำหนด `DocumentNo varchar(150)` แต่เลขเอกสารมีอักษรไทย — collation ปริยาย (CP1252) จะทำให้เป็น `?` เงียบ ๆ จึงคงชนิดตาม §5.2 แล้วเปลี่ยน collation แทน + เพิ่ม self-check ยืนยัน round-trip; (2) `IF ... CREATE DATABASE` ตาม snippet ใน design ใช้ไม่ได้ (ต้องเป็น statement เดียวใน batch) จึงห่อ `EXEC()` แบบเดียวกับ `01-principals.sql`; (3) `TOP` + `OFFSET` ในนิพจน์เดียวกันผิด syntax จึงใช้ `OFFSET ... ROWS FETCH NEXT (@PageSize + 1) ROWS ONLY` ความหมายเดียวกับที่ design เขียน; (4) เพิ่ม temp table `#match` ก่อน `#page` เพื่อให้ predicate เขียนที่เดียว (ไม่งั้น EXACT count กับหน้าเพจต้องเขียน predicate ซ้ำ 4 ชุดแล้ว drift เงียบ ๆ) และ TotalRows ของหน้าเกินท้ายชุดยังถูก; (5) ขอบบน coverage เทียบ `< DATEADD(day, 1, @To)` ให้ inclusive ครอบทั้งวัน เพราะพารามิเตอร์เป็น `date` แต่คอลัมน์เป็น `datetime2(0)`; (6) NonMotor SP คืน `LicensePlateNumber` เป็น NULL คงที่ตาม §5.2 และ seed ฝั่ง mammothdb ใส่ค่าจริงไว้ 1 แถวเพื่อให้ contract test พิสูจน์การกันคอลัมน์นี้ได้; (7) seed ล้างด้วย `DELETE FROM dbo.Documents` ทั้งตาราง ไม่ใช่ลบตาม prefix แบบ `seed-demo.sql` (database จำลองล้วน ไม่มีข้อมูลอื่นให้รักษา) — รายละเอียดทั้งหมด + ค่า seed ที่ task 2 ต้องใช้ อยู่ใน `HANDOFF.md` section Task 1

- [x] 2. SP contract tests — `tests/Integration.Tests/SpDocumentContractTests.cs` + helper `IntegrationDb.ForCatalog(string)` ยิง SP ตรงด้วย connection `pol_app` พิสูจน์ contract ของ task 1 ครบตามตาราง Testing Strategy: normalization defaults, cap 25, THROW ครบ 9 + ลำดับ multi-invalid, 2 RS ตามลำดับ + ชื่อคอลัมน์, FAST (totals NULL, RS2 <= PageSize, HasNextPage), ทะเบียนรถเฉพาะ Motor, M1 (PaymentStatus ผิด + PaidDateFrom -> 50007 ยังยิง), M3 (RENEWAL window ต่อแถวเมื่อ ALL), หน้าเกินท้ายชุด, case-sensitivity, LIKE escape, predicate REQ-2.12 ทุกตัว
     Satisfies: REQ-2 (ทั้งหมด — ตัวพิสูจน์), REQ-3.1, REQ-10.1. Depends on: 1. Verify: `dotnet test tests/Integration.Tests --filter Category=Integration` (source `.env.integration` ใน call เดียวกัน) เขียว.
     Evidence: `source .env.integration && dotnet test tests/Integration.Tests/Integration.Tests.csproj --filter Category=Integration` -> `Passed!  - Failed:     0, Passed:    91, Skipped:     0, Total:    91` (เดิม 47 + ใหม่ 44 case จาก 24 test method: 20 `[Theory]` รันทั้ง Motor/NonMotor = 40 case + 4 `[Fact]` เฉพาะฝั่ง — 3 Motor, 1 NonMotor); `dotnet build tests/Integration.Tests/Integration.Tests.csproj -warnaserror` -> 6 projects, 0 errors, 0 warnings. ครอบตามตาราง Testing Strategy ครบทุกช่อง: normalization defaults (ส่งแค่ 2 พารามิเตอร์บังคับ -> UNPAID/ALL/ALL/EXACT/PageNo 1/PageSize 25/SearchWindowMonths 6 + TotalRows 28/27 พิสูจน์ว่า default คือ UNPAID ไม่ใช่ ALL), `@PageNo=-5`+`@PageSize=0` -> 1/25, cap 25 (`@PageSize=100`), THROW ครบ 9 หมายเลข (assert `SqlException.Number`) รวม blank กับ NULL แยกกันสำหรับ 50004/50005, ลำดับ multi-invalid ตาม fixed order ยิงครบทั้ง chain 9 ขั้น (ส่ง input ผิดพร้อมกันทุกตัวแล้วถอดทีละตัว -> 50004 -> 50005 -> 50001 -> 50002 -> 50007 -> 50006 -> 50003 -> 50008 -> 50009), M1 (`@PaymentStatus='XXX'` + `@PaidDateFrom` -> 50007 ยังยิง), case-sensitivity ทั้ง 4 enum (`unpaid`/`exact`/`policy`/`cmi`) -> เลขตรง, 2 result sets ตามลำดับ + RS1 หนึ่งแถวเป๊ะ + ไม่มี RS ที่สาม (assert อยู่ใน helper ทุกการเรียก) + ชื่อคอลัมน์ครบตามลำดับ §5.1 (8) และ §5.2 (32 รวม InsuranceType/SourceSystem), FAST (TotalRows/TotalPages เป็น DBNull, RS2 = 25 แถว, `@PageSize=3` -> 3 แถวพิสูจน์ว่าแถวที่ +1 ไม่รั่ว, หน้าสุดท้าย HasNextPage 0), หน้าเกินท้ายชุด (`@PageNo=99` -> `28|2|99|25|0|1` + RS2 ว่าง), paging seam (หน้า 1 + หน้า 2 = TotalRows แถวไม่ซ้ำและเรียง ordinal ต่อเนื่อง), predicate REQ-2.12 ครบ: SaleCode exact scope (ขอ SaleCode ของอีกฝั่ง -> ได้แถวปนแถวเดียว), `@BranchCode` validate-only (100/400/999 -> TotalRows เท่ากันทั้งสามค่า แม้ 999 ไม่ตรงแถวไหนเลย), DocumentType exact (APPLICATION/RENEWAL -> 1 แถวที่ระบุ), ProductGroup filter บน SourceSystem + สองกลุ่มรวมกัน = TotalRows พอดี, `@PolicyNo`/`@ApplicationNo` exact (ตัดท้าย 1 ตัวอักษร -> 0 แถว), coverage inclusive ทั้ง 4 ขอบ (บีบ from=to ลงวันเดียว -> ได้แถวขอบ), `@PaidDateFrom` บังคับ PAID ทั้งที่ default เป็น UNPAID, LIKE escape (`@InsuredName` = `%` / `_` / `100%` -> ได้แถวที่มีอักขระจริงแถวเดียว ทั้งที่ถ้าไม่ escape `%` จะได้ทั้งหน้า), M3 window ต่อแถวเมื่อ DocumentType=ALL (`@SearchText='95000'`/`'96000'` ดึงแถว RENEWAL ทั้งใน/นอกกรอบมาชนกัน -> Motor เก็บ 950004 ทิ้ง 950005/950006 = ตัดสินด้วย EndDate, NonMotor เก็บ 960004 ทิ้ง 960005 = ตัดสินด้วย StartDate ทุก type), ทะเบียนรถ (`'9ฮฮ 9999'` + `'9ฮฮ'` เจอบน Motor; `'8ฮฮ 8888'` บน NonMotor 0 แถว และคอลัมน์ LicensePlateNumber ฝั่ง NonMotor เป็น NULL ทุกแถวรวมแถวที่เก็บค่าจริงไว้), ขอบ 6 เดือนพอดี (950013 inclusive), ลำดับ ORDER BY ตามอักษรไทย (หน้า 2 ของ Motor = `950120`,`950004`,`950014` ตามลำดับนั้น). พิสูจน์ว่า assertion กัดจริงด้วย mutation ชั่วคราว 2 จุด (`TotalRows: 28 -> 29`, `RenewalKeptSeq: 950004 -> 950005`) -> `Failed: 8, Passed: 36` แล้ว revert กลับ + รัน suite ซ้ำเขียว 91/91. Viewports 375/768/1440: n/a (ไม่มี UI — integration test ล้วน). Deviations: ไม่มี — ไม่พบ behavior ของ SP ที่ขัด design/requirements จึงไม่ต้องแก้ `02-external-sim.sql`; จุดเดียวที่ปรับจากถ้อยคำใน task คือใช้ `@InsuredName` (ไม่ใช่ `@SearchText`) เป็นตัวพิสูจน์ LIKE escape เพราะ §3/§4 ไม่ได้ให้ `@SearchText` ค้น `ShowName` (ตรงกับที่ HANDOFF task 1 ยิงมือไว้) และเสริมด้วย `%`/`_` เดี่ยว ๆ ซึ่งเป็นตัวพิสูจน์ที่เด็ดขาดกว่า `100%`.

- [x] 3. ชั้น port + wire DTO + exception mapping — `Products.Application/Ports/` (`ISpDocumentGateway`, `SpDocumentContracts.cs` ตาม signature ใน design — ไม่มี BranchCode บน request, `SpDocumentSearchRejectedException`) + `BuildingBlocks.Application/UpstreamUnavailableException` + arm 503 ใน `BuildingBlocks.Web/ProblemDetailsExceptionHandler.Map()` + unit tests ของ exception mapping — done = build เขียว ยังไม่มี consumer
     Satisfies: REQ-4.1, REQ-4.2, REQ-4.3, REQ-4.4, REQ-4.5, REQ-4.6. Verify: `dotnet build pol-core.slnx -warnaserror` + unit test ใหม่เขียว.
     Evidence: ไฟล์ใหม่ 4 + แก้ 2 (commit `69ff065`, 179 insertions ไม่มี deletion) — `src/Modules/Products/Products.Application/Ports/{ISpDocumentGateway,SpDocumentContracts,SpDocumentSearchRejectedException}.cs`, `src/BuildingBlocks/BuildingBlocks.Application/UpstreamUnavailableException.cs`, `+2 บรรทัดใน src/BuildingBlocks/BuildingBlocks.Web/ProblemDetailsExceptionHandler.cs`, `+17 บรรทัดใน tests/Hosts.Tests/ProblemDetailsExceptionHandlerTests.cs`. `dotnet build pol-core.slnx -warnaserror` -> `64 projects, 0 errors, 0 warnings`; `dotnet test tests/Hosts.Tests --filter FullyQualifiedName~ProblemDetailsExceptionHandlerTests` -> `Passed! Failed: 0, Passed: 13` (เดิม 9); `dotnet test tests/Hosts.Tests` เต็มโปรเจกต์ -> `Passed! Failed: 0, Passed: 365`; `dotnet test tests/Architecture.Tests` -> `Passed! Failed: 0, Passed: 225` (boundary rule เดิมยังถือ ไม่ต้องแก้); `dotnet test tests/Products.Tests` -> `Passed! 54`; `dotnet test tests/BuildingBlocks.Tests` -> `Passed! 43`; `bash scripts/check-rename-identifiers.sh` -> `rename-identifier gate: OK`. REQ ที่พิสูจน์: REQ-4.1 port เมธอดเดียว `SearchAsync(SpDocumentSearchRequest, CancellationToken)` คืน `SpDocumentSearchResult`; REQ-4.2 request 17 พารามิเตอร์ + `Target` ไม่มี `BranchCode` (นับด้วยตาแล้วเทียบ §2 ทีละตัว); REQ-4.3 `SpPaginationMetadata` 8 field ตรง §5.1 โดย `TotalRows`/`TotalPages` เป็น `long?`; REQ-4.4 `SpDocumentItem` 32 field nullable ทั้งหมด ลำดับตรง `ExpectedItemColumns` ของ `SpDocumentContractTests` เป๊ะ (คัดลอกลำดับจาก contract test ที่ pin ลำดับจริงของ RS2 ไว้แล้ว — `InsuranceType` ตัวแรก `PaymentStatus` ปิดท้าย); REQ-4.5 `SpDocumentSearchRejectedException : ArgumentException` พก `SpErrorNumber` ผ่าน bucket `ArgumentException` เดิม ไม่มี arm ของตัวเอง (ยืนยันจากลำดับ pattern matching ใน `Map()`: arm เดียวที่ match ได้ก่อนคือ `ArgumentException` บรรทัด 66 — `NotFoundException`/`Gone`/`Concurrency`/`Conflict`/`MerchantBinding` ไม่อยู่บน chain การสืบทอด) + test `A_rejected_sp_search_carries_its_error_number` assert `SpErrorNumber == 50007` และ `IsAssignableFrom<ArgumentException>`; REQ-4.6 `UpstreamUnavailableException` -> `503` + fixed detail `"Upstream dependency unavailable"` และเข้า `OpaqueBuckets` theory เดิมที่ assert ว่า body ไม่มีข้อความ exception (คง fixed-detail principle ของ handler ไม่ echo message). พิสูจน์ว่า assertion กัดจริงด้วย mutation ชั่วคราวบน production arm (`Status503ServiceUnavailable` -> `Status502BadGateway`) -> `Failed: 1, Passed: 12` (`Expected: 503 / Actual: 502`) แล้ว revert + รันซ้ำเขียว 13/13. Viewports 375/768/1440: n/a (ไม่มี UI — ชั้น type + exception mapping ล้วน). Deviations: ไม่มีจาก design — signature ทุก record ตรงตาม design ตัวต่อตัว; จุดที่ตัดสินเองภายในกรอบ task: (1) แยก `SpDocumentSearchRejectedException` เป็นไฟล์ของตัวเองตาม convention ของ `Payments.Application/Ports/PspRejectedException.cs` แทนที่จะรวมใน `SpDocumentContracts.cs`, (2) unit test ทั้งสองเคสอยู่ใน `tests/Hosts.Tests/ProblemDetailsExceptionHandlerTests.cs` ที่มีอยู่แล้ว (ไม่สร้างไฟล์ใหม่) — ทำให้ Hosts.Tests อ้าง namespace `Products.Application.Ports` ซึ่ง task 6 ต้องรู้ตอนเขียน insulation guard REQ-10.5 (ดู HANDOFF section Task 3)

- [x] 4. Domain + mapper — `ProductInput` เพิ่ม `PaymentStatus`/`PaidDate` (B1; ไล่แก้ caller ทุกจุดรวม `CreateProductCommand` + tests + seed path), `Product.Create` honor wire PAID/PaidDate, refactor `ApplyFields` ร่วม, `RefreshFromExternal` (DocumentNo guard + side-flip guard + no-downgrade semantics), `SpDocumentItemMapper` (skip row + dedupe + SourceSystem->ProductGroup + ignore wire InsuranceType + naive datetime) + unit tests ครบ (`ProductTests`, `SpDocumentItemMapperTests`)
     Satisfies: REQ-7.3, REQ-7.4, REQ-7.5, REQ-7.6, REQ-7.7, REQ-7.10. Depends on: 3. Verify: `dotnet test tests/Products.Tests` เขียว + build -warnaserror.
     Evidence: commit `e995b65` — ไฟล์ใหม่ 2 (`src/Modules/Products/Products.Application/Ports/SpDocumentItemMapper.cs`, `tests/Products.Tests/SpDocumentItemMapperTests.cs`) + แก้ 10 (`Products.Domain/{Product,ProductInput}.cs` + caller 8 จุดใน 6 ไฟล์ test). `dotnet build pol-core.slnx -warnaserror` -> `64 projects, 0 errors, 0 warnings`; `dotnet test tests/Products.Tests` -> `Passed! Failed: 0, Passed: 102` (เดิม 54); `dotnet test pol-core.slnx --filter "Category!=Integration"` -> เขียวทั้ง 16 โปรเจกต์ (Hosts.Tests 365, Architecture.Tests 225, Payments.Tests 162, Merchants.Tests 120, Carts.Tests 15 ฯลฯ ไม่มี Failed); `bash scripts/check-rename-identifiers.sh` -> `rename-identifier gate: OK`. REQ ที่พิสูจน์: REQ-7.3 `RefreshFromExternal` เรียก `ApplyFields` ตัวเดียวกับ `Create` (validation ชุดเดิมครบ + เพิ่ม `Enum.IsDefined(PaymentStatus)`) และมี 2 guard — DocumentNo ไม่ตรง (เทียบหลัง trim) กับ ProductGroup ข้ามฝั่ง InsuranceType (VMI->FIRE throw; VMI<->CMI, FIRE<->MISC ผ่าน ยืนยันด้วย `[Theory]` 4 เคส) + test พิสูจน์ว่า validation ของ Create ยังกัดผ่าน Refresh 4 ข้อ (ทศนิยมเกิน 2 ตำแหน่ง, TotalPremium 0, CMI+APPLICATION, StartDate>EndDate); REQ-7.4 local PAID + wire UNPAID -> คง PAID + PaidDate เดิม ขณะที่ TotalPremium ยัง update; REQ-7.5 wire PAID + PaidDate -> set ทั้งคู่; REQ-7.6 wire PAID ไม่มี PaidDate -> คง PaidDate เดิม (และ `Create` ยอมให้ PAID + PaidDate null); REQ-7.7 mapper ข้ามแถวพร้อมเหตุผล: DocumentNo/SaleCode ว่าง (null/""/ช่องว่างล้วน), TotalPremium null/0/ติดลบ, enum ไม่ตรง wire แบบ case-sensitive (`vmi`/`policy`/`paid`/`ALL`/`"0"`/`"CMI, VMI"` ถูกข้ามหมด) + dedupe DocumentNo แถวแรกชนะ และแถวที่ถูก skip ไม่จอง key (แถวดีถัดไปยังผ่าน) + คงลำดับแถวของ SP; REQ-7.10 `ProductGroup` มาจาก `SourceSystem` และ wire `InsuranceType` ถูก ignore (test ส่ง `InsuranceType="NonMotor"` คู่กับ `SourceSystem="VMI"` -> ได้ `ProductGroup.VMI`); B1 `Create` honor PAID/PaidDate จาก input (เลิก hardcode UNPAID/null). พิสูจน์ว่า assertion กัดจริงด้วย mutation ชั่วคราว 2 จุด: (1) `var paid = PaymentStatus is PAID || input...` -> `input.PaymentStatus is PAID` (2) สลับ `CommissionAmount`/`CommissionPercent` ใน mapper -> `Failed: 2, Passed: 100` โดยแดงตรงตัวที่ตั้งใจ (`RefreshFromExternal_never_downgrades_a_local_PAID`, `Every_field_of_a_row_reaches_the_ProductInput`) แล้ว revert + รันซ้ำเขียว 102/102. Viewports 375/768/1440: n/a (ไม่มี UI — domain + mapper ล้วน). Deviations: (1) `PaymentStatus`/`PaidDate` วางไว้ในกลุ่มพารามิเตอร์บังคับ (ต่อจาก `TotalPremium`) ไม่ใช่ท้าย record ตามถ้อยคำใน task — C# ห้ามพารามิเตอร์บังคับตามหลังพารามิเตอร์ที่มี default และ default `UNPAID` คือช่องเงียบเดียวกับที่ B1 เกิด จึงเลือกให้ทุก construction site ต้องระบุเอง (caller เดิมทุกจุดส่ง `PaymentStatus.UNPAID, null` ตาม task); (2) `ApplyFields` normalize `PaidDate` เป็น null เมื่อสถานะเป็น UNPAID เพื่อรักษา invariant ที่ `Product.PaidDate` ประกาศไว้ ("null while UNPAID") — wire UNPAID ที่พก PaidDate มาจึงไม่ทำให้ field ขัดกันเอง; (3) mapper ใช้ switch expression ต่อ enum แทน `Enum.TryParse` เพราะ TryParse รับ `"0"` และ `"CMI, VMI"` เป็นสมาชิกด้วย (มี test คุมทั้งสองรูปแบบ); (4) `MappedSpDocument` พก `SkipReason` เพิ่มจาก shape `(item, ProductInput?)` ใน design เพื่อให้ task 6 log ได้ว่าข้ามเพราะอะไร — รายละเอียด + shape ที่ task 6 ต้องใช้อยู่ใน `HANDOFF.md` section Task 4

- [x] 5. Adapter จริง — `Products.Infrastructure/Sp/{SpDocumentOptions,SpDocumentGateway}.cs` (typed params 17+@BranchCode, RS order + GetOrdinal, error mapping M8-hardened: cancellation passthrough / 5000x -> Rejected / drift -> Upstream), `Microsoft.Data.SqlClient` pin exact ใน `Directory.Packages.props` + csproj, `PostConfigure` derive connection ใน `Program.cs`, DI ใน `ProductsModuleRegistration` + `tests/Integration.Tests/SpDocumentGatewayIntegrationTests.cs` (mapping ทั้ง 2 ฝั่ง, 50006 -> exception เลขตรง, @BranchCode จาก options)
     Satisfies: REQ-5 (ทั้งหมด), REQ-6.6, REQ-10.2. Depends on: 1, 3. Verify: integration tests เขียว + Hosts.Tests ยัง boot ได้ (`dotnet test tests/Hosts.Tests`).
     Evidence: commit `727ebd2` — ไฟล์ใหม่ 3 (`src/Modules/Products/Products.Infrastructure/Sp/{SpDocumentOptions,SpDocumentGateway}.cs`, `tests/Integration.Tests/SpDocumentGatewayIntegrationTests.cs`) + แก้ 6 (`Directory.Packages.props`, `Products.Infrastructure.csproj`, `ProductsModuleRegistration.cs`, `src/Hosts/Api/Program.cs`, `tests/Integration.Tests/Integration.Tests.csproj`, `tests/Architecture.Tests/RawConnectionTests.cs`) รวม 558 insertions / 1 deletion. `dotnet build pol-core.slnx -warnaserror` -> `64 projects, 0 errors, 0 warnings`; `source .env.integration && dotnet test tests/Integration.Tests/Integration.Tests.csproj --filter Category=Integration` -> `Passed! Failed: 0, Passed: 107, Skipped: 0, Total: 107` (เดิม 91 + ใหม่ 16 case จาก 8 test method: 6 `[Theory]` รันทั้ง Motor/NonMotor = 12 case + 2 `[Fact]`); `dotnet test tests/Hosts.Tests` -> `Passed! Failed: 0, Passed: 365` (17 host ยัง boot ได้กับ options/DI ใหม่ — ไม่มี ValidateOnStart ตาม REQ-5.7); `dotnet test tests/Architecture.Tests` -> `Passed! Failed: 0, Passed: 225`; `dotnet test pol-core.slnx --filter "Category!=Integration"` -> เขียวทั้ง 16 โปรเจกต์ ไม่มี Failed; `bash scripts/check-rename-identifiers.sh` -> `rename-identifier gate: OK`. REQ ที่พิสูจน์: REQ-5.1 `Microsoft.Data.SqlClient` + `CommandType.StoredProcedure` + typed parameter 18 ตัวตามชนิด/ขนาดที่ SP ประกาศ (`VarChar` 3/10/20/30, `NVarChar` 100/200, `Date` x4, `DateTime2` x2, `Int` x2 — `DBNull.Value` เมื่อ null); REQ-5.2 เลือก connection จาก `request.Target` (test เดียวกันรันทั้ง 2 ฝั่งด้วย connection คนละ catalog และได้ `InsuranceType` คงที่ต่อฝั่ง Motor/NonMotor); REQ-5.3 RS1 หนึ่งแถว -> `NextResultAsync()` -> RS2 อ่านทุกคอลัมน์ผ่าน `GetOrdinal` ตามชื่อ (ไม่มี index ดิบในไฟล์เลย) + test `Every_column_of_a_row_lands_in_its_own_field` assert ครบทั้ง 32 field ของแถวแกน (`950001` / `960001`) รวม `NetPremium + Stamp + TaxVat = TotalPremium` และ `CommissionAmount = ROUND(Net * Pct / 100, 2)`; REQ-5.4 RS1 ไม่มีแถว -> `UpstreamUnavailableException`; REQ-5.5 `SqlException.Number in 50001..50009` -> `SpDocumentSearchRejectedException` (test `@CountMode='X'` -> `SpErrorNumber == 50006` + `IsAssignableFrom<ArgumentException>` = ได้ 400 จาก bucket เดิม); REQ-5.6 `SqlException` อื่น -> `LogError` เต็ม (procedure/target/Number/State/Class + inner exception) แล้ว `UpstreamUnavailableException` (test ชี้ Motor connection ไปที่ catalog ของแอปที่ไม่มี SP); REQ-5.7 `SpDocumentOptions` section `SpDocument` ครบ 4 property (`BranchCode` = `"000"`, 2 connection string nullable, `CommandTimeoutSeconds` = 15) **ไม่มี `.ValidateOnStart()`** — Hosts.Tests 365 เขียวยืนยัน; REQ-5.8 `PostConfigure<SpDocumentOptions>` ใน `Program.cs` เติมเฉพาะค่าที่ว่าง/blank ด้วย `new SqlConnectionStringBuilder(appConnString) { InitialCatalog = "hippodb"|"mammothdb" }` (ไม่มี env var / compose variable ใหม่ ตาม REQ-3.4 — render-check ทั้ง 2 CI ไม่ถูกแตะ); REQ-5.9 `AddSingleton<ISpDocumentGateway, SpDocumentGateway>()` ใน `AddProductsModule()` (เรียกอยู่แล้วที่ `Program.cs`) + pin `Microsoft.Data.SqlClient` **6.1.1** exact ใน `Directory.Packages.props` = เลขเดียวกับที่ `dotnet list src/BuildingBlocks/BuildingBlocks.Infrastructure package --include-transitive` รายงานว่า EF SqlServer 10.0.8 resolve อยู่แล้ว (graph ไม่แตก); REQ-6.6 `@BranchCode` มาจาก options ไม่ใช่ request — 2 test คุม: `'999'` (ไม่ตรงแถวไหนเลย) ยังคืน TotalRows 28/27 ครบ = validate-only และเดินทางถึง SP จริง, กับ options ที่เป็นช่องว่างล้วน -> `SpDocumentSearchRejectedException` 50004 (ไม่มีทางเกิดจาก request ได้เลย จึงเป็นตัวพิสูจน์ต้นทางของค่านี้); REQ-10.2 integration test ของ adapter จริงต่อ sim DB ผ่าน `IntegrationDb.ForCatalog` (login `pol_app` = พิสูจน์ GRANT ไปด้วย) ครอบ mapping ปกติ 2 ฝั่ง + FAST (`TotalRows`/`TotalPages` เป็น null ไม่ใช่ 0) + PAID row มี `PaidDate` + error mapping 3 แบบ; M8 cancellation: token ที่ถูก cancel มาก่อน -> `OperationCanceledException` (ไม่ใช่ 503) และใน `catch (SqlException)` เรียก `cancellationToken.ThrowIfCancellationRequested()` ก่อน map เสมอ. พิสูจน์ว่า assertion กัดจริงด้วย mutation ชั่วคราว 3 จุดพร้อมกัน: (1) สลับ ordinal `CommissionPercent`/`CommissionAmount` (2) ย่อช่วง error เป็น `50001..50005` (3) hardcode `"000"` แทน `_options.BranchCode` -> `Failed: 6, Passed: 10` แดงตรงตัวที่ตั้งใจทั้ง 6 (`Every_column_of_a_row_lands_in_its_own_field` x2, `A_procedure_error_becomes_a_rejection_carrying_its_number` x2, `A_blank_branch_code_in_options_is_rejected_by_the_procedure` x2) แล้ว revert + รันซ้ำเขียว 16/16. Viewports 375/768/1440: n/a (ไม่มี UI — adapter + options + DI ล้วน). Deviations: (1) **`tests/Architecture.Tests/RawConnectionTests.cs` ต้องยกเว้น `SpDocumentGateway` หนึ่งชื่อ** — guard เดิมห้าม production infrastructure อ้าง `Microsoft.Data.SqlClient.SqlConnection` เลย (เหตุผล: raw connection ข้าม query filter + `IWriteAuthorizer` ของ app database) แต่ REQ-5.1 สั่ง ADO.NET ตรงโดยเจตนา; gateway ต่อ `hippodb`/`mammothdb` ซึ่งไม่มีแถวผูก merchant ไม่อยู่ใน DbContext ใด และ login มีแค่ EXECUTE — ไม่มี floor ให้ข้าม จึงยกเว้น **ด้วยชื่อ type เดียว** (`.That().DoNotHaveName("SpDocumentGateway")`) ไม่ใช่ถอดทั้ง assembly ออกจากรายการ (ยืนยันว่าช่องยกเว้นแคบจริงด้วยการเปลี่ยนชื่อเป็น `SpDocumentGatewayXX` ชั่วคราว -> guard แดงทันทีที่ offender เดิม แล้ว revert); (2) `tests/Integration.Tests/Integration.Tests.csproj` เพิ่ม `ProjectReference` ไป `Products.Infrastructure` เพื่อขับ production type ตัวจริง (ไม่ใช่สำเนา) — task 6 ต้องให้ insulation guard REQ-10.5 สแกน production assembly เท่านั้น มิฉะนั้นทั้งไฟล์นี้และ `ProblemDetailsExceptionHandlerTests` จะทำให้ guard แดง; (3) แถวแกนถูกดึงด้วย `PolicyNo` exact ไม่ใช่ตำแหน่งในหน้า เพราะ `ORDER BY DocumentNo` ตัดสินด้วยอักษรไทย (`บต` < `อค` ทำให้ `960001` ไม่ได้อยู่หน้า 1) และไม่ assert ค่าวันที่แบบสัมบูรณ์ (seed สัมพัทธ์กับ `GETDATE()` ของวันที่ bootstrap รัน) — รายละเอียดที่ task 6 ต้องใช้อยู่ใน `HANDOFF.md` section Task 5

- [x] 6. Cutover read path + upsert — `ProductFilterDto` (+`InsuranceType?`/`CountMode` + Parse), `ListProductsHandler` เขียนใหม่ (routing 5 เคส -> gateway -> mapper -> upsert -> envelope), `ProductPage`, `IProductRepository` (ลบ `ListAsync`, เพิ่ม `UpsertByDocumentNoAsync`) + impl ใน `Persistence.MerchantRuntime` (retry race 2601/2627 + reset tracker), endpoint `Program.cs` (`Produces<ProductPage>` + 503 + description) + `SfsOpenApi.AddProductQueryParameters`, ชะตากรรม test เดิม (M10: `git rm tests/Architecture.Tests/ProductRepositoryListTests.cs`, rewrite 2 ไฟล์ Hosts.Tests ด้วย fake gateway), insulation guard `tests/Hosts.Tests/SpInsulationTests.cs` (fail-closed), repository integration tests (upsert new/refresh/no-downgrade/retry), handler + filter unit tests
     Satisfies: REQ-6.1, REQ-6.2, REQ-6.3, REQ-6.4, REQ-6.5, REQ-6.7, REQ-7.1, REQ-7.2, REQ-7.8, REQ-7.9, REQ-8 (ทั้งหมด), REQ-9 (ทั้งหมด), REQ-4.7, REQ-4.8, REQ-10.3, REQ-10.4, REQ-10.5. Depends on: 3, 4, 5. Verify: test suite เต็ม (unit + integration + Hosts + Architecture) เขียว.
     Evidence: commit `9b2a3dd` — ไฟล์ใหม่ 4 (`tests/Hosts.Tests/{FakeSpDocumentGateway,SpInsulationTests}.cs`, `tests/Integration.Tests/ProductUpsertIntegrationTests.cs`, `tests/Products.Tests/ListProductsHandlerTests.cs`), ลบ 1 ด้วย `git rm` (`tests/Architecture.Tests/ProductRepositoryListTests.cs`), แก้ 15 รวม 974 insertions / 479 deletions. ผล verify ทุกชุด: `dotnet build pol-core.slnx -warnaserror` -> `64 projects, 0 errors, 0 warnings`; `dotnet test tests/Products.Tests` -> `Passed! Failed: 0, Passed: 135` (เดิม 102 — ไม่มี test เดิมหาย); `source .env.integration && dotnet test tests/Integration.Tests --filter Category=Integration` -> `Passed! Failed: 0, Passed: 112, Skipped: 0, Total: 112` (เดิม 107 + ใหม่ 5); `dotnet test tests/Hosts.Tests` -> `Passed! Failed: 0, Passed: 369` (เดิม 365: -1 round-trip เดิม +2 ใหม่ +3 insulation; 17 host ยัง boot ได้); `dotnet test tests/Architecture.Tests` -> `Passed! Failed: 0, Passed: 200` (เดิม 225 ลบ 25 case ของไฟล์ที่ถูกลบ); `dotnet test pol-core.slnx --filter "Category!=Integration"` -> เขียวทั้ง 16 โปรเจกต์ ไม่มี Failed; `bash scripts/check-rename-identifiers.sh` -> `rename-identifier gate: OK`. REQ ที่พิสูจน์: REQ-6.1-6.4 routing table ครบ 5 เคสใน `ListProductsHandlerTests` (`[Theory]` 6 case ตรวจ Target + `@ProductGroup` ที่ส่งจริง: CMI/VMI/FIRE/MISC ทั้งแบบมีและไม่มี insuranceType, absent+Motor/NonMotor -> `'ALL'`; ขัดแย้ง 4 คู่ -> `ArgumentException`; ว่างทั้งคู่ -> `ArgumentException`); REQ-6.5 `countMode` absent -> EXACT, `EXACT`/`FAST` ผ่าน, `fast`/`APPROX`/`""` -> 400 อ้าง 50006 (`ProductFilterDtoTests`) + ค่าเดินทางถึง request จริง; REQ-6.7 พฤติกรรมเดิมของ `Parse` ครบ (test เดิม 20 case ยังเขียวไม่ถูกแตะ); REQ-7.1 handler เรียก `ISpDocumentGateway.SearchAsync` ไม่แตะ `shop.Products` ในการค้น (repository ไม่มี `ListAsync` ให้เรียกแล้ว); REQ-7.2 `UpsertByDocumentNoAsync` สร้าง/refresh ตาม DocumentNo บน SQL Server จริง (`An_unseen_document_is_created`, `A_document_already_here_is_refreshed_in_place` — Id เดิมคงอยู่, 1 แถวเท่านั้น) + wire PAID เข้าถึง input จริง (`The_upsert_receives_the_mapped_rows_with_their_wire_payment_state`); REQ-7.8 race จำลองด้วย `SaveChangesInterceptor` ที่ INSERT แถวชนจาก context อื่นในช่วงระหว่าง read กับ save -> repository reset tracker + reload + replay สำเร็จ ได้ค่าของเราไม่ใช่ของคู่แข่ง และแถวมีเดียว (`A_document_inserted_by_a_concurrent_writer_is_reloaded_and_the_page_still_answers` + assert ว่า interceptor ยิงจริง); REQ-7.9 ลำดับตาม SP (`Items_keep_the_order_the_procedure_returned` ส่ง z/a/m -> ได้ z/a/m) และทุก item มี Guid ของแถว local (round-trip test เอา Id ไปอ่านต่อผ่าน `GetProductByIdHandler` ได้); REQ-7.7 แถวเสีย 3 แบบ (TotalPremium null, SourceSystem ไม่รู้จัก, SaleCode ช่องว่าง) ถูกข้าม + log warning ขณะที่แถวดี 2 แถวยังตอบ และ `TotalRows` ยังเป็นของ SP (3 ทั้งที่หน้ามี 2); REQ-8.1/8.2 envelope คัดลอกตรง ๆ ทั้ง 8 field รวม FAST -> `TotalRows`/`TotalPages` เป็น null (ไม่ใช่ 0) และหน้าเกินท้ายชุด -> items ว่าง + `HasPreviousPage` จริง; REQ-8.3 endpoint ยังใช้ `SfsQueryParser.ParsePaging` เหมือนเดิม (ไม่ถูกแตะ) และ `SfsOpenApiTests` เดิมยังเขียว (พารามิเตอร์ยังเป็น page/limit/productFilters เท่านั้น); REQ-8.4 `.Produces<ProductPage>` + `.ProducesProblem(503)` + description ใหม่ + `SfsOpenApi.AddProductQueryParameters` อธิบาย insuranceType/countMode; REQ-9.1 `ListAsync` + window constants + `IClock` + `SfsLike` หายจาก `ProductRepository` ทั้งหมด (ctor เหลือพารามิเตอร์เดียว); REQ-9.2 upsert + save + retry อยู่ใน Persistence; REQ-9.3 `Add`/`GetAsync`/`GetProductByIdQuery`/`DocumentPaidOnOrderPaidConsumer`/cart/checkout ไม่ถูกแตะ (`InsuranceCheckoutEndToEndTests` เส้น cart -> checkout -> paid order เดิมยังเขียวทั้งเส้น); REQ-9.4 ไม่มี migration ใหม่; REQ-4.7/4.8/10.5 `SpInsulationTests` โหลด production assembly 46 ตัวจาก output dir แบบ fail-closed (assert ว่าเจอไฟล์ครบก่อน) แล้วสแกน IL ด้วย NetArchTest ว่า 44 ตัวนอก `Products.Application`/`Products.Infrastructure` ไม่อ้าง namespace `Products.Application.Ports` + `[Theory]` ยืนยัน signature ของ `ProductPage`/`ProductListItem` ไม่มี type จาก namespace นั้นและไม่มีชื่อขึ้นต้น `SpDocument`; REQ-10.3 unit tests ครบทั้ง handler/filter/domain; REQ-10.4 Hosts.Tests + Architecture.Tests เขียวโดยไม่แก้ boundary rule ใด. พิสูจน์ว่า assertion กัดจริงด้วย mutation ชั่วคราว 4 จุด แล้ว revert ทุกจุด: (1) `IsUniqueViolation` เปลี่ยนเลขเป็น `{ Number: 9999 }` -> Integration `Failed: 1, Passed: 4` ตกที่ race test พร้อม SqlException duplicate key จริง; (2) `ResolveTarget` เคส "ว่างทั้งคู่" เปลี่ยนจาก throw เป็นเดา `(Motor, "ALL")` -> Products.Tests `Failed: 1, Passed: 134`; (3) ยัด `typeof(ISpDocumentGateway)` เข้า `ProductRepository` -> `SpInsulationTests Failed: 1` พร้อมข้อความชี้ offender `Persistence.MerchantRuntime.Products.ProductRepository`; (4) ใส่ชื่อ assembly ปลอม `Products.Retired` ในรายการ -> `Failed: 1` ด้วยข้อความ fail-closed ("would scan nothing") พิสูจน์ว่า guard ไม่เงียบเมื่อ list เพี้ยน. Viewports 375/768/1440: n/a (ไม่มี UI — handler/repository/endpoint metadata + tests). Deviations: (1) **routing validation อยู่ใน handler ไม่ใช่ `ProductFilterDto.Parse`** — design sequence diagram เขียน "resolve Target" ไว้ที่ handler และ handler ต้อง resolve อยู่แล้วเพื่อเลือก SP จึงเช็คขัดแย้ง/ว่างที่จุดเดียวไม่ซ้ำ; ผลคือเคส "insuranceType ขัดแย้ง" ที่ตาราง Testing Strategy วางไว้ใต้ Filter unit ย้ายไปอยู่ `ListProductsHandlerTests` ส่วน `ProductFilterDtoTests` คุมการ parse ของ insuranceType + countMode ครบตามเดิม; (2) **เพิ่ม PackageReference `Microsoft.Extensions.Logging.Abstractions` 10.0.8** ใน `Products.Application` + `PackageVersion` ใน `Directory.Packages.props` — REQ-7.6/7.7 สั่งให้ handler log แต่ไม่มี Application project ไหนเข้าถึง `ILogger` ได้เลย (`Orders.Application/OrderPaidConsumer.cs` เขียนคอมเมนต์ค้างไว้ว่า "inject an ILogger here once that package is wired"); เลข 10.0.8 = เลขเดียวกับที่ EF resolve transitive อยู่แล้ว (`dotnet list package --include-transitive`) จึงไม่แตก graph; (3) **`tests/Integration.Tests` เพิ่ม ProjectReference ไป `Persistence.MerchantRuntime` + รับ `InternalsVisibleTo`** — design วาง repository integration tests ไว้ที่โปรเจกต์นี้ แต่ convention เดิมของมันคือ raw connection ล้วน (คอมเมนต์ใน `OrderSummaryReaderIntegrationTests` ระบุไว้); retry path เป็นพฤติกรรมของ EF ChangeTracker จึงต้องขับ `ProductRepository`/`MerchantRuntimeDbContext` ตัวจริง — ไฟล์อื่นในโปรเจกต์ยังเป็น raw connection เหมือนเดิม และคอมเมนต์ที่ทั้ง 2 csproj ระบุขอบเขตข้อยกเว้นไว้; (4) **`ProductInsuranceFieldsRoundTripTests` เปลี่ยนรูปเป็น upstream row -> upsert -> list -> get-by-id** (เดิม create -> list -> get-by-id) เพราะ list ไม่อ่าน `shop.Products` แล้ว รูปเดิมจึงเป็นไปไม่ได้ + เพิ่ม test ที่ค้นซ้ำแล้วได้ Guid เดิม; **`InsuranceCheckoutEndToEndTests` เปลี่ยน assert "เอกสาร PAID หลุดจากลิสต์ UNPAID" เป็น "upstream บอก UNPAID แล้ว local PAID ต้องไม่ถูก downgrade" (REQ-7.4)** — เงื่อนไขกรอง UNPAID ย้ายไปอยู่ใน SP แล้ว ของเดิมจึงทดสอบสิ่งที่โค้ดนี้ไม่ได้ทำอีกต่อไป ส่วนเส้น cart/checkout/order คงเดิมทุกบรรทัด; (5) `CountModeValue` เป็น computed property ที่ `Parse` บังคับอ่าน (ล้อ `PaymentStatusFilter` เดิม) แทนการเขียน validation block แยก; (6) จำลอง race ด้วย EF `SaveChangesInterceptor` ที่ INSERT แถวชนจาก context อื่นตอน save ครั้งแรก (ยิงครั้งเดียว) — เป็นวิธีเดียวที่ทำให้ช่วงระหว่าง read กับ save ของ method เดียวถูกแทรกได้แบบ deterministic; (7) ลบ helper `FixedClock` ที่กลายเป็นของกำพร้าใน `WorkerWriteFloorTests`/`InsuranceCheckoutEndToEndTests` หลัง `ProductRepository` เลิกรับ `IClock`; (8) แก้คอมเมนต์ใน `PolicyReportRepository.cs` ที่อ้าง `ProductRepository.ListAsync` (dangling reference ที่งานนี้ทำให้เกิด) — รายละเอียดทั้งหมดอยู่ใน `HANDOFF.md` section Task 6

- [ ] 7. Quality gates + docs + E2E — รัน `dotnet build pol-core.slnx -warnaserror`, suite เต็มรวม Integration, `bash scripts/spec-trace.sh products-sp-gateway` (ต้องพิมพ์ `OK:`), `check-rename-identifiers.sh`, `check-migration-lineage.sh`; E2E checklist ตาม design (fresh compose, curl ทั้ง 2 ฝั่ง, EXACT/FAST, 400 countMode, add-to-cart จาก Id upsert, 503 ตอน sim DB ดับ); อัปเดต docs ที่อ้าง read path เดิม (`docs/reference/platform-modules.md` ส่วน Products ถ้ามี) + บันทึกผลลง spec
     Satisfies: REQ-11 (ทั้งหมด). Depends on: 1-6. Verify: ทุก gate ผ่าน + E2E checklist ติ๊กครบใน task notes.

## Suggested execution batches

Feature นี้ coupled หนัก (SQL contract <-> adapter <-> handler ใช้ contract ก้อนเดียวกัน) —
default = **all-in-one session**: `/spec-implement all` หรือ `scripts/pane-loop.sh products-sp-gateway all-in-one`
ไม่ติด `Batch:` แยก เพราะไม่มี task เล็ก same-type ที่แยกขาดจริง; ถ้าจะแยกเพื่อ accuracy
ให้ตัดที่รอยต่อ 1+2 (SQL ล้วน) | 3-6 (C#) | 7 (gate)
