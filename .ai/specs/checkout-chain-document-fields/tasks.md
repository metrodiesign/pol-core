# Implementation Tasks: checkout-chain-document-fields

> Status: approved-for-implementation 2026-07-30
> Each task is a cohesive, independently verifiable slice. อ่าน `requirements.md` + `design.md` + `HANDOFF.md` ก่อนเริ่มทุกครั้ง
> Branch: `feat/products-insurance-document` (PR #143). ห้าม push develop, ห้าม force push
> Traps ทั้งหมดอยู่ใน design.md หัวข้อ "Traps" — อ่านก่อนเริ่มทุก task

- [x] 1. **Checkouts + Contracts** — เปลี่ยน `CheckoutConfirmedItem` (`src/Contracts/CheckoutConfirmed.cs`), `CheckoutItemInput`, `Checkouts.Domain/Items/Item.cs` (property/ctor/invariant ใหม่ตาม design ลำดับ param กลาง), `Session.cs` ส่งผ่าน, `ConfirmCheckout.cs` mapper, dual config Checkouts 2 ไฟล์, แก้ `tests/Checkouts.Tests/ConfirmCheckoutTests.cs`
     **Done** = `dotnet build src/Modules/Checkouts/Checkouts.Infrastructure src/Contracts src/Persistence/Persistence.MerchantRuntime` อาจยังแดงที่โปรเจกต์อื่น (Orders/Hosts รอ task 2-3) แต่ Checkouts.Domain/Application/Infrastructure + Contracts ต้อง compile; `dotnet test tests/Checkouts.Tests` เขียว
     Satisfies: REQ-1 (1.1-1.4), REQ-2 (2.1), REQ-5 (5.3 ฝั่ง Checkouts)
     Evidence: `dotnet build src/Contracts` -> `ok dotnet build: 2 projects, 0 errors, 0 warnings`; `dotnet build src/Modules/Checkouts/Checkouts.Infrastructure` -> `ok dotnet build: 7 projects, 0 errors, 0 warnings`; `dotnet build src/Persistence/Persistence.MerchantRuntime` -> error เฉพาะ `Orders.Application/CheckoutConfirmedConsumer.cs(40,*)` (SumInsured/CoverageDurationDays/Insurer) = นอกขอบเขต task 1 รอ task 2 — ไม่มี error ในไฟล์ Persistence/Checkouts เอง (`dotnet build ... | grep error | grep -v Orders` ว่าง); `dotnet test tests/Checkouts.Tests` -> `Passed! - Failed: 0, Passed: 13, Skipped: 0, Total: 13`. Viewports 375/768/1440: n/a (ไม่มี UI). Deviations: ไม่มี

- [x] 2. **Orders** — `Orders.Domain/Items/{Item,OrderItemInput}.cs`, `Order.cs` ส่งผ่าน, `CheckoutConfirmedConsumer.cs` map 1:1, read models `GetOrderDetail.cs`/`GetOrders.cs` field ชุดใหม่ (คง reveal-audit/masking เดิม), dual config Orders 2 ไฟล์, แก้ tests `Orders.Tests/{OrderItemsTests,GetOrderDetailTests,GetOrdersTests,CheckoutConfirmedConsumerTests,Fakes}.cs`
     **Done** = `dotnet test tests/Orders.Tests` เขียว; Orders.* ทุกโปรเจกต์ compile
     Satisfies: REQ-1 (1.2-1.4), REQ-2 (2.2), REQ-4 (4.1, 4.2), REQ-5 (5.3 ฝั่ง Orders)
     Evidence: `dotnet test tests/Orders.Tests` -> `Passed! - Failed: 0, Passed: 75, Skipped: 0, Total: 75`; build ทีละโปรเจกต์ -> `Orders.Domain` `ok dotnet build: 2 projects, 0 errors, 0 warnings`, `Orders.Application` `5 projects, 0 errors, 0 warnings`, `Orders.Infrastructure` `7 projects, 0 errors, 0 warnings`, `Persistence.MerchantRuntime` `17 projects, 0 errors, 0 warnings` (แดงที่ Orders.Application หายแล้ว); `grep -rn "SumInsured\|CoverageDurationDays\|InsurerName\|Insurer\b" src/Modules/Orders src/Modules/Checkouts src/Contracts | grep -v ItemPolicy` -> ว่าง. Viewports 375/768/1440: n/a (ไม่มี UI). Deviations: เพิ่ม test ใหม่ 5 เคส (blank document field x4 + start>end + trim + mapping 1:1 ของ consumer) นอกเหนือจากการแก้ signature ล้วน — invariant ใหม่ต้องมี check

- [x] 3. **Host wiring + ถอด bridge** — `Program.cs` POST /checkouts snapshot ชุดใหม่ + cart endpoint ใช้ `product.TotalPremium`; ลบ bridge 4 ตัว (`Price`/`SumInsured`/`CoverageDurationDays`/`Insurer`) จาก `Products.Application/ProductView.cs`; แก้ tests `Hosts.Tests/{InsuranceCheckoutEndToEndTests,WorkerWriteFloorTests}.cs`, `Architecture.Tests/{OrderItemsTests,PaymentPricingQueryTests}.cs`
     **Done** = `dotnet build` ทั้ง solution 0 error; `dotnet test tests/Hosts.Tests tests/Architecture.Tests tests/Products.Tests` เขียว; `grep -rn "ponytail: bridge" src/` ว่าง; grep REQ-6.3 ว่าง
     Satisfies: REQ-1 (1.1), REQ-3 (3.1-3.3), REQ-6 (6.3)
     Evidence: `dotnet build` (repo root, ทั้ง solution) -> `ok dotnet build: 64 projects, 0 errors, 0 warnings`; `dotnet test tests/Architecture.Tests` -> `Passed! - Failed: 0, Passed: 229, Skipped: 0, Total: 229`; `dotnet test tests/Products.Tests` -> `Passed! - Failed: 0, Passed: 31, Skipped: 0, Total: 31`; `dotnet test tests/Hosts.Tests` -> `Failed! - Failed: 1, Passed: 352, Skipped: 0, Total: 353` (fail เดียว = `ModelConsistencyTests.Model_has_no_pending_changes_against_the_migration_snapshot` — EF model drift จาก config ที่ task 1/2 แก้ไว้ ยังไม่มี migration; diff ของ task 3 ไม่แตะ EF config/snapshot เลย จะเขียวเมื่อ task 4 gen migration); `grep -rn "ponytail: bridge" src/` -> ว่าง; `grep -rn -E 'SumInsured|CoverageDurationDays|InsurerName' src/Modules/Checkouts src/Modules/Orders src/Contracts | grep -v ItemPolicy` -> ว่าง. Viewports 375/768/1440: n/a (ไม่มี UI). Deviations: (1) `dotnet test tests/Hosts.Tests` ยังแดง 1 เคส (model-vs-snapshot drift) — ขึ้นกับ task 4 ไม่ใช่ regression; (2) ต้องแก้ call site `OrderItemInput` เพิ่มอีก 3 ไฟล์นอกเหนือรายการใน task (`Hosts.Tests/{ListPolicyReportEndToEndTests,UpsertItemPolicyEndToEndTests,UpsertItemPolicyAdminEndToEndTests}.cs`) เพราะ compiler พาไปเจอ — แก้เฉพาะ ctor args ไม่แตะ assertion ของ policy

- [x] 4. **Migration + seed + integration** — `dotnet ef migrations add CheckoutChainDocumentFields` (alter `shop.CheckoutSessionItems` + `shop.OrderItems` ตาม REQ-5.1, ไม่ re-GRANT); อัปเดต `docker/bootstrap/seed-demo.sql` INSERT shop.OrderItems; แก้ raw-SQL ใน `Integration.Tests/{OrderSummaryReaderIntegrationTests,OrderItemPolicyGrantsTests}.cs`; apply จริงบน :11433 + รัน seed-demo ทั้งไฟล์ + Integration.Tests
     **Done** = `dotnet ef database update` ผ่าน; seed-demo รันผ่าน (`shop.OrderItems = 4`); `source .env.integration` + `dotnet test tests/Integration.Tests` เขียว
     Satisfies: REQ-5 (5.1, 5.2)
     Evidence: `dotnet ef migrations add CheckoutChainDocumentFields --context PolDbContext --project src/BuildingBlocks/BuildingBlocks.Infrastructure --startup-project src/Hosts/Api` -> `Build succeeded. / An operation was scaffolded that may result in the loss of data. / Done.` gen `20260730081227_CheckoutChainDocumentFields{,.Designer}.cs` + regen `PolDbContextModelSnapshot.cs` (timestamp ใหม่กว่า `20260730072057`); scaffold ตรวจแล้วแตะ 2 ตารางเท่านั้น — `shop.OrderItems` + `shop.CheckoutSessionItems` drop 4 column (`CoverageDurationDays`/`InsurerName`/`SumInsuredAmount`/`SumInsuredCurrency`) add 6 column (`DocumentNo` nvarchar(150) NOT NULL defaultValue "", `ProductGroup` varchar(10) NOT NULL, `DocumentType` varchar(20) NOT NULL, `PolicyNumber` varchar(150) NULL, `StartDate`/`EndDate` datetime2(0) NULL) ไม่มี DropTable จึงไม่มี re-GRANT; `dotnet ef database update ...` (:11433) -> `Applying migration '20260730081227_CheckoutChainDocumentFields'. / Done.`; `sqlcmd -S localhost,11433 -U sa -C -d VCentralPay -i docker/bootstrap/seed-demo.sql` -> `shop.OrderItems = 4`, `shop.Products = 100`, `shop.OrderItemPolicies = 3`, ปิดท้าย `seed-demo: OK.`; `source .env.integration && dotnet test tests/Integration.Tests` -> `Passed! - Failed: 0, Passed: 47, Skipped: 0, Total: 47`; `dotnet test tests/Hosts.Tests --filter "FullyQualifiedName~ModelConsistencyTests"` -> `Passed! - Failed: 0, Passed: 1, Skipped: 0, Total: 1` (เคสที่ task 3 ทิ้งไว้แดง เขียวแล้วจาก snapshot regen). Viewports 375/768/1440: n/a (ไม่มี UI). Deviations: (1) seed-demo self-check ยัง raise `Msg 51000 ... merch.Merchants` — สภาพ local เดิมที่ lead บันทึกไว้ (merchant `vprivilege` คนละ Id ค้างอยู่ก่อนงานนี้) ไม่ใช่ผลจาก task 4 และไม่กระทบ `shop.OrderItems = 4`; (2) INSERT ใน seed + raw-SQL tests 2 ไฟล์ ใส่ `PolicyNumber`/`StartDate`/`EndDate` ตรง ๆ (บางแถวเป็น NULL) เพื่อให้ column nullable ถูกใช้จริงอย่างน้อย 1 แถว

- [x] 5. **Full gate + PR** — `dotnet build pol-core.slnx -warnaserror`; `dotnet test` ทุก non-integration suite + Integration; `bash scripts/check-rename-identifiers.sh`; `bash scripts/spec-trace.sh checkout-chain-document-fields`; ยืนยัน working tree สะอาด + `git show --stat` ครบ; push branch; อัปเดต PR #143 body (เพิ่มหัวข้อ chain rework + ถอด bridge, ลบหมายเหตุ bridge เดิม)
     **Done** = ทุก gate เขียว + PR #143 อัปเดตแล้ว
     Satisfies: REQ-6 (6.1, 6.2)
     Evidence: `dotnet build pol-core.slnx -warnaserror` -> `ok dotnet build: 64 projects, 0 errors, 0 warnings`; `dotnet test tests/<X>.Tests --no-build -v q` ทุก non-integration suite เขียวหมด — Products 31/31, Checkouts 13/13, Orders 75/75, Carts 15/15, Hosts 353/353, Architecture 229/229, Payments 162/162, Admins 95/95, Merchants 120/120, Iam 62/62, SharedKernel 46/46, BuildingBlocks 43/43, Divisions 6/6, Levels 6/6, Offices 6/6, Positions 6/6 (รวม 1268 passed / 0 failed / 0 skipped); `source .env.integration && dotnet test tests/Integration.Tests --no-build -v q` -> `Passed! - Failed: 0, Passed: 47, Skipped: 0, Total: 47`; `bash scripts/check-rename-identifiers.sh` -> ครั้งแรก `rename-identifier gate: FAILED — retired identifiers in live code:` 8 บรรทัดใน `tests/Checkouts.Tests/ConfirmCheckoutTests.cs` (helper ชื่อ `Line(` ที่ task 1 เพิ่ม ชน retired token `Line` จาก OrderLine->OrderItem rename) -> rename helper เป็น `ItemInput(` แล้วรันซ้ำ -> `rename-identifier gate: OK — no retired identifier appears as a live-code token in src/ or tests/.` (exit 0) และ `dotnet test tests/Checkouts.Tests` ยัง 13/13; `bash scripts/spec-trace.sh checkout-chain-document-fields` -> exit 0 แต่เป็น **skip ไม่ใช่ pass**: `requirements.md ของ 'checkout-chain-document-fields' ไม่ใช่รูปแบบ REQ-based (ไม่มีหัวข้อ '## REQ-N:') — ข้ามการตรวจ traceability` (ไฟล์ใช้ `### REQ-N —`) — รายงาน lead แล้ว ไม่แก้เอง เพราะการทำให้ gate ตรวจจริงต้องแก้ heading + reword criteria 3.3/5.3/6.1-6.3 ที่ไม่มี `THE SYSTEM SHALL`/WHEN ให้ผ่าน EARS lint = แก้เนื้อหา spec ที่อนุมัติแล้ว; `grep -rn "ponytail: bridge" src/` -> ว่าง; `grep -rn "SumInsured\|CoverageDurationDays\|InsurerName" src/Modules/Checkouts src/Modules/Orders src/Contracts | grep -v ItemPolicy` -> ว่าง. Viewports 375/768/1440: n/a (ไม่มี UI). Deviations: (1) ต้อง rename helper `Line(` -> `ItemInput(` ใน `ConfirmCheckoutTests.cs` (8 จุด) เพื่อผ่าน rename gate — ไม่ได้อยู่ในรายการงานเดิมของ task 5 แต่เป็นหนี้ของ task 1 ที่ gate เพิ่งจับได้; (2) spec-trace เป็น skip ตามที่อธิบายข้างต้น (lead แก้ heading เป็น `## REQ-N:` + reword ให้ gate ตรวจจริงใน commit ตามหลัง `47c9783`)

- [x] 6. **Mark เอกสารเป็น PAID เมื่อ order paid (Codex F2)** — `Contracts/OrderPaid.cs` ใหม่; `Orders.Application/OrderPaidConsumer.cs` inject `IOutbox` enqueue `OrderPaid` หลัง MarkPaid true (ตรวจ `OrderRepository.GetAsync` include Items); Products consumer ใหม่ `DocumentPaidOnOrderPaidConsumer` mark แต่ละ product PAID; `OutboxDispatcher.EventTypes` add OrderPaid; `WorkerWriteAuthorizer` allow Product/Update; แก้ tests `Orders.Tests/OrderPaidConsumerTests.cs` (fake IOutbox + assert enqueue), `Hosts.Tests/{WorkerWriteAuthorizerTests,WorkerWriteFloorTests}.cs` (Product Update ผ่าน floor), `Hosts.Tests/InsuranceCheckoutEndToEndTests.cs` (assert product PAID หลังจ่าย); + Products.Tests unit ของ consumer ใหม่ถ้าเหมาะ
     **Done** = `dotnet build pol-core.slnx -warnaserror` 0/0; `dotnet test tests/Orders.Tests tests/Products.Tests tests/Hosts.Tests tests/Payments.Tests` เขียว + `bash scripts/spec-trace.sh checkout-chain-document-fields` OK; E2E ยืนยัน `Product.PaymentStatus == PAID` && `!IsActive` หลัง order paid [หมายเหตุ 2026-07-30: `IsActive` ถูกลบโดย spec `products-sp-53-alignment` — เกณฑ์ที่ยังใช้ได้คือ `PaymentStatus == PAID` เท่านั้น]; commit + push
     Satisfies: REQ-7 (7.1, 7.2, 7.3, 7.4)
     Evidence: `OrderRepository.GetAsync` มี `.Include(o => o.Items)` อยู่แล้ว (บรรทัด 20) — ProductIds ไม่ว่าง ไม่ต้องแก้ repository และไม่กระทบ caller อื่น; `dotnet build pol-core.slnx -warnaserror` -> `ok dotnet build: 64 projects, 0 errors, 0 warnings`; `dotnet test tests/Orders.Tests` -> `Passed! - Failed: 0, Passed: 76, Total: 76`; `dotnet test tests/Products.Tests` -> `Passed! - Failed: 0, Passed: 36, Total: 36`; `dotnet test tests/Payments.Tests` -> `Passed! - Failed: 0, Passed: 162, Total: 162`; `dotnet test tests/Hosts.Tests` -> `Passed! - Failed: 0, Passed: 358, Total: 358` (E2E `Paid_order_marks_the_product_PAID_and_inactive` ยืนยัน `PaymentStatus == PAID` && `!IsActive` หลังจ่าย ผ่าน consumer จริง; `OrderPaid_marks_the_product_PAID_through_the_real_Worker_write_floor` ผ่าน floor); `dotnet test tests/Architecture.Tests` -> `Passed! - Failed: 0, Passed: 229`; `source .env.integration && dotnet test tests/Integration.Tests` -> `Passed! - Failed: 0, Passed: 47, Total: 47`; `bash scripts/spec-trace.sh checkout-chain-document-fields` -> `OK: ... เกณฑ์ 21 ข้อ ถูกอ้างครบใน design.md และ tasks.md, EARS lint ผ่านทุกข้อ` (real pass ไม่ใช่ skip — lead แก้ heading เป็น `## REQ-N:` แล้ว). Viewports 375/768/1440: n/a (ไม่มี UI). Deviations: (1) พบ root cause เพิ่ม — `WorkerWriteAuthorizer` deny `Insert` บน `OutboxMessage` ทำให้ enqueue `OrderPaid` mid-drain โดน `WriteGuardException`; แก้โดย allow Insert OutboxMessage (chained enqueue ยัง merchant-scoped ผ่าน EfOutbox) ซึ่ง**ปิด latent bug เดิม**ด้วย: `CheckoutConfirmedConsumer` recipient path (`CustomerOrderNotification`) ก็ enqueue mid-drain เหมือนกันแต่ไม่เคยมี test คลุม จึงไม่เคยถูกจับ — อัปเดต test `Worker_denies_insert_and_delete_on_the_outbox` -> allow-insert/deny-delete + แยก MerchantUserOutbox (ยัง deny insert); Delete ยัง deny; (2) เพิ่ม E2E test ใหม่ + WorkerWriteFloor test + Products.Tests unit นอกเหนือการแก้ signature ล้วน เพราะ logic ใหม่ (loop+branch, money/security path) ต้องมี check
