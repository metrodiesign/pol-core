# Implementation Tasks: Purchase Flow Completion (ปิด flow ซื้อประกันภัย End-to-End)

> Status: approved 2026-08-02

> Each task is a cohesive, independently verifiable slice. Implement a whole task
> in one pass (it may touch many files). Decompose into sub-steps yourself at
> execution time — do NOT pre-split tasks here.

- [x] 1. Fix cart add-item 409 — `ValueGeneratedNever()` บน Cart Item.Id ทั้ง 2 mirror configuration + empty migration sync snapshot + regression test 2 DI scope (`CreateCartHandler` → `AddItemToCartHandler`) + แก้ `InsuranceCheckoutEndToEndTests` ให้เดิน handler จริง (ลบ `new Cart(...)` workaround + comment ที่โทษ rls-to-query-filter ผิด) — done = add-item ผ่านทั้ง SQLite E2E และ endpoint จริง
     Satisfies: REQ-1 (all criteria). Verify: `dotnet test tests/Carts.Tests tests/Hosts.Tests --filter InsuranceCheckout` + fresh-DB `ef database update` ไม่มี PendingModelChangesWarning.
     Evidence:
       - test: `dotnet test tests/Carts.Tests` -> 15 passed / 0 failed (พฤติกรรม cart เดิมคงไว้ — REQ-1.2 merge ครอบที่ `CartTests.AddItem_merges_same_product_and_subtotal_sums`; ชุดนี้เป็น domain/handler ล้วน ไม่มี assertion เรื่อง `PaymentStatus` จึงไม่ครอบ REQ-1.3)
       - test: `dotnet test tests/Hosts.Tests --filter InsuranceCheckout` -> 6 passed / 0 failed รวม regression 2-scope ใหม่ `Adding_an_item_to_a_cart_created_by_an_earlier_request_inserts_the_line` (REQ-1.1 + REQ-1.2 ข้าม scope) — `CreateCartHandler` ใน context แรก, `AddItemToCartHandler` ใน context ที่สอง, merge ซ้ำใน context ที่สาม
       - test (REQ-1.3): `dotnet test tests/Hosts.Tests --filter MerchantLifecycle` -> 1 passed / 0 failed — `MerchantLifecycleEndpointTests.Adding_a_product_that_is_not_UNPAID_is_rejected_with_400` ยิง `POST /api/v1/carts/{id}/items` ผ่าน route + policy `merchant-user` + CSRF filter + lambda จริง (WebApplicationFactory, fake scheme + fake `IProductRepository` ไม่มี DB) ด้วยเอกสาร `PaymentStatus.PAID` -> 400
       - regression proof (REQ-1.3): ตัดเงื่อนไข `PaymentStatus` ออกจาก guard ชั่วคราว (`Program.cs:679` -> `if (product is null)`) -> test แดง `Expected: BadRequest / Actual: Conflict` แล้ว restore -> เขียว (guard หายเมื่อไหร่ test จับได้)
       - test: `dotnet test tests/Hosts.Tests` -> 387 passed / 0 failed (suite เต็ม รวม `ModelConsistencyTests` = ไม่มี pending model change)
       - test: `dotnet test tests/Architecture.Tests --filter CartItemAggregateBoundary|MoneyColumnMapping` -> 6 passed / 0 failed
       - regression proof (REQ-1.4): revert `ValueGeneratedNever()` เป็น `ValueGeneratedOnAdd()` ชั่วคราวบน mirror `Persistence.MerchantRuntime` -> `--filter InsuranceCheckout` = 6 failed / 0 passed ทุกข้อด้วย `ConcurrencyConflictException` ที่ `AddItemToCartHandler.cs:32` (inner: expected 1 row affected, actually 0) แล้ว restore กลับ -> 6 passed
       - migration: `20260802134226_CartItemClientMintedId` มี `Up`/`Down` ว่างจริง (snapshot-sync ล้วน — snapshot diff เหลือแค่ลบ `.ValueGeneratedOnAdd()` ออกจาก `Carts.Domain.Items.Item.Id`); fresh-DB replay จากศูนย์บน catalog เปล่า `VCentralPayT1Probe` (:11433) -> ทุก migration applied รวม `20260802134226_CartItemClientMintedId` ปิดท้าย `Done.` ไม่มี PendingModelChangesWarning; dev DB `VCentralPay` -> `No migrations were applied. The database is already up to date.` ไม่มี warning
       - viewports: n/a — logic-only
       - deviations: (1) verify command ตามตัวอักษรรันไม่ได้ — `dotnet test` รับได้ project เดียว (MSB1008) จึงแยกเป็น 2 คำสั่ง; (2) regression 2-scope วางไว้ใน `InsuranceCheckoutEndToEndTests` (ตาม verify command ของ task นี้) ไม่ใช่ `MerchantLifecycleEndpointTests` ที่ design.md ระบุ — `MerchantLifecycleEndpointTests` ถูกสร้างแล้วในรอบแก้ evidence และมีเฉพาะ test REQ-1.3 ตัวเดียว, T2 เติม lifecycle test ที่เหลือ (start ซ้ำ 409, abandon→restart, ...) ลงไฟล์เดิม; (3) probe DB `VCentralPayT1Probe` (ของ T1) และ `VCentralPayFreshT1` (ของ teammate ก่อนหน้า) ยังค้างบน :11433 — `destructive-guard` block `DROP DATABASE` โดยต้องให้ user ยืนยัน จึงไม่ลบเอง (ไม่ bypass hook), dev DB `VCentralPay` ไม่ถูกแตะ
- [x] 2. ปิดวงจร cart–checkout — freeze cart ตอน StartCheckout (endpoint orchestration + `MarkCartCheckedOutCommand`), `GetOpenForCartAsync` pre-check + filtered unique index `IX_CheckoutSessions_CartId_Open` (2 mirror, named overload), endpoint `POST /checkouts/{id}/abandon` (gate merchant-user+CSRF) + แก้ `Session.Abandon()` Abandoned→no-op + `Cart.Reopen()`/`ReopenCartCommand`, map cart mutation exception → 409 — done = start ซ้ำ 409 ทุกทาง, abandon→restart ได้
     Satisfies: REQ-2 (all criteria). Depends on: 1. Verify: `dotnet test tests/Carts.Tests tests/Checkouts.Tests tests/Hosts.Tests --filter MerchantLifecycle` + Integration.Tests เคส index บน :11433.
     Evidence:
       - test: `dotnet test tests/Carts.Tests` -> 20 passed / 0 failed (เดิม 15) — `Cart.Reopen()` CheckedOut→Open + Open→no-op (REQ-2.5/2.9), `MarkCartCheckedOutHandler`/`ReopenCartHandler` + owner check, และ `A_checked_out_cart_rejects_edits` ขยายเป็นครบ 4 mutation รวม `AddItem` (REQ-2.7 ชั้น domain)
       - test: `dotnet test tests/Checkouts.Tests` -> 21 passed / 0 failed (เดิม 11) — ไฟล์ใหม่ `CheckoutLifecycleTests`: start ซ้ำบน cart ที่มี session Started **และ** Confirmed -> `ConflictException` (REQ-2.3), abandoned ไม่บล็อก, cart อื่นไม่บล็อก, `AbandonCheckoutHandler` Started→Abandoned + คืน CartId (REQ-2.5), Abandoned→no-op (REQ-2.9), Confirmed→throw (REQ-2.6), unknown→`NotFoundException`
       - test (REQ-2.1/2.2/2.3/2.5/2.6/2.7/2.8/2.9 ระดับ HTTP): `dotnet test tests/Hosts.Tests --filter MerchantLifecycle` -> 13 passed / 0 failed — `MerchantLifecycleEndpointTests` ขยายจาก 1 เป็น 13 test ยิงผ่าน route + policy `merchant-user` + CSRF filter + minimal-API lambda + mediator pipeline จริง (`WebApplicationFactory`, fake scheme + fake `IActorContext`/`ICartRepository`/`ICheckoutRepository`/`IUnitOfWork` = ไม่มี DB): start -> cart CheckedOut, start บน cart frozen -> 409, start ขณะมี live session (cart ยัง Open) -> 409, mutation 4 เส้นบน cart frozen -> 409 (Theory), abandon -> cart Open แล้ว start ใหม่ได้ 200, abandon ซ้ำ -> 200, abandon session Confirmed -> 409, abandon session ไม่มีอยู่ -> 404, abandon ไม่มี CSRF -> 403 + session ไม่เปลี่ยนสถานะ
       - test: `dotnet test tests/Hosts.Tests` -> 399 passed / 0 failed (suite เต็ม เดิม 387 รวม `ModelConsistencyTests` = ไม่มี pending model change)
       - test: `dotnet test tests/Architecture.Tests` -> 204 passed / 0 failed (suite เต็ม) รวมไฟล์ใหม่ `OpenCheckoutIndexTests` 2 test ที่พิสูจน์ว่า **ทั้งสอง** model (`PolDbContext` ผ่าน `Checkouts.Infrastructure` = เจ้าของ migration, และ `MerchantRuntimeDbContext` ผ่าน `Persistence.MerchantRuntime`) ประกาศ index ชื่อ/คีย์/unique/filter ตรงกัน — เป็นด่านที่รันเสมอแม้ CI ข้าม integration job
       - test (REQ-2.4 บังคับใช้จริง): `source .env.integration && dotnet test tests/Integration.Tests --filter OpenCheckoutIndex` -> 3 passed / 0 failed บน pol-db :11433 — DDL identity (`is_unique`/`has_filter`/`([Status] IN ((0), (1)))`/คีย์เดียว `CartId`), session ที่สองบน cart เดิม (Started แล้ว Confirmed) -> `SqlException` 2627/2601 + ข้อความมีชื่อ index (ตัวเลขเดียวกับที่ `MerchantRuntimeUnitOfWork.IsUniqueViolation` แปลงเป็น `ConflictException` -> 409 = race แพ้ index ได้ 409 เดียวกับ pre-check), abandoned 2 ใบไม่บล็อก Started ใบใหม่
       - regression proof (REQ-2.1): ลบ `MarkCartCheckedOutCommand` ออกจาก endpoint `/checkouts` -> `Starting_a_checkout_freezes_the_cart` แดง (1 failed / 12 passed) แล้ว restore -> เขียว
       - regression proof (REQ-2.2): พลิกเงื่อนไข cart-Open ที่ endpoint เป็นค่าที่ไม่มีวันตรง -> `Starting_a_checkout_on_a_frozen_cart_is_409` แดง แล้ว restore -> เขียว
       - regression proof (REQ-2.3): กลับด้าน pre-check `GetOpenForCartAsync` (`is not null` -> `is null`) -> Checkouts.Tests 5 failed + Hosts.Tests 3 failed (รวม `Starting_a_checkout_while_a_live_session_exists_is_409`) แล้ว restore -> เขียวทั้งคู่
       - regression proof (REQ-2.5/2.9): (ก) ถอด branch `Abandoned -> return` ออกจาก `Session.Abandon()` -> `Abandoning_an_already_abandoned_session_succeeds_without_changing_anything` + `Abandoning_the_same_checkout_twice_succeeds` แดง; (ข) ลบ `ReopenCartCommand` ออกจาก endpoint abandon -> `Abandoning_a_checkout_reopens_the_cart_and_lets_it_start_a_new_one` แดง — restore ทั้งสอง -> เขียว
       - test: `dotnet test` บน `tests/{Orders,Payments,Products,BuildingBlocks,SharedKernel}.Tests` -> 76 / 162 / 137 / 43 / 46 passed, 0 failed (ไม่มี suite ข้างเคียงพัง)
       - migration: `20260802142744_CheckoutOneOpenSessionPerCart` = `CreateIndex`/`DropIndex` ล้วน (ไม่มีคอลัมน์ใหม่ ไม่ต้อง GRANT เพิ่ม, `docker/bootstrap/assert-fresh-db.sql` ไม่มี count ที่เกี่ยวข้อง); เช็คก่อน apply ว่า `shop.CheckoutSessions` ไม่มี CartId ซ้ำใน Status IN (0,1); apply บน dev DB `VCentralPay` (:11433) สำเร็จ + รันซ้ำได้ `No migrations were applied. The database is already up to date.` ไม่มี PendingModelChangesWarning; replay ต่อบน catalog `VCentralPayT1Probe` (fresh-replay ของ T1) -> `Applying migration ... Done.` และ `sys.indexes` ยืนยัน `is_unique=1 has_filter=1`
       - gate: `scripts/check-migration-lineage.sh` OK, `scripts/check-rename-identifiers.sh` OK, `dotnet build -warnaserror` 0 errors / 0 warnings
       - viewports: n/a — logic-only
       - deviations: (1) verify command ตามตัวอักษรรันไม่ได้ — `dotnet test` รับได้ project เดียว (MSB1008, trap #1 ของ T1) จึงแยกเป็น 3 คำสั่ง; (2) **REQ-2.7 ไม่ต้องเขียน mapping ใหม่เลย** — `ProblemDetailsExceptionHandler` map `InvalidOperationException` -> 409 อยู่แล้ว และ guard 4 จุดใน `Cart` ครบตาม design จึงเหลือแค่ test (domain 4 mutation + endpoint Theory 4 เส้น) ตามที่ design ระบุ "ไม่แตะ domain"; (3) `AbandonCheckoutHandler` ใช้ `NotFoundException` (404) ตอนไม่พบ session ต่างจาก `ConfirmCheckoutHandler` ที่ยังใช้ `InvalidOperationException` (409) — เลือก 404 เพราะถูกต้องกว่าและไม่ leak, ไม่แตะ handler เดิม (T8 มีงานแก้ `GetSessionHandler` เป็น `NotFoundException` อยู่แล้ว ทิศทางเดียวกัน); (4) `Cart.Reopen()` เขียนเป็น `Status = CartStatus.Open` บรรทัดเดียว — enum มี 2 ค่า no-op เมื่อ Open จึงได้ฟรี ไม่ต้องมี branch; (5) endpoint test ใช้ fake persistence port แทน DB จริง — ชั้นที่พิสูจน์คือ orchestration/status-code ส่วน EF+index พิสูจน์ที่ Integration.Tests แยกกัน; (6) probe DB `VCentralPayT1Probe` / `VCentralPayFreshT1` ยังค้างบน :11433 เหมือนเดิม (`destructive-guard` block `DROP DATABASE`) — task นี้ใช้ซ้ำแทนสร้างใหม่ ไม่เพิ่มขยะ
- [x] 3. `PaymentConfirmationService` + session expiry — แตกเส้น fetch→verify→claim→mark→enqueue ออกจาก `HandlePspWebhookHandler` เป็น service เดียว (key แชร์ `charge:{id}:confirmed`, enqueue-on-transition, outcome `Conflicted` สำหรับ terminal+Paid → webhook ตอบ 200 + LogCritical, branch PSP `Failed` → `MarkFailed`), เพิ่ม `Session.OpenTtl`/`IsExpiredAt` + lazy expire verify-first ใน `CreateSessionHandler` (2-phase SaveChanges ใน tx เดียว) — done = webhook behavior เดิมไม่เปลี่ยน (test เดิมเขียวหมด) + expiry ครบทุก branch
     Satisfies: REQ-3 (all criteria). Verify: `dotnet test tests/Payments.Tests` + Integration.Tests เคส 2-phase expire+insert ผ่าน `IX_PaymentSessions_OrderId_Open`.
     Evidence:
       - test: `dotnet test tests/Payments.Tests` -> 189 passed / 0 failed (เดิม 162) — **test เดิมทั้ง 162 ตัวเขียวโดยไม่แก้ assertion ใด ๆ** (แก้เฉพาะการประกอบ harness เพราะ ctor ของ `HandlePspWebhookHandler`/`CreateSessionHandler` รับ `PaymentConfirmationService` เพิ่ม); ใหม่: `PaymentConfirmationServiceTests` 15 (paid / no-amount status-only / mismatch 2 เคส + Critical / scale / duplicate / enqueue-on-transition / PSP Failed → MarkFailed / no-chargeId expire โดยไม่แตะ vault / no-chargeId ยังไม่หมดอายุ = pending / stale + PSP pending → expire หลัง fetch / stale + PSP paid → **Paid ไม่ใช่ Expired** / ambiguous fetch โยนออกโดยไม่ตัดสิน / terminal+Paid → `Conflicted`+Critical (Theory Expired,Failed) / terminal ที่ PSP ไม่ยืนยัน), `PaymentSessionTests` +5 (`OpenTtl`=24h + `IsExpiredAt` boundary 0/23/24/48 ชม. + terminal ไม่หมดอายุตามนาฬิกา), `CreateSessionHandlerTests` +6 (lazy expire 2-phase, verify-first, paid → 409, ambiguous → โยนต่อ, expired ไม่ resume, TTL-1 นาที ยัง resume)
       - test (REQ-3.2 บน SQL Server จริง): `source .env.integration && dotnet test tests/Integration.Tests --filter PaymentSessionExpiry` -> 2 passed / 0 failed บน pol-db :11433 — ไฟล์ใหม่ `PaymentSessionExpiryIntegrationTests`: (ก) UPDATE(Expired) แล้วค่อย INSERT ใน transaction เดียว commit ผ่าน เหลือ open 1 แถวและเป็นแถวใหม่, (ข) สลับลำดับ INSERT ก่อน UPDATE -> `SqlException` 2627/2601 พร้อมชื่อ `IX_PaymentSessions_OrderId_Open` (เลขเดียวกับที่ `MerchantRuntimeUnitOfWork.IsUniqueViolation` แปลงเป็น 409) = ลำดับ UPDATE-ก่อน-INSERT เป็นข้อบังคับจริง ไม่ใช่ความชอบ
       - test: `dotnet test tests/Hosts.Tests` -> 399 passed / 0 failed (เท่าเดิม; host boot จริง + `ValidateOnBuild` = `PaymentConfirmationService` resolve ได้ครบทุก dependency), `--filter ModelConsistencyTests` -> 1 passed (ไม่มี pending model change — task นี้ไม่มีคอลัมน์ใหม่ ไม่มี migration)
       - test: `dotnet test tests/Architecture.Tests` -> 204 passed / 0 failed (รวม `TransactionInventoryTests` ที่เพิ่ม call site `CreateSessionHandler.cs` = 1 พร้อม classification single-context)
       - test: `dotnet test` บน `tests/{Orders,Checkouts,Carts,Products}.Tests` -> 76 / 21 / 20 / 137 passed, 0 failed (ไม่มี suite ข้างเคียงพัง)
       - regression proof (verify-first, REQ-3.1/3.2): แทรก `if (session.IsExpiredAt(now)) return ExpireIfStale(...)` ก่อน fetch (= ตัดสินด้วยนาฬิกาก่อนถาม PSP) -> 5 failed รวม `A_stale_session_the_customer_actually_paid_is_marked_paid_not_expired` + `An_expired_session_the_customer_paid_blocks_a_replacement_instead_of_being_expired` แล้ว restore -> เขียว
       - regression proof (enqueue-on-transition): ลบ `if (!transitions) return AlreadyPaid;` -> `Two_callers_holding_different_keys_still_publish_PaymentPaid_only_once` แดง (event ซ้ำ) แล้ว restore -> เขียว
       - regression proof (REQ-3.4/3.5 `Conflicted`): เปลี่ยน guard terminal เป็น `Status is Created` -> 2 test แดงทั้ง Expired และ Failed (`MarkPaid` โยนแทนที่จะคืน `Conflicted` = webhook จะ 500 วน) แล้ว restore -> เขียว
       - regression proof (2-phase order): สลับให้ `MintAsync` ทำก่อน `ConfirmAsync` ใน transaction -> 4 test ของ lazy expire แดง (`SaveCountWhenMinted` = 0 แทน 1) แล้ว restore -> เขียว
       - gate: `dotnet build -warnaserror` -> 64 projects, 0 errors, 0 warnings
       - viewports: n/a — logic-only
       - deviations: (1) verify command ตามตัวอักษรแยกรันทีละ project (MSB1008, trap #1 ของ T1); (2) **ambiguous fetch ไม่ถูกแปลงเป็นค่า outcome** — service ไม่ catch รอบ `FetchChargeAsync` เลย ปล่อย exception ของ adapter ทะลุ (ตรงกับ design "ไม่ตัดสินอะไร (ผู้เรียกตอบ 409/pending ตามบริบท)") เหตุผล: webhook ต้องคง 500 -> PSP redeliver ซึ่งเป็นทางที่ inquiry ล้มชั่วคราวได้ confirm ในที่สุด, และ `catch` กว้าง ๆ บนเส้น money = อ่าน "ไม่รู้" เป็น "ยังไม่จ่าย"; T4/T8 catch ที่ call site ของตัวเอง (`HttpRequestException`/`TaskCanceledException`/`JsonException`) แล้วตอบ 409/`pending`; (3) idempotency context เปลี่ยน `psp-webhook` -> `payment-confirmation` และ key `charge:{id}:{evt.Status}` -> `charge:{id}:confirmed` — key เดิมที่ spent ไว้บน prod กลายเป็น inert แต่ redelivery หลัง deploy ไม่ยิง event ซ้ำเพราะ enqueue-on-transition (design rule 3) เป็นด่านจริง (มี test คุม); (4) webhook เดินผ่านกฎเดียวกันทั้งชุดจึงได้พฤติกรรมใหม่เพิ่ม 2 อย่างที่ design สั่งไว้อยู่แล้ว: charge ที่ PSP ตอบ `Failed` -> `MarkFailed` (map `Processed`) และ session ที่ Paid อยู่แล้ว -> `Duplicate` แทนการ enqueue ซ้ำ — ไม่มี assertion เดิมข้อไหนเปลี่ยน; (5) webhook ค้นหา session **ก่อน** fetch (เดิม fetch ก่อน) เพราะ service รับ `Session` เป็น input เดียว — ผลต่างเกิดเฉพาะ webhook ของ charge ที่ยังไม่มีแถว session: เดิมตอบ 200 Ignored เมื่อ fetch ยังไม่ Paid, ตอนนี้โยน -> 500 -> redeliver ซึ่งเป็นคำตอบที่ถูกต้องกว่า (race: notification มาก่อน `SetPspCharge` commit); (6) `TransactionInventoryTests` เพิ่มแถวใหม่ในไฟล์ test แต่ **ไม่ได้แก้ transaction inventory ใน design.md ของ spec `rls-to-query-filter`** ที่ข้อความ error ชี้ไป — classification เขียนเป็น comment ชี้มาที่ design.md ของ spec นี้ (แถว "Expire + mint ใหม่") แทน; (7) integration test พิสูจน์ลำดับด้วย raw SQL ไม่ใช่ด้วยการเดิน EF handler จริง — Integration.Tests ไม่มี `InternalsVisibleTo` เข้า `Persistence.MerchantRuntime` และ SQLite ไม่มี filtered index; ลำดับที่ handler ใช้จริงถูก pin แยกใน `Payments.Tests` ผ่าน `SaveCountWhenMinted` (hook `FakeSessionRepository.OnAdd`)
- [x] 4. Order cancel — `ReleaseOpenSessionCommand` (ผ่าน `PaymentConfirmationService`: ไม่มี open → ok / ไม่มี chargeId → expire / มี chargeId → fetch ก่อน / สด → 409) + `CancelOrderCommand` + endpoint `POST /orders/{orderId}/cancel` (merchant-user + CSRF, ไม่มี iam key) — done = cancel ได้เมื่อปลอดภัยเท่านั้น idempotent
     Satisfies: REQ-4 (all criteria). Depends on: 3. Verify: `dotnet test tests/Orders.Tests tests/Payments.Tests tests/Hosts.Tests --filter Cancel`.
     Evidence:
       - test: `dotnet test tests/Payments.Tests --filter ReleaseOpenSession` -> 9 passed / 0 failed — ไฟล์ใหม่ `ReleaseOpenSessionHandlerTests`: ไม่มี open session -> ปล่อยผ่านโดยไม่แตะ vault (REQ-4.1), session ไม่มี chargeId เกิน TTL -> `MarkExpired` โดยไม่ถาม PSP (REQ-4.2), session สด -> `ConflictException` + สถานะไม่เปลี่ยน (REQ-4.3), session มี chargeId เกิน TTL + PSP ยังไม่ settle -> fetch ก่อนแล้วค่อย expire (REQ-4.2), PSP ตอบ Failed -> ปล่อยผ่าน, PSP ตอบ Paid -> `ConflictException` + session Paid + enqueue `PaymentPaid` (REQ-4.4 ที่ชั้น money), inquiry ล้ม 3 แบบ (`HttpRequestException`/`TaskCanceledException`/`JsonException`, Theory) -> `ConflictException` + session ยัง Redirected + SaveCount 0
       - test: `dotnet test tests/Orders.Tests --filter Cancel` -> 6 passed / 0 failed (4 ใหม่ + 2 เดิมที่ชื่อมีคำว่า Cancel) — ไฟล์ใหม่ `CancelOrderHandlerTests`: AwaitingPayment -> Cancelled + save 1 ครั้ง (REQ-4.1), Cancelled ซ้ำ -> สำเร็จไม่เปลี่ยนอะไร (REQ-4.5), Paid -> `InvalidOperationException` + สถานะคง Paid + ไม่ save (REQ-4.4), order ไม่รู้จัก -> `NotFoundException`
       - test (REQ-4 ระดับ HTTP): `dotnet test tests/Hosts.Tests --filter Cancel` -> 9 passed / 0 failed (7 ใหม่) — ไฟล์ใหม่ `OrderCancelEndpointTests` ยิงผ่าน route + policy `merchant-user` + CSRF filter + minimal-API lambda + mediator pipeline จริง (`WebApplicationFactory`, fake scheme + fake `IActorContext`/`IOrderRepository`/`ISessionRepository`/`IUnitOfWork` = ไม่มี DB; `PaymentConfirmationService` ตัวจริง resolve ผ่าน DI และตัดสิน offline เพราะทุกเคสเป็น session ไม่มี chargeId): ไม่มี session -> 200 + Cancelled, session เกิน TTL -> session Expired **แล้ว** order Cancelled, session สด -> 409 + order ยัง AwaitingPayment + session ยัง Created, order Paid -> 409, cancel ซ้ำ -> 200 ทั้งสองครั้ง, order ไม่รู้จัก -> 404, ไม่มี CSRF -> 403 + order ไม่เปลี่ยน (REQ-4.6)
       - test: `dotnet test tests/Hosts.Tests` -> 406 passed / 0 failed (suite เต็ม เดิม 399 รวม `ModelConsistencyTests` = ไม่มี pending model change; task นี้ไม่มีคอลัมน์ใหม่ ไม่มี migration)
       - test: `dotnet test tests/Architecture.Tests` -> 204 passed / 0 failed (suite เต็ม; ไม่มี `ExecuteInTransactionAsync` call site ใหม่ `TransactionInventoryTests` จึงไม่ต้องแก้)
       - test: `dotnet test` บน `tests/{Orders,Payments,Checkouts,Carts,Products}.Tests` -> 80 / 198 / 21 / 20 / 137 passed, 0 failed (ไม่มี suite ข้างเคียงพัง)
       - regression proof (ลำดับ release-ก่อน-cancel): สลับให้ endpoint ส่ง `CancelOrderCommand` ก่อน `ReleaseOpenSessionCommand` -> `Cancelling_an_order_with_a_live_payment_session_is_409` แดง (order ถูก Cancelled ทั้งที่ session ยังจ่ายได้) แล้ว restore -> เขียว
       - regression proof (whitelist REQ-4.3): เพิ่ม `Pending` เข้า whitelist ที่ปล่อยผ่าน -> `A_live_session_refuses_the_release` (Payments) + `Cancelling_an_order_with_a_live_payment_session_is_409` (Hosts) แดงทั้งคู่ แล้ว restore -> เขียว
       - regression proof (ambiguous fetch): เปลี่ยน `catch` filter จาก `HttpRequestException or TaskCanceledException or JsonException` เป็น `PspRejectedException` -> Theory `An_inquiry_that_fails_refuses_the_release` แดงทั้ง 3 เคส (exception ของ adapter ทะลุออกเป็น 500 แทน 409) แล้ว restore -> เขียว
       - gate: `dotnet build -warnaserror` -> 64 projects, 0 errors, 0 warnings
       - viewports: n/a — logic-only
       - deviations: (1) verify command ตามตัวอักษรรันไม่ได้ — `dotnet test` รับได้ project เดียว (MSB1008, trap #1 ของ T1) จึงแยกเป็น 3 คำสั่ง; (2) **ไม่มี iam key ใหม่และไม่มี gate site ใหม่** — endpoint ใช้แค่ `RequireAuthorization("merchant-user")` + `RequireUserCsrf()` ตาม REQ-4.6 จึงไม่แตะ `PermissionGateSitesTests` (ยังคง 26 sites); (3) `ReleaseOpenSessionCommand`/`CancelOrderCommand` ไม่รับ `MerchantId` (design เขียนไว้แบบนั้น) — `IMerchantScoped` เป็น marker ล้วน และ query filter คุม scope ให้แล้ว ต่างจาก `ResendOrderSummaryCommand` ที่ต้องใช้ merchantId ไปทำ outbox; (4) catch ของ ambiguous มีเงื่อนไข `&& !cancellationToken.IsCancellationRequested` ด้วย — client ที่ยกเลิก request เองไม่ใช่ "PSP ตอบไม่ได้" จึงไม่ควรกลายเป็น 409; (5) endpoint test ใช้ fake persistence port แทน DB (pattern trap #8 ของ T2) และเลือกเคสที่ session ไม่มี chargeId ทั้งหมด ทำให้ `PaymentConfirmationService` ตัวจริงตัดสินได้โดยไม่แตะ vault/PSP — ชั้น fetch-to-confirm พิสูจน์แยกที่ `Payments.Tests`; (6) `docs/reference/merchants.md` เพิ่มแถว cancel เข้าตาราง funnel endpoints; docs อื่นที่ยัง stale (`platform-modules.md`, `src-structure.md`) ยังคงค้างตามแผน T2 ที่จะแก้รอบเดียวตอนปิด PR
- [x] 5. กรองเอกสารขายแล้ว + double-sell signal — post-filter แถว PAID ใน `ListProductsHandler` เมื่อ filter ที่ส่งให้ SP คือ UNPAID, `OrderPaid` + `OrderId` (additive), `Product.MarkPaid(paidDate, orderId)` + คอลัมน์ `SoldOrderId`, `LogCritical` เฉพาะ `SoldOrderId` มีค่าและต่าง order (replay เงียบ) — done = เอกสารขายแล้วหายจากหน้า UNPAID
     Satisfies: REQ-5 (all criteria). Verify: `dotnet test tests/Products.Tests tests/Orders.Tests` + Integration.Tests เคสกรอง (fixture ตัวเอง ห้ามแตะ seed 42 แถว SaleCode 77001).
     Evidence:
       - test: `dotnet test tests/Products.Tests` -> 148 passed / 0 failed (เดิม 137) — `ListProductsHandlerTests` +5 (REQ-5.1: เอกสารที่ mirror เป็น PAID หายจากหน้า UNPAID แต่ยัง upsert อยู่, REQ-5.2: totals ยังเป็นของ SP ทั้งที่หน้าว่าง, Theory `ALL`/`PAID` ไม่ตัดอะไรเลย, แถว PAID ที่มาจาก upstream เองก็ถูกตัดด้วยกฎเดียวกัน); `ProductTests` +2 (`SoldOrderId` first-write ไม่ถูกเขียนทับโดยผู้ซื้อรายที่สอง, `Guid.Empty` ไม่ถูกจดเป็นผู้ซื้อ) + ขยาย `MarkPaid_sets_status_and_paid_date` ให้ assert `SoldOrderId`; `DocumentPaidOnOrderPaidConsumerTests` +4 (REQ-5.4: redeliver order เดิม = เงียบ, order ต่าง = `LogCritical` 1 ครั้งพร้อม order ทั้งสองใบในข้อความ + คง `SoldOrderId` ใบแรก, payload เก่าไม่มี order id = retire ได้แต่ไม่จด/ไม่ log, payload เก่ามาทีหลังก็ไม่ report double-sale)
       - test: `dotnet test tests/Orders.Tests` -> 80 passed / 0 failed (จำนวนเท่าเดิม) — `It_enqueues_OrderPaid_with_the_lines_product_ids_on_transition` เพิ่ม assertion `paid.OrderId == order.Id`: emitter นี้เป็นตัวเดียวในระบบ ถ้าไม่เติมค่า consumer จะแยก redeliver จาก double-sell ไม่ได้ตลอดไป
       - test (REQ-5.1/5.3/5.4 บน SQL Server จริง): `source .env.integration && dotnet test tests/Integration.Tests --filter ProductSearchFilter` -> 1 passed / 0 failed บน pol-db :11433 — ไฟล์ใหม่ `ProductSearchFilterIntegrationTests` เดินทั้งเส้นด้วย `ProductRepository`/`MerchantRuntimeDbContext`/`DocumentPaidOnOrderPaidConsumer` ตัวจริง: ค้นครั้งแรกเจอ 2 ใบ -> consumer ขายไป 1 ใบ -> ค้นซ้ำโดย upstream ยังรายงาน UNPAID ทั้งคู่ -> หน้า UNPAID เหลือใบเดียว แต่ `TotalRows` ยังเป็น 2 (REQ-5.2) -> ถาม `ALL` ยังเจอใบที่ขายแล้วเป็น PAID (REQ-5.3) -> อ่านแถวจาก DB ยืนยัน `SoldOrderId` = order ที่ซื้อ (คอลัมน์ใหม่ round-trip จริง)
       - test (contract additive): `dotnet test tests/BuildingBlocks.Tests` -> 44 passed / 0 failed (เดิม 43) — `OutboxSerializerTests.An_OrderPaid_payload_without_an_order_id_still_deserializes` ยิง JSON รูปเก่า (ไม่มี `orderId`) ผ่าน `OutboxSerializer.Options` ตัวเดียวกับที่ dispatcher ใช้ -> deserialize ได้ `OrderId = Guid.Empty` (ข้อความที่ค้างในตู้ outbox ตอน deploy replay ได้ ไม่ throw)
       - test: `dotnet test tests/Hosts.Tests` -> 406 passed / 0 failed (จำนวนเท่าเดิม) รวม `ModelConsistencyTests` = ไม่มี pending model change; `InsuranceCheckoutEndToEndTests.Paid_order_marks_the_product_PAID_so_it_can_no_longer_be_sold` เปลี่ยน assertion เป็น "หน้า UNPAID ว่าง + หน้า ALL ยังเจอใบเดิม PAID พร้อม PaidDate" (SQLite E2E เต็มเส้น order->consumer->ค้นซ้ำ)
       - test: `dotnet test tests/Architecture.Tests` -> 204 passed / 0 failed; `dotnet test` บน `tests/{Carts,Checkouts,Payments}.Tests` -> 20 / 21 / 198 passed, 0 failed; `--filter ProductUpsert` บน Integration.Tests -> 6 passed (upsert เดิมไม่พัง)
       - regression proof (REQ-5.1): พลิก post-filter เป็น `paymentStatus == nameof(PAID)` -> Products.Tests 4 failed (`A_document_sold_here_is_dropped_from_an_UNPAID_page`, `Dropping_a_sold_document_does_not_restate_the_procedures_totals`, `A_page_that_did_not_ask_for_UNPAID_keeps_the_sold_document(PAID)`, `An_upstream_PAID_row_is_dropped_from_an_UNPAID_page_too`) + Hosts.Tests `--filter InsuranceCheckout` 1 failed + Integration.Tests `--filter ProductSearchFilter` 1 failed — 3 ชั้นจับได้พร้อมกัน แล้ว restore -> เขียว
       - regression proof (REQ-5.4 ตัด noise): ตัดเงื่อนไข `soldOn != notification.OrderId` ออกจาก guard ของ `LogCritical` -> `A_redelivery_of_the_same_order_is_silent` แดง (redeliver ปกติกลายเป็น Critical) แล้ว restore -> เขียว
       - regression proof (first-write): ตัด `SoldOrderId is null` ออกจาก `Product.MarkPaid` -> `MarkPaid_keeps_the_first_order_that_bought_the_document` + `A_second_order_paying_for_a_sold_document_is_Critical` แดงทั้งคู่ (ผู้ซื้อใบที่สองเขียนทับใบแรก signal จึงเงียบตลอดกาล) แล้ว restore -> เขียว
       - regression proof (emitter): ถอด `order.Id` ออกจาก `_outbox.Enqueue(new Contracts.OrderPaid(...))` -> `It_enqueues_OrderPaid_with_the_lines_product_ids_on_transition` แดง แล้ว restore -> เขียว
       - migration: `20260802164627_ProductSoldOrderId` = `AddColumn`/`DropColumn` ล้วน (`shop.Products.SoldOrderId uniqueidentifier NULL`) — คอลัมน์บนตารางเดิมจึงไม่ต้อง GRANT เพิ่ม และ `docker/bootstrap/assert-fresh-db.sql` ไม่มี count ที่เกี่ยวกับ `shop.Products`; apply บน dev DB `VCentralPay` (:11433) `Applying migration ... Done.` และ replay ต่อบน catalog `VCentralPayT1Probe` สำเร็จเช่นกัน ไม่มี PendingModelChangesWarning
       - gate: `scripts/check-migration-lineage.sh` OK, `scripts/check-rename-identifiers.sh` OK, `dotnet build -warnaserror` -> 64 projects, 0 errors, 0 warnings
       - viewports: n/a — logic-only
       - deviations: (1) verify command ตามตัวอักษรรันไม่ได้ — `dotnet test` รับได้ project เดียว (MSB1008, trap #1 ของ T1) จึงแยกเป็นคนละคำสั่ง; (2) **ไม่มีการแก้ไฟล์ mirror configuration เลย** — `SoldOrderId` เป็น `Guid?` ซึ่ง EF map เป็น `uniqueidentifier NULL` ด้วย convention ทั้งใน `Products.Infrastructure.ProductConfiguration` (เจ้าของ migration) และ `Persistence.MerchantRuntime.Products.ProductConfiguration` (runtime) โดยไม่ต้องเขียน `builder.Property` ที่ไหน; `ModelConsistencyTests` + integration test ที่อ่านคอลัมน์ผ่าน `MerchantRuntimeDbContext` เป็นหลักฐานว่าทั้งสอง model เห็นคอลัมน์นี้ตรงกัน; (3) **assertion เดิมของ E2E ถูกแก้ ไม่ใช่แค่เพิ่มของใหม่** — `Paid_order_marks_the_product_PAID_so_it_can_no_longer_be_sold` เคย assert ว่าเอกสารที่ขายแล้ว **ยังอยู่** บนหน้าค้นหา default (ซึ่ง default = UNPAID) นั่นคือพฤติกรรมที่ REQ-5.1 มาแก้พอดี จึงเปลี่ยนเป็น assert หน้า UNPAID ว่าง + ยังพิสูจน์ REQ-5.3 ต่อผ่านหน้า `ALL`; (4) `Contracts.OrderPaid.OrderId` เป็น positional parameter ที่มี default (`Guid OrderId = default`) ไม่ใช่ required — เพื่อให้ payload v1 ที่ค้างใน outbox ตอน deploy deserialize ได้ (พิสูจน์ด้วย `OutboxSerializerTests` เคสใหม่ ไม่ใช่ด้วยการเดา STJ); (5) การตัดสิน double-sell อยู่ที่ **consumer ไม่ใช่ domain** — `Products.Domain` ไม่มี logger และไม่ควรมี, ส่วน `MarkPaid` เก็บแค่ first-write; (6) `docs/reference/products.md` อัปเดต 3 จุด (แถว `SoldOrderId` ในตารางคอลัมน์, ลายเซ็น `MarkPaid(paidDate, orderId)`, บรรทัด post-filter ของ `GET /products`) — docs อื่นที่ยัง stale (`platform-modules.md`, `src-structure.md`) ยังค้างตามแผน T2 ที่จะแก้รอบเดียวตอนปิด PR; (7) integration test ใช้ SaleCode `90001` + `DocumentNo` prefix ต่อรัน และลบด้วย prefix ตัวเองใน `DisposeAsync` — seed 42 แถว SaleCode 77001 ไม่ถูกอ่านและไม่ถูกแตะ; (8) probe DB `VCentralPayT1Probe` / `VCentralPayFreshT1` ยังค้างบน :11433 เหมือนเดิม (`destructive-guard` block `DROP DATABASE`) — task นี้ใช้ probe เดิมซ้ำ ไม่สร้างใหม่
- [x] 6. Checkout/Order enrichment — `PaymentChannel`/customer 3 field/`Discount: Money` บน `Checkouts.Session`+Items → `CheckoutConfirmed` (additive) → `Orders.Order`+Items, `NotificationRecipient = CustomerPhone ?? CustomerEmail ?? Recipient`, `IOrderNoSequence` port + `shop.OrderNoSeq` + GRANT + `BypassPrimitiveTests.AllowedPorts`, migration เขียนมือลำดับ nullable→backfill(OrderNo + แตก Recipient)→NOT NULL→unique index→DROP Recipient (ทุกคอลัมน์ NOT NULL มี DB DEFAULT), read surfaces (`GET /orders` + SFS `filters=orderNo:eq:`, detail, summary +orderNo), `CustomerOrderNotification`+OrderNo — done = order ใหม่มี OrderNo/channel/discount/customer ครบ + payload เก่า replay ได้
     Satisfies: REQ-6 (6.2-6.8), REQ-7 (all criteria). Verify: `dotnet test tests/Checkouts.Tests tests/Orders.Tests` + Integration.Tests sequence/GRANT + fresh-DB migration proof บน DB มีข้อมูล.
     Evidence:
       - test: `dotnet test tests/Checkouts.Tests` -> 34 passed / 0 failed (เดิม 21) — ไฟล์ใหม่ `CheckoutEnrichmentTests` 13 test: discount default 0 ในสกุลของบรรทัด (REQ-6.3), ยอด = Σ net ทั้งกรณี 1 และ 2 บรรทัด, discount > บรรทัด -> reject, discount คนละสกุล -> reject (REQ-6.4), amount ไม่เท่า Σ net -> reject ทั้งสองทิศ (REQ-6.5), channel นอกชุด -> reject + Theory 3 channel ที่รองรับ (REQ-6.1/6.2), wire value = ชื่อ member; `ConfirmCheckoutTests` แก้ `Start_captures_the_channel_and_the_customer` (REQ-6.1/6.6) + `Confirm_transitions_and_emits_CheckoutConfirmed` assert `PaymentChannel`/`CustomerName`/`CustomerPhone`/`CustomerEmail` + `Recipient` ที่ยังถูกเติมเป็นค่าเดียวกับที่ consumer derive (REQ-6.8)
       - test: `dotnet test tests/Orders.Tests` -> 93 passed / 0 failed (เดิม 80) — ไฟล์ใหม่ `OrderEnrichmentTests` 10 test: order รับ OrderNo/channel/customer/discount ครบ (REQ-7.1/7.2), recipient = phone ก่อน แล้ว email (F-03), payload v1 ไม่มี field ใหม่ยังเปิด order + ยังแจ้งลูกค้าผ่าน `Recipient` เดิม + customer เป็นค่า placeholder เดียวกับ DB DEFAULT (REQ-7.5), v1 ไม่มี recipient -> ไม่ enqueue, discount 0 รับสกุลจากบรรทัดไม่ใช่ default THB ของ payload, replay ไม่มินต์เลขซ้ำและไม่เปิด order ที่สอง (REQ-7.1), order ไม่มีเลขสร้างไม่ได้, ยอด order ต้องเท่า Σ net; `GetOrdersTests` +3 (OrderNo บน list surface REQ-7.3, filter orderNo แคบผลจริง REQ-7.4, filter ที่ไม่ตรง -> ว่าง)
       - test: `dotnet test tests/SharedKernel.Tests` -> 75 passed / 0 failed (เดิม 46) — `CustomerContactTests` (REQ-6.6/6.7: name/phone บังคับ, phone format 4 เคสผิด/3 เคสถูก, email optional + 4 รูปแบบผิดรวม display-name, ข้อความ error ไม่ echo ค่า, ลำดับ phone->email->null, placeholder ตรงกับ DB DEFAULT) + `LineAmountsTests` (REQ-6.3/6.4: gross/net, discount ว่าง -> 0 ในสกุลบรรทัด, เท่าบรรทัดได้, เกินบรรทัด/คนละสกุล reject, ติดลบสร้าง `Money` ไม่ได้ตั้งแต่แรก)
       - test (REQ-7.5 payload เก่า): `dotnet test tests/BuildingBlocks.Tests` -> 46 passed / 0 failed (เดิม 44) — `OutboxSerializerTests` +2: JSON รูป v1 ของ `CheckoutConfirmed` (ไม่มี channel/customer/discount) และ `CustomerOrderNotification` (ไม่มี orderNo) deserialize ผ่าน `OutboxSerializer.Options` ตัวจริง ได้ค่า default ครบ ไม่ throw
       - test (REQ-6 ระดับ HTTP): `dotnet test tests/Hosts.Tests --filter MerchantLifecycle` -> 32 passed / 0 failed (เดิม 13) — `MerchantLifecycleEndpointTests` +19 ยิงผ่าน route + policy + CSRF + lambda จริง: Theory 3 channel ลงคอลัมน์ + customer 3 field (REQ-6.1/6.6), channel ผิด/ตัวพิมพ์เล็ก/ว่าง/ตัวเลขล้วน `"0"`,`"2"`/comma list `"CARD,INSTALLMENT"` -> 400 และไม่มี session ถูกสร้าง + cart ยัง Open (REQ-6.2), customer ขาด/ชื่อว่าง/เบอร์ว่าง/เบอร์สั้น/อีเมลผิด -> 400 (REQ-6.7), discount หักออกจากยอดจริง (REQ-6.3), discount เกินบรรทัด/ติดลบ -> 400 (REQ-6.4), `amount` ที่ client ส่งไม่ตรงยอด server -> 400 และตรง -> 200 (REQ-6.5)
       - test (REQ-7.3/7.4 ระดับ HTTP): `dotnet test tests/Hosts.Tests --filter OrderCancelEndpointTests` -> 12 passed / 0 failed (เดิม 7) — 4 test ใหม่บน `GET /api/v1/orders`: `filters=[{"field":"orderNo","operator":"eq","value":"…"}]` ส่งค่าถึง repository จริงและ OrderNo อยู่ใน response, ไม่มี filter = ขอทั้งหมด (null ไม่ใช่ "ไม่ตรงอะไรเลย"), field/operator ที่ไม่รองรับถูกทิ้งเงียบตามกฎ whitelist ของ SFS, `filters` ที่ไม่ใช่ JSON -> 400 (ไม่ใช่ 500)
       - test: `dotnet test tests/Hosts.Tests` -> 431 passed / 0 failed (suite เต็ม เดิม 406) รวม `ModelConsistencyTests` = ไม่มี pending model change และไฟล์ใหม่ `OrderSummaryEndpointTests` (REQ-7.3 surface ที่สาม: fake `IOrderSummaryReader` แล้วยิง `GET /api/v1/orders/{token}/summary` จริง assert property `orderNo` บน JSON wire — ถอด OrderNo ออกจาก `OrderSummaryResponse` = test นี้แดง)
       - test: `dotnet test tests/Architecture.Tests` -> 206 passed / 0 failed (เดิม 204) — `BypassPrimitiveTests.AllowedPorts` +1 (`Persistence.MerchantRuntime/Orders/OrderNoSequence.cs`) และ `MoneyColumnMappingTests` +2 (`CheckoutSessionItems.Discount`/`OrderItems.Discount` -> `DiscountAmount decimal(19,4)` + `DiscountCurrency char(3)` non-unicode fixed-length)
       - test: `dotnet test` บน `tests/{Carts,Payments,Products}.Tests` -> 20 / 198 / 148 passed, 0 failed (ไม่มี suite ข้างเคียงพัง)
       - test (REQ-7.1 บน SQL Server จริง): `source .env.integration && dotnet test tests/Integration.Tests --filter "OrderNoSequence|OrderSummaryReader|OrderItemPolicyGrants"` -> 6 passed / 0 failed บน pol-db :11433 — ไฟล์ใหม่ `OrderNoSequenceIntegrationTests`: `sys.database_permissions` ยืนยัน pol_app มี GRANT UPDATE บน `shop.OrderNoSeq` แล้ว **consume จริง 2 ค่าในฐานะ pol_app** (monotonic), รูปเลขจาก `OrderNoSequence.Format` ตัวจริง = 13 ตัวอักษร `ORD69` + running 8 หลัก, INSERT เลขซ้ำ -> `SqlException` 2601/2627 พร้อมชื่อ `IX_Orders_OrderNo` (เลขเดียวกับที่ `MerchantRuntimeUnitOfWork` แปลงเป็น 409); `OrderSummaryReaderIntegrationTests` sync SQL ทั้ง 2 query ให้ตรง `OrderSummaryReader` ตัวจริงแบบ column-for-column (query แรก 8 คอลัมน์รวม OrderNo, query สอง 4 คอลัมน์รวม ProductId, FieldCount = 4 ยังพิสูจน์ว่าไม่ select InsuredDateOfBirth) — สำเนามือเดิม drift ไปแล้วทั้งคู่ (REQ-7.3)
       - migration proof (DB มีข้อมูล): `20260802172209_CheckoutOrderEnrichment` — สร้าง catalog `VCentralPayT6Probe` (:11433) + `CREATE USER pol_app`, migrate ถึง `20260802164627_ProductSoldOrderId`, INSERT ข้อมูลรูปเก่า (order 3 ใบ: recipient เป็นอีเมล / เป็นเบอร์ / null, checkout session 2 ใบ: อีเมล / เบอร์) แล้วรัน migration -> ผลตรวจ: OrderNo backfill ครบ (`ORD6900000001..03`) null 0 แถว ซ้ำ 0 แถว, คอลัมน์เป็น `varchar(13) NOT NULL` + `IX_Orders_OrderNo` unique, แตก recipient ถูกทาง (`buyer@example.com` -> CustomerEmail, `0812345678` -> CustomerPhone, null -> ไม่มีทั้งคู่), `shop.Orders.NotificationRecipient` **ยังอยู่ครบค่าเดิม** (F-03), `shop.CheckoutSessions.NotificationRecipient` ถูก DROP แล้ว (0 คอลัมน์) และ split ถูกต้อง, DiscountAmount/DiscountCurrency ของแถวเก่าได้ `0`/`THB`, `sys.sequences.OrderNoSeq` current_value 3 + GRANT UPDATE ให้ pol_app 1 รายการ
       - migration proof (rollback): `ef database update 20260802164627_ProductSoldOrderId` บน probe -> `Reverting migration ... Done.`, `shop.CheckoutSessions.NotificationRecipient` กลับมาพร้อมค่าเดิม (`session@example.com` / `0899999999`), `shop.Orders.OrderNo` และ sequence หายไปทั้งคู่ แล้ว re-apply -> `Applying migration ... Done.` (ขึ้น-ลง-ขึ้น ได้จริง); dev DB `VCentralPay` apply สำเร็จเช่นกัน
       - regression proof (REQ-7.1 replay): ย้ายการมินต์เลขไปก่อน idempotency skip ใน `CheckoutConfirmedConsumer` -> `A_replay_mints_no_second_number` แดง แล้ว restore -> เขียว
       - regression proof (F-03 ลำดับ recipient): สลับเป็น `notificationRecipient ?? customer.NotificationRecipient` -> `Without_a_phone_the_email_becomes_the_recipient` แดง แล้ว restore -> เขียว
       - regression proof (REQ-6.3/6.5): ให้ `Session.Start` บวก gross แทน net -> Checkouts.Tests 3 failed (`A_discounted_line_prices_the_checkout_at_the_net_total`, `Discounts_across_several_lines_all_come_off_the_total`, `An_amount_that_is_not_the_net_sum_is_rejected(15000)`) แล้ว restore -> เขียว
       - regression proof (REQ-7.4 SFS whitelist): เปลี่ยนเงื่อนไข operator เป็น `!= In` (รับ `like` ด้วย) -> `Listing_orders_silently_drops_a_filter_it_does_not_support(like)` แดง แล้ว restore -> เขียว
       - gate: `scripts/check-migration-lineage.sh` OK, `scripts/spec-trace.sh purchase-flow-completion` OK (54 เกณฑ์ครบ), `dotnet build -warnaserror` -> 64 projects, 0 errors, 0 warnings; `scripts/check-rename-identifiers.sh` **รอบแรกแดง** (exit 1, 11 hit) — helper ใน `CheckoutEnrichmentTests` ชื่อ `Line(...)` ชนกับ retired identifier `Line` (OrderLine->OrderItem, policy-reference-record REQ-7.5) และ gate อ่านจาก `git ls-files` จึงเงียบตอนยังไม่ `git add`; แก้แล้ว (helper -> `Insured(...)`) และรันซ้ำหลัง `git add` -> OK
       - verifier fix (REQ-6.2 ค่าที่ไม่ใช่ชื่อ member): `POST /checkouts` เดิมใช้ `Enum.TryParse` + `Enum.IsDefined` ซึ่งรับทั้งตัวเลข (`"0"`,`"2"`) และ comma list (`"CARD,INSTALLMENT"`) — เปลี่ยนเป็นเทียบกับ `Enum.GetNames<PaymentChannel>()` (Ordinal) ก่อน parse; regression proof: คืนโค้ดเดิมชั่วคราว -> `Starting_a_checkout_with_an_unsupported_channel_is_400` แดง 3 เคสใหม่ (`"0"`,`"2"`,`"CARD,INSTALLMENT"`) แล้ว restore -> เขียว 6/6
       - verifier fix รอบ 2 (REQ-7.3 surface 2/3 ไม่มีด่าน): เดิมไม่มี test ไหน assert `OrderDetailView.OrderNo` และไม่มี test แตะ `orderNo` บน `OrderSummaryResponse` wire เลย (ถอดออกแล้ว build ผ่าน test เขียวหมด) — เพิ่ม assertion `OrderNo` ใน `GetOrderDetailTests.Every_lines_full_InsuredIdNumber_is_returned` (Orders.Tests ยัง 93/0), ไฟล์ใหม่ `OrderSummaryEndpointTests` ล็อก wire (Hosts.Tests 430->431), sync SQL สำเนามือใน `OrderSummaryReaderIntegrationTests` ให้ตรง reader จริง (Integration --filter OrderSummaryReader 1/0 บน :11433)
       - viewports: n/a — logic-only
       - deviations: (1) verify command ตามตัวอักษรรันไม่ได้ — `dotnet test` รับได้ project เดียว (MSB1008, trap #1 ของ T1) จึงแยกคำสั่ง; (2) **`filters=orderNo:eq:<value>` ใน design.md ทำเป็นสัญญา SFS จริงของ repo** คือ JSON DSL `filters=[{"field":"orderNo","operator":"eq","value":"…"}]` ผ่าน `SfsQueryParser` ที่มีอยู่ (endpoint อื่นอีก 5 ตัวใช้ตัวนี้) — ไม่ประดิษฐ์ wire format ที่สองแบบ `field:op:value`; `page/limit/sort/search` ยังไม่รับบน `GET /orders` จึงไม่ใส่ `SfsQueryParamsMarker` (ไม่ประกาศ param ที่ endpoint ไม่ได้ใช้); (3) **`Discount` บน contract เป็น `DiscountAmount`/`DiscountCurrency` (Money แบบแตกร่าง)** ตามตาราง Contract evolution ของ design — เพราะ `Money` เป็น struct ที่ `default` ไม่ถูกต้อง (Currency null) จึงไม่มี default ที่ปลอดภัยสำหรับ positional record; ชั้น domain/DB ยังเป็น `Money` เต็มรูป (`MoneyColumnMappingTests` คุม) และ discount 0 รับสกุลจากบรรทัด ไม่ใช่ 'THB' ตายตัว; (4) **`CustomerContact` + `LineAmounts` วางใน `SharedKernel`** ไม่ใช่ซ้ำใน 2 domain — Checkouts กับ Orders ถือคอลัมน์ชุดเดียวกันและอ้างกันไม่ได้ ถ้าแยกจะได้กฎ validate/money 2 ชุดที่ drift; (5) **`Order.Create` เพิ่ม `orderNo` เป็น parameter บังคับ** (แก้ call site 28 จุดใน test) แทนการปล่อยให้ set ทีหลัง — คอลัมน์เป็น NOT NULL, aggregate ที่สร้างได้โดยไม่มีเลขคือ aggregate ที่ commit ไม่ได้; (6) `Checkouts.Domain.Session` **ตัด `NotificationRecipient` ทิ้ง** (คอลัมน์ถูก DROP ตาม design) — ปลายทางแจ้งลูกค้าย้ายไปเป็น `CustomerPhone ?? CustomerEmail`; ฝั่ง `Orders.Order.NotificationRecipient` คงไว้ตาม F-03; (7) `StartCheckoutRequest.Recipient` ถูกแทนที่ด้วย `customer{name,phone,email}` — เป็น breaking change ของ body ตัวเดียวในงานนี้ ตรงตาม REQ-6.6 ที่ระบุว่าเป็น contract หน้าจอ Merchant SPA; (8) DB DEFAULT ที่ migration ใส่เป็น named constraint (`DF_*`) ซึ่ง EF model ไม่รู้จัก — `Down()` จึง DROP CONSTRAINT ก่อน DROP COLUMN เอง (migration ถัดไปที่จะลบคอลัมน์เหล่านี้ต้องทำแบบเดียวกัน); (9) `Integration.Tests` เต็มชุดมี 3 test แดงที่ **มีอยู่ก่อนงานนี้** (`SpDocumentContractTests` เรื่อง coverage window — ยืนยันด้วยการ `git stash` แล้วรันซ้ำได้ผลเดิม) นอกนั้น 122 passed; (10) probe DB `VCentralPayT6Probe` (ใหม่, ของ task นี้) + `VCentralPayT1Probe` / `VCentralPayFreshT1` (เดิม) ยังค้างบน :11433 — `destructive-guard` block `DROP DATABASE` จึงไม่ลบเอง (ไม่ bypass hook), dev DB `VCentralPay` ไม่ถูกแตะนอกจาก apply migration
- [x] 7. 2C2P 3 ช่องทาง — ขยาย `TwoCTwoPAdapter.SupportedMethods` + `PaymentChannelFor` (`card`→CC, `promptpay`→QR, `installment`→IPP), อัปเดต `EnabledMethods` ใน seed-demo + บันทึก ops step สำหรับ connection เดิม, eligibility check ตอน `POST /checkouts` (channel ที่ connection ไม่รองรับ → 400) — done = เลือกได้เฉพาะช่องทางที่ชาร์จได้จริง
     Satisfies: REQ-6 (6.1). Depends on: 6. Verify: `dotnet test tests/Payments.Tests --filter TwoCTwoP` + `tests/Hosts.Tests --filter Checkout`.
     Evidence:
       - test: `dotnet test tests/Payments.Tests --filter TwoCTwoP` -> 47 passed / 0 failed — `SupportedMethods` ประกาศครบ 3 method (เดิม card อย่างเดียว) และ `CreateRedirectCharge_derives_the_payment_channel_from_the_session_method` เป็น Theory 3 เส้น pin `card`→`CC` / `promptpay`→`QR` / `installment`→`IPP` บน claim `paymentChannel` ของ JWT ที่ยิงจริง; เคส "ปฏิเสธ method ที่ honour ไม่ได้" ย้ายไปใช้ค่านอก vocabulary (`wallet`, `CC`) เพราะ promptpay/installment ไม่ใช่ค่าที่ต้องปฏิเสธอีกแล้ว
       - test (mapping จุดเดียว): `dotnet test tests/Payments.Tests --filter PaymentMethods` -> `PaymentMethodsTests` +9 — `PaymentMethods.ForChannel` map `CARD`/`PROMPTPAY_QR`/`INSTALLMENT` -> `card`/`promptpay`/`installment` และปฏิเสธ (400) ค่าที่ไม่ใช่ channel wire value (`card`, `promptpay_qr`, `PROMPTPAY`, `QR`, ว่าง, null) — ฟังก์ชันนี้คือจุดเดียวที่ channel กลายเป็น method ทั้ง checkout gate และ (T8) เส้นจ่ายของลูกค้า
       - test (eligibility handler): `dotnet test tests/Payments.Tests --filter MethodPayable` -> 7 passed / 0 failed — ไฟล์ใหม่ `MethodPayableHandlerTests`: 3 channel ที่ทั้ง connection และ adapter รับ -> true, method ที่ connection ไม่เปิด -> false, method ที่ adapter ขับไม่ได้ -> false, connection `IsEnabled=0` -> false, ไม่มี connection เลย -> false, connection ของ merchant อื่นไม่นับ, method นอก vocabulary -> `ArgumentException` (400) ไม่ใช่ false
       - test: `dotnet test tests/Payments.Tests` -> 218 passed / 0 failed (suite เต็ม เดิม 198)
       - test (REQ-6.1 ระดับ HTTP): `dotnet test tests/Hosts.Tests --filter Checkout` -> 33 passed / 0 failed; `--filter MerchantLifecycle` -> 35 passed / 0 failed (เดิม 32) — `MerchantLifecycleEndpointTests` +3: channel ที่ connection ของ merchant ไม่เปิด (`PROMPTPAY_QR`/`INSTALLMENT` บน connection `card`) -> 400, ไม่มี session ถูกสร้าง, cart ยัง Open และ `CARD` เส้นเดียวกันยัง 200 (พิสูจน์ว่า gate อ่าน method list จริง ไม่ได้ปัดทุกอย่าง), merchant ที่ไม่มี connection เลย -> 400; `CartFactory` เพิ่ม fake `IConnectionRepository` (พารามิเตอร์ `enabledMethods`, default 3 method) โดยยังใช้ `TwoCTwoPAdapter` ตัวจริงที่ host register (`SupportedMethods` ไม่ต้องยิง HTTP)
       - test: `dotnet test tests/Hosts.Tests` -> 434 passed / 0 failed (suite เต็ม เดิม 431) รวม `ModelConsistencyTests` = ไม่มี pending model change (task นี้ไม่มี migration)
       - test: `dotnet test` บน `tests/{Checkouts,Carts,Orders,Products,Merchants,SharedKernel,BuildingBlocks,Architecture}.Tests` -> 34 / 20 / 93 / 148 / 134 / 75 / 46 / 206 passed, 0 failed (ไม่มี suite ข้างเคียงพัง)
       - regression proof (eligibility gate): ถอด `MethodPayableQuery` ออกจาก `POST /checkouts` (เหลือแค่เรียก `ForChannel`) -> Hosts.Tests `--filter MerchantLifecycle` แดง 3 (`Starting_a_checkout_on_a_channel_the_merchant_cannot_be_charged_on_is_400` 2 เคส + `..._with_no_connection_is_400`) ด้วย `Expected: BadRequest / Actual: OK` แล้ว restore -> เขียว
       - regression proof (channel mapping): เปลี่ยน `promptpay` -> `"CC"` -> `CreateRedirectCharge_derives_the_payment_channel_from_the_session_method(promptpay)` แดง (`Expected ["QR"] / Actual ["CC"]`) แล้ว restore -> เขียว
       - regression proof (gate อ่าน adapter จริง): หด `SupportedMethods` กลับเป็น `{ card }` -> `Starting_a_checkout_records_the_channel_and_the_customer` แดง 2 เคส (`PROMPTPAY_QR`, `INSTALLMENT`) — พิสูจน์ว่า eligibility ที่ endpoint intersect กับ capability ของ adapter จริง ไม่ได้ดูแค่ `EnabledMethods` แล้ว restore -> เขียว
       - ops step (บังคับก่อน/พร้อม deploy): connection ที่มีอยู่แล้วบน env ใด ๆ ต้องเพิ่ม method ใหม่เอง — `UPDATE txn.PspConnections SET EnabledMethods = 'card,promptpay,installment' WHERE Psp = 0 AND MerchantId = <merchant>;` (ค่าเดิมเป็น subset ของ `merch.Merchants.EnabledChannels`) มิฉะนั้น `POST /checkouts` จะปฏิเสธ 400 และ `CreateSessionHandler.EnsureEligible` ยัง 409 — บันทึกไว้ใน `docker/bootstrap/seed-demo.sql` (comment เหนือ `txn.PspConnections`) และต้องเข้า PR body
       - gate: `scripts/check-rename-identifiers.sh` OK (รันหลัง `git add` ตาม trap #32), `dotnet build -warnaserror` -> 64 projects, 0 errors, 0 warnings
       - viewports: n/a — logic-only
       - deviations: (1) verify command ตามตัวอักษรรันไม่ได้ทั้งบรรทัด — `dotnet test` รับ project เดียว (MSB1008, trap #1) จึงแยกคำสั่ง; (2) **ไม่ได้แก้ค่า `EnabledMethods` ใน seed-demo** — 2C2P connection ของ vPrivilege เปิดครบ 3 method อยู่แล้ว ส่วน vCommerce (`card,promptpay`) / vSouvenir (`card`) ถูกออกแบบให้แคบโดยเจตนาใน spec `demo-seed-data` (invariant ที่ระบุไว้: `EnabledMethods` ⊆ `merch.Merchants.EnabledChannels`) การขยายให้ครบ 3 ต้องแก้ `EnabledChannels` ของ merchant ตามไปด้วย = แก้ data design ของอีก spec โดยไม่ได้ประโยชน์ต่อ REQ-6.1 และยังทำให้เคส "ช่องทางที่ merchant นี้จ่ายไม่ได้" ไม่มีข้อมูล demo เหลือ; แก้เป็นอัปเดต comment ให้ตรงความจริง + เขียน ops step แทน; (3) **จุด mapping channel→method อยู่ที่ `Payments.Domain.PaymentMethods.ForChannel`** ไม่ใช่ข้าง ๆ `Checkouts.Domain.PaymentChannel` — `Checkouts.Domain` reference ได้แค่ `SharedKernel` จึงเห็น `PaymentMethods` ไม่ได้ ถ้าวางฝั่ง Checkouts จะต้องเขียน literal `"card"`/`"promptpay"`/`"installment"` ซ้ำ (drift บนเส้น money); host import `Payments.Domain` ทั้ง namespace ไม่ได้เพราะ `SessionStatus` ชนกับของ Admins จึงใช้ alias `using PaymentMethods = Payments.Domain.PaymentMethods;`; (4) eligibility ถามผ่าน mediator query ใหม่ `MethodPayableQuery` (Payments.Application) ตาม design "host-layer query ไปยัง Payments" — คืน `bool` ไม่ throw เพราะ endpoint ต้องแปลงเป็น 400 ขณะที่ `CreateSessionHandler` ยังถือ throw/409 ไว้เหมือนเดิม (ไม่แตะ) จึงมีกฎ 2 ที่แต่ถามคำถามเดียวกัน มี test คุมทั้งคู่; (5) merchant ที่ไม่มี 2C2P connection เลยได้ 400 เท่ากับ channel ที่ปิดอยู่ (design ระบุ 400 สำหรับ gate นี้ ไม่แยกเคส); (6) ไม่ได้รัน `tests/Integration.Tests` ในรอบนี้ — task นี้ไม่แตะ EF model/migration/SQL เลย (`ModelConsistencyTests` ใน Hosts.Tests ยืนยันไม่มี pending model change)
- [ ] 8. Customer payment path — `POST /orders/{token}/pay` + `POST /orders/{token}/payment-status` (AllowAnonymous + policy `customer-payment` ~10/นาที/IP + short-circuit terminal), `ConfirmPaymentStatusCommand` (4 ค่า รวม `Cancelled`), `OrderSummary` contract change (+MerchantId ห้าม leak, +OrderNo, +PaymentChannel, −PaymentSessionId), token หมดอายุ → 404, `GET /payments/sessions/{id}` + แก้ `GetSessionHandler` → `NotFoundException` — done = ลูกค้าจ่ายครบวงจาก summary link ถึงหน้าสถานะ
     Satisfies: REQ-8 (all criteria). Depends on: 3, 6, 7. Verify: `dotnet test tests/Payments.Tests tests/Hosts.Tests --filter "MerchantLifecycle|CustomerPayment"` + manual E2E บน dev (sandbox 2C2P).

## Suggested execution batches

> Feature นี้ COUPLED หนัก (contracts + migration + `PaymentConfirmationService` แชร์ข้ามงาน) —
> DEFAULT = รันทุก task ใน session เดียว: `scripts/pane-loop.sh purchase-flow-completion all-in-one`
> (หรือ `/spec-implement all`) ตามลำดับ 1→2→3→4→5→6→7→8
> ไม่มี Batch tag — ไม่มีคู่ task ที่เล็ก+ชนิดเดียวกันพอจะได้ประโยชน์; task 3 กับ 6 เป็น
> foundational/core ควรได้ context สด ถ้าจะแยก pane ให้แยกที่รอยต่อ PR: (1,2) / (3,4,5) / (6,7,8)
