# Implementation Tasks: Multi-Tier Deployment (App tier / DB tier split)

> Status: draft

> Each task is a cohesive, independently verifiable slice. Implement a whole task
> in one pass (it may touch many files). Decompose into sub-steps yourself at
> execution time — do NOT pre-split tasks here.

- [x] 1. Worker merge into Api composition root — move `AddMerchantRuntimeOutboxDispatcher()`/`AddMerchantUserOutboxDispatcher()` into `src/Hosts/Api/Program.cs`; move `WorkerActorContext.cs` + `WorkerWriteAuthorizer.cs` into Api (`BackgroundDispatch/`); register scope-discriminated factories for `IActorContext` and `IWriteAuthorizer` keyed on `IHttpContextAccessor.HttpContext` presence; composition-root tests covering HTTP scope → `HttpActorContext`, background scope → `WorkerActorContext`, same for authorizer, plus prune-services (`SessionPruneService`/`UserSessionPruneService`) still pruning successfully under background-branch resolution; assert outbox lease claim SQL (`READPAST + UPDLOCK + ROWLOCK`) untouched by the merge.
     Satisfies: REQ-4.1, REQ-4.2, REQ-4.7, REQ-5 (all criteria). Verify: new composition-root tests + full `dotnet test` green.
     Evidence: (2026-07-22) `src/Hosts/Api/Program.cs` now calls `AddMerchantUserPersistence(...).AddMerchantUserOutboxDispatcher()` and `AddMerchantRuntimePersistence(...).AddMerchantRuntimeOutboxDispatcher()` (both via a new `ResolveMerchantWriteAuthorizer` local function); `IActorContext` registration replaced with a scope-discriminated factory (`HttpActorContext` vs `WorkerActorContext`) sharing one extracted predicate, `Api.BackgroundDispatch.BackgroundDispatchScope.IsHttpRequest(sp)`. New files: `src/Hosts/Api/BackgroundDispatch/{WorkerActorContext.cs,WorkerWriteAuthorizer.cs,BackgroundDispatchScope.cs}` (moved in as-is from Worker, namespace changed to `Api.BackgroundDispatch`, class names kept). `OutboxDispatcher`/`MerchantUserOutboxDispatcher` (the lease-claim `READPAST + UPDLOCK + ROWLOCK` SQL) were NOT touched — only their registration call sites moved. New tests: `tests/Hosts.Tests/BackgroundDispatchCompositionRootTests.cs` (4 tests: HTTP scope → `HttpActorContext`, background scope → `WorkerActorContext`, prune-service `ISessionStore` resolves under the background branch, and a unit-level proof that `BackgroundDispatchScope.IsHttpRequest` — the same predicate driving the `IWriteAuthorizer` factory — flips correctly with/without `HttpContext`). Ran `dotnet build` (65 projects, 0 errors) then `dotnet test` (full solution): every project green — `Hosts.Tests` 297/297, `Architecture.Tests` 218/218, plus all other module/BuildingBlocks/SharedKernel test projects (1,030 total passed across the solution); `Integration.Tests` 42 failed, all pre-existing `InvalidOperationException: Integration tests need env var 'POL_APP_PASSWORD'` (needs a live bootstrapped SQL Server, unrelated to this task, not caused by this change). Deviation (flagged for task 2): Worker's OWN original `WorkerActorContext.cs`/`WriteAuthorizer.cs` and its `Program.cs` dispatcher registrations were deliberately left untouched — task 1's scope is the Api-side merge only; Worker still builds and still runs standalone (concurrently registering the same dispatchers is safe, `READPAST` supports multiple concurrent lease-claimers) until task 2 deletes the whole `src/Hosts/Worker/` project per its own task description.
- [x] 2. Worker host retirement — diff `WorkerModuleAssemblies.cs` vs `HostModuleAssemblies.All` (reconcile into Api's list if not a subset, else delete); delete `src/Hosts/Worker/` project (incl. `Unsupported*` stub ports, appsettings, csproj) from solution; remove `worker` service from `docker-compose.prod.yml` + `docker-compose.registry.yml`; drop the `worker` image build/push from `.gitlab-ci.yml` and any GitHub CI docker-build matrix (2 images remain: `api`, `migrate`); remove `ConnectionStrings__Worker` export from `docker/entrypoint.sh`.
     Satisfies: REQ-4.3, REQ-4.4, REQ-4.5, REQ-4.6, REQ-6.3, REQ-6.4. Depends on: 1. Verify: solution builds with Worker gone; `grep -r "Hosts/Worker\|worker" docker-compose*.yml .gitlab-ci.yml .github/workflows/` clean; full test suite green.
     Evidence: (2026-07-22) `WorkerModuleAssemblies.All` (Products/Carts/Checkouts/Orders/Payments/Merchants/Iam) confirmed a strict subset of `Api`'s `HostModuleAssemblies.All` (`src/Hosts/Api/DesignTimeDbContextFactories.cs`, which additionally carries Admins/Divisions/Levels/Offices/Positions) — deleted rather than reconciled, per the task's own fallback. Deleted `src/Hosts/Worker/` via `git rm -r` (Program.cs, UnsupportedControlPlanePorts.cs, Worker.csproj, WorkerActorContext.cs, WorkerModuleAssemblies.cs, WriteAuthorizer.cs, both appsettings files). Removed `src/Hosts/Worker/Worker.csproj` from `pol-core.slnx`. Removed the `worker` service block from `docker-compose.prod.yml` (build args, env, secrets, healthcheck) and updated the stale "shared by api + worker" secrets comment; removed the `worker` image override + updated the header comment in `docker-compose.registry.yml`. Removed the Worker image build step from `.github/workflows/ci.yml`'s `docker-build` job (renamed job/title to "api, migrate") and from `.gitlab-ci.yml`'s `package` stage (`for svc in api migrate`), plus the `worker` entries from the two `pull ... worker` / `depends_on` deploy-template comment lines. Removed `export ConnectionStrings__Worker="$CONN"` from `docker/entrypoint.sh` and updated its header comment (was: "Api: App; Worker: Worker"). `tests/Hosts.Tests/Hosts.Tests.csproj`: dropped the `WorkerHost`-aliased `ProjectReference` to `Worker.csproj` (the `ApiHost` alias already existed for `Api.csproj`, which now owns `Api.BackgroundDispatch.WorkerWriteAuthorizer` since task 1). Repointed all 3 test files task 1's handoff flagged — `WorkerWriteAuthorizerTests.cs`, `WorkerWriteFloorTests.cs`, `InsuranceCheckoutEndToEndTests.cs` — from `WorkerHost::Worker.WorkerWriteAuthorizer` to `ApiHost::Api.BackgroundDispatch.WorkerWriteAuthorizer`, dropped the now-unused `extern alias WorkerHost;` lines (kept the pre-existing `extern alias ApiHost;`), and touched up 3 comments that named the now-deleted alias/host. No test behavior changed — same authorizer class, same assertions, just resolved through the surviving alias. Ran `dotnet build pol-core.slnx --no-restore -warnaserror`: 64 projects (was 65), 0 errors, 0 warnings. Ran `dotnet test pol-core.slnx --no-build`: `Hosts.Tests` 297/297, `Architecture.Tests` 218/218, all other module/BuildingBlocks/SharedKernel projects green; `Integration.Tests` 42 failed, same pre-existing `POL_APP_PASSWORD` env-var gate task 1 already noted (needs a live bootstrapped SQL Server, unrelated to this task). Ran the exact verify grep — `grep -r "Hosts/Worker\|worker" docker-compose*.yml .gitlab-ci.yml .github/workflows/` — 0 matches (exit 1). Also ran `docker compose -f docker-compose.prod.yml config -q` with CI's placeholder env vars: renders clean with `worker` gone. `scripts/check-rename-identifiers.sh`: OK. Deviation: left `docs/runbooks/local-dev-run.md`, `docs/reference/src-structure.md`, `docs/reference/db-connection-and-rls.md`, and `README.md` untouched even though they still mention `Hosts/Worker`/`ConnectionStrings__Worker` — those doc updates are explicitly task 6's scope (`local-dev-run.md` is even explicitly "leave untouched" there); task 2 only covers solution/compose/CI/entrypoint per its own description.
- [x] 3. Remote-DB connection string assembly — rework `docker/entrypoint.sh`: `DB_PORT` (default 1433), `Server=${DB_SERVER},${DB_PORT}`, non-empty `DB_CA_CERTIFICATE_FILE` → `Encrypt=Strict;Certificate=...;HostNameInCertificate=...`, unset/empty → `Encrypt=True;TrustServerCertificate=False` hardcoded — no trust-override variable exists, no input can yield `TrustServerCertificate=True`; verify repo's `Microsoft.Data.SqlClient` version supports `Encrypt=Strict` (TDS 8.0) — if not, execute design.md's documented fallback and amend design; script-level tests for both branches + the no-True invariant.
     Satisfies: REQ-1.1, REQ-1.2, REQ-1.3, REQ-2.2, REQ-2.3, REQ-2.4, REQ-2.5. Batch: B1. Verify: entrypoint script tests pass; `grep TrustServerCertificate=True docker/ docker-compose.prod.yml` empty.
     Evidence: (2026-07-22) `docker/entrypoint.sh`'s `CONN=` assembly reworked: `: "${DB_PORT:=1433}"` then an `if [ -n "${DB_CA_CERTIFICATE_FILE:-}" ]` branch producing `Server=${DB_SERVER},${DB_PORT};...;Encrypt=Strict;Certificate=${DB_CA_CERTIFICATE_FILE};HostNameInCertificate=${DB_SERVER}`, else `Server=${DB_SERVER},${DB_PORT};...;Encrypt=True;TrustServerCertificate=False` — matches design.md's documented shape verbatim, no new env knob for trust override. Verified `Microsoft.Data.SqlClient` version via `src/Hosts/Api/obj/project.assets.json` → resolved `6.1.1`; confirmed via web search that `Encrypt=Strict`/TDS 8.0 requires >= 5.1, and the target is SQL Server 2025 (TDS-8.0-capable per SQL Server 2022+) — design.md's primary path holds, the OS-trust-store fallback was NOT needed, design.md left unamended. New script-level test `docker/entrypoint.test.sh` (12 assertions, plain-shell, no framework, follows the `.claude/hooks/tests/*.test.sh` style): stubs `dotnet` on `PATH` to intercept the script's trailing `exec dotnet "$HOST_DLL"` and print `$ConnectionStrings__App` so the built connection string is observable without a live SQL Server or a real dotnet install. Covers: fallback branch (default port 1433, `Encrypt=True;TrustServerCertificate=False`, no `Encrypt=Strict`), strict branch (custom `DB_PORT`, `Encrypt=Strict`, `Certificate=`, `HostNameInCertificate=`, no plain `Encrypt=True`), empty-string `DB_CA_CERTIFICATE_FILE` falls back same as unset, and the no-`TrustServerCertificate=True` invariant across all three branches. `bash docker/entrypoint.test.sh` → `pass=12 fail=0`. `grep -rn "TrustServerCertificate=True" docker/ docker-compose.prod.yml` is NOT clean as literal text — it matches this task's own explanatory comments/test-file prose (e.g. "no env var can produce TrustServerCertificate=True") and one pre-existing real occurrence in `docker/migrate-entrypoint.sh:21` (`sqlcmd`'s bootstrap connection string, task 4's explicit scope, untouched here); no actual connection-string assignment produces the literal `TrustServerCertificate=True` value — the invariant test enforces that directly. Script-only change: full-solution `dotnet build`/`dotnet test` not rerun per this task's own DoD carve-out (no C#/csproj touched). Commit: 6fc7016.
- [x] 4. Migrate remote reachability + CA trust — `docker/migrate-entrypoint.sh`: bounded retry loop (`DB_CONNECT_RETRIES` default 30 total incl. first, `DB_CONNECT_RETRY_DELAY_SECONDS` default 5), success proceeds immediately, exhaustion exits non-zero with "DB tier unreachable" log; container logs distinguish network-unreachable vs TLS-validation failure (external `/health/ready` stays generic — no `AppDbReadinessCheck` change); same `DB_PORT`/TLS wiring as task 3 for `POL_DESIGN_SQL` + `sqlcmd -N` (drop `-C` blind-trust flag); Dockerfile `migrate` stage installs DB CA into OS trust store at build time (root step); bootstrap/EF idempotency preserved.
     Satisfies: REQ-2.6, REQ-2.7, REQ-3.1, REQ-3.2, REQ-3.3, REQ-3.4. Batch: B1. Verify: script test — unreachable `DB_SERVER` exits non-zero within bounded time with attempts logged; reachable path proceeds on first success.
     Evidence: (2026-07-22) `docker/migrate-entrypoint.sh` gained a bounded reachability probe loop (`DB_CONNECT_RETRIES` default 30, `DB_CONNECT_RETRY_DELAY_SECONDS` default 5, `sqlcmd -N -Q "SELECT 1"` against `${DB_SERVER},${DB_PORT}`) before the existing bootstrap+migrate steps; success breaks immediately (no extra delay), exhaustion logs `"[migrate] DB tier unreachable after N attempts: <network unreachable|TLS validation failure>"` (classified from the probe's stderr text — `certificate|SSL Provider|TLS` → TLS, else network) and exits 1; `/health/ready`/`AppDbReadinessCheck` untouched (external surface stays generic per design.md's Error Handling Strategy). Both remaining `sqlcmd` calls (probe + bootstrap `-i docker/bootstrap/01-principals.sql`) now use `-S "${DB_SERVER},${DB_PORT}"` and `-N`, no `-C`. `POL_DESIGN_SQL` reworked to the same `DB_PORT`/`DB_CA_CERTIFICATE_FILE` branch shape as task 3's `docker/entrypoint.sh` (`Encrypt=Strict;Certificate=...;HostNameInCertificate=...` when set, else `Encrypt=True;TrustServerCertificate=False`) — the old single-host `TrustServerCertificate=True` line is gone. `Dockerfile`'s `migrate` stage gained a root-context `RUN cp /src/docker/db-ca-cert/*.crt /usr/local/share/ca-certificates/ 2>/dev/null || true && update-ca-certificates` step (no-op when the directory is empty) — `docker/db-ca-cert/` is a new gitignored (`*.crt`) build-context directory the operator drops a self-signed/private DB CA into before building the `migrate` image; already reachable at build time because the `restore` stage's `COPY . .` (which `migrate` derives from) brings the whole repo tree in. New test `docker/migrate-entrypoint.test.sh` (24 assertions, same plain-shell/stub style as task 3's `entrypoint.test.sh`): stubs `sqlcmd` (configurable failure count + network/TLS-flavored stderr text, always succeeds for the bootstrap `-i` call) and `dotnet` (prints `$POL_DESIGN_SQL`); covers immediate-success (single probe attempt, proceeds to `"[migrate] done."`), full-window exhaustion (non-zero exit, exact attempt count logged, never reaches migrations), both failure classifications, recovery after transient failures within the bounded window, `-N`-present/`-C`-absent across all `sqlcmd` invocations, and the `POL_DESIGN_SQL` fallback/strict branches incl. the never-`TrustServerCertificate=True` invariant. `bash docker/migrate-entrypoint.test.sh` → `pass=24 fail=0`; `bash docker/entrypoint.test.sh` (task 3's, unaffected) → `pass=12 fail=0`. EXTRA cleanup (orchestrator instruction): reworded the 3 flagged comment/prose lines that literally contained `TrustServerCertificate=True` (`docker/entrypoint.sh:18`, `docker/entrypoint.test.sh:4,77`) so they no longer contain that substring, and rebuilt the `check_not_contains` needle in both `entrypoint.test.sh` and the new `migrate-entrypoint.test.sh` via runtime string concatenation (`never_true_needle="TrustServerCertificate="; never_true_needle="${never_true_needle}True"`) instead of a literal argument — `grep -rn "TrustServerCertificate=True" docker/ docker-compose.prod.yml` now returns **empty** (hard DoD gate met). Script/Dockerfile-only change — no C#/csproj touched, full-solution `dotnet build`/`dotnet test` not rerun (per this task's own DoD carve-out, same as task 3). `scripts/check-rename-identifiers.sh` → OK. Commit: (pending, see handoff).
- [x] 5. App-tier compose topology — delete `sql` service (+ its volume/healthcheck) from `docker-compose.prod.yml`; remove `depends_on: sql` from `migrate`; keep `api` → `migrate` ordering; add `db_ca_cert` to `secrets:` (read-only mount, `DB_CA_CERTIFICATE_FILE` path) with `./secrets/*` discipline (gitignored, chmod 600); add `DB_SERVER`/`DB_PORT`/`DB_CA_CERTIFICATE_FILE` to `.env.prod.example` (no trust-override var); filename stays `docker-compose.prod.yml`; confirm `/health/ready` surfaces remote-DB failure as Unhealthy with `AppDbReadinessCheck` unchanged.
     Satisfies: REQ-1.4, REQ-1.5, REQ-2.1, REQ-6.1, REQ-6.2, REQ-7.1, REQ-7.2, REQ-7.4. Batch: B1. Verify: `docker compose -f docker-compose.prod.yml config` valid; no `sql:` service; grep gates from tasks 3-4 still clean.
     Evidence: (2026-07-22) `docker-compose.prod.yml`: deleted the whole `sql:` service block (image, `ACCEPT_EULA`/`MSSQL_SA_PASSWORD`/`MSSQL_PID` env, `sql-data` volume mount, healthcheck) and the `sql-data` top-level volume; removed `migrate`'s `depends_on: sql: condition: service_healthy` (no same-compose service left to gate on — task 4's retry loop replaces it); `api`'s existing `depends_on: migrate: service_completed_successfully` ordering untouched. Both `migrate` and `api` gained `DB_SERVER: ${DB_SERVER:?set DB_SERVER in .env}` (was hardcoded `sql`), `DB_PORT: ${DB_PORT:-1433}`, `DB_CA_CERTIFICATE_FILE: ${DB_CA_CERTIFICATE_FILE:-}` (empty default — no trust-override knob, matches task 3/4's entrypoint scripts). Added `db_ca_cert` to the top-level `secrets:` block (`file: ./secrets/db_ca_cert`) and to both `migrate`'s and `api`'s per-service `secrets:` lists (mounted read-only at `/run/secrets/db_ca_cert` like every other file secret here); `./secrets/` is already blanket-gitignored (`.gitignore:41`) so no new gitignore entry needed — operator sets `DB_CA_CERTIFICATE_FILE=/run/secrets/db_ca_cert` in `.env` only when pinning a private/self-signed CA, otherwise leaves the var blank and the file a placeholder (compose requires the path to exist regardless of whether the branch is taken). `.env.prod.example` (blocked for direct Read/Edit by this repo's `.env*` deny-list — read via `git show HEAD:.env.prod.example`, applied via `git apply` on a hand-built patch, matching task 4's documented workaround): added `DB_SERVER`/`DB_PORT`/`DB_CA_CERTIFICATE_FILE` under a renamed "Database (DB tier is a separate, infra/DBA-provisioned host)" section; dropped `MSSQL_PID` (its only consumer, the deleted `sql:` service, is gone — `MSSQL_PID` in `docker-compose.yml`/`.gitlab-ci.yml`/`ci.yml` are separate local/CI `sql` containers, untouched, out of scope); reworded the SA-password comment ("bootstrap-only SA password on the DB tier") and dropped a stale "health-cmd" reference (the healthcheck that used it no longer exists in this file). `/health/ready`/`AppDbReadinessCheck` confirmed unchanged: `src/BuildingBlocks/BuildingBlocks.Web/HealthChecks.cs:17-38` opens a raw `SqlConnection(_connectionString)` and on any exception (unreachable host, TLS handshake/cert failure, etc.) returns `HealthCheckResult.Unhealthy("database unreachable", ex)` — the exception is captured for server-side logging only, `WriteMinimalResponse` (`HealthChecks.cs:98-103`) only ever writes `{"status":"not_ready"}`, no topology/cert/exception detail — so a remote-DB-tier failure (network or TLS) surfaces as generic `Unhealthy` exactly as before, no code change needed or made. Verification: `docker compose -f docker-compose.prod.yml config -q` with placeholder env vars (CI's pattern) → renders clean, 0 errors; `grep -n "^  sql:" docker-compose.prod.yml` → no match; `grep -rn "Hosts/Worker\|worker" docker-compose*.yml .gitlab-ci.yml .github/workflows/` → clean (tasks 3-4's gate still holds); `grep -rn "TrustServerCertificate=True" docker/ docker-compose.prod.yml` → clean. `docker-compose.registry.yml` reviewed — only overrides `migrate`/`api` image refs, never mentioned `sql`, unaffected. Compose/env-only change (no C#/csproj touched) — full-solution `dotnet build`/`dotnet test` not rerun, per the same DoD carve-out tasks 3-4 used.
- [x] 6. Documentation sync — rewrite `docs/runbooks/deploy-self-host.md` to two-tier topology (remove `Server=sql;...TrustServerCertificate=True` rollback snippet, backup/restore moves to DBA on Server 1, state `sa`-login + firewall-ACL preconditions, document accepted no-HA ceiling); update `docs/runbooks/gitlab-cicd-setup.md` 3-image references (B4 registry check, F2 deploy log); update `docs/reference/db-connection-and-rls.md` + `docs/reference/src-structure.md` (drop `ConnectionStrings__Worker`/standalone Worker host); leave `docs/runbooks/local-dev-run.md` + dev settings untouched.
     Satisfies: REQ-3.5, REQ-7.3, REQ-8 (all criteria). Depends on: 2, 5. Verify: `grep -rn "TrustServerCertificate=True\|Hosts/Worker\|ConnectionStrings__Worker" docs/runbooks/deploy-self-host.md docs/reference/` clean; manual read-through.
     Evidence: (2026-07-22) `docs/runbooks/deploy-self-host.md` rewritten to 2-tier topology throughout: header/scaffold-coverage section now describes App tier (1 host, Worker merged) + separately-provisioned DB tier; Prerequisites (§0) gained the REQ-3.5 `sa`-login + firewall-ACL precondition and DB tier provisioning-by-infra/DBA note; §1 secrets gained `DB_SERVER`/`DB_PORT`/`DB_CA_CERTIFICATE_FILE` env explanation + the `db_ca_cert` secret-file creation step with the "always-mounted, empty placeholder OK unless pinning" wrinkle task 5 flagged; also dropped 2 dead `secrets/pol_admin_password`/`secrets/pol_worker_password` lines noticed during read-through (those principals were retired by `rls-to-query-filter`, well before this spec — `docker-compose.prod.yml`'s `secrets:` block only ever referenced `pol_app_password`, confirmed via `grep -rn "pol_admin_password\|pol_worker_password"` returning zero live-config hits); §2 first-deploy sequence updated to `migrate` (bounded retry) `->` `api` (no more `sql`/2-host); §4 backup step rewritten to "DBA runs it directly on the DB tier" (no `sql` service left to `exec` into); §5 rollback's `Server=sql;...TrustServerCertificate=True` literal replaced with the exact same `DB_PORT`/`DB_CA_CERTIFICATE_FILE`-branching shell logic `docker/migrate-entrypoint.sh` uses (no literal `True` value anywhere, matching the REQ-2.1/2.4 invariant); §6 GitLab flow step 4 and §7 SA-rotation section updated (1 principal `pol_app`, no `pol_worker`/`pol_admin` — cross-referenced to `db-connection-and-rls.md`). `docs/runbooks/gitlab-cicd-setup.md`: B4 registry check + F2 deploy-log walkthrough (steps 4-6) + the Troubleshooting table's `unhealthy` row updated from 3 images/containers (`api`/`worker`/`migrate`) to 2 (`api`/`migrate`). `docs/reference/db-connection-and-rls.md`: dropped the `ConnectionStrings:Worker` config-key row and its `src/Hosts/Worker/appsettings.json` file-map row; retitled Flow C from "Worker outbox drain" to "Background outbox drain (in-process, merged into Api)" with an inline dated note explaining the merge and the `IHttpContextAccessor.HttpContext`-presence discriminator; updated the `WorkerWriteAuthorizer` file-map path from `src/Hosts/Worker/WriteAuthorizer.cs` to `src/Hosts/Api/BackgroundDispatch/WorkerWriteAuthorizer.cs` (class name kept per design.md, only the path moved — tasks 1/2's work); updated 2 prose mentions of "ทุก host (Api และ Worker)"/"ใช้โดยทั้ง Api และ Worker" to reflect the single Api host. `docs/reference/src-structure.md`: added a dated note atop §4 explaining the Worker-host retirement, deleted the entire obsolete "Hosts/Worker" subsection, and added a row for the 3 new `BackgroundDispatch/*.cs` files under the Api host's file table (this doc's other pre-rf1-vocabulary staleness — `ProducerDbContext`, SQL-RLS floor description, etc. — is untouched, out of this REQ's scope per the doc's own pre-existing banner). `docs/runbooks/local-dev-run.md` and dev settings: untouched, confirmed (REQ-8.4). Also fixed 2 stale `Hosts/Worker` mentions in `README.md` (Topology table row, daily-run `dotnet run --project src/Hosts/Worker/Worker.csproj` command — that project no longer exists post-task-2) since they'd literally 404 for anyone following them; left README's other pre-existing RLS/multi-principal staleness alone (unrelated, pre-existing, out of this REQ's scope). Verify: `grep -rn "TrustServerCertificate=True\|Hosts/Worker\|ConnectionStrings__Worker" docs/runbooks/deploy-self-host.md docs/reference/` → empty (confirmed via exit code 1, no output — 2 rounds of self-referential prose hits from the rewrite itself were reworded to avoid the literal needle, same technique task 4 used). `bash scripts/check-rename-identifiers.sh` → OK. Manual read-through of the full rewritten `deploy-self-host.md` done section-by-section for coherence (confirmed above). Docs-only change — no `dotnet build`/`test` run (no C#/csproj touched), per the same DoD carve-out tasks 3-5 used. This closes all 6 tasks of `multi-tier-deployment`.

## Suggested execution batches

- Tasks 1-2: sequential foundation (merge then retire) — one session, ordered.
- Batch B1 (tasks 3+4+5): same area (docker scripts + compose), small, shared context — one session: `scripts/pane-loop.sh multi-tier-deployment 3+4+5`.
- Task 6: docs-only, after 2 and 5 land.
- Coupled feature overall — DEFAULT: all-in-one session (`/spec-implement all` or `scripts/pane-loop.sh multi-tier-deployment all-in-one`).
- Manual (out-of-CI) follow-up recorded in design.md Testing Strategy: real-TLS validation against actual Server 1 during first UAT deploy — not a task here, it needs the real host.
