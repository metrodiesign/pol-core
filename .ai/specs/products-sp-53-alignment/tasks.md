# Implementation Tasks: products-sp-53-alignment

> Status: unknown
> อ่าน `requirements.md` + `design.md` + `HANDOFF.md` ก่อนเริ่มทุกครั้ง. Traps ทั้งหมดอยู่ใน design.md หัวข้อ "Traps"
> Branch: `feat/products-sp-53-alignment`. ห้าม push develop, ห้าม force push, ห้าม merge
> ลำดับ task ตรงกับ T1-T9 ใน `HANDOFF.md` หนึ่งต่อหนึ่ง
> **task gate**: ตอน flip `- [ ]` -> `- [x]` ต้องเติม `Evidence:` ใน Edit เดียวกัน และบรรทัด `Evidence:` ห้ามมี `-` นำหน้า
> **build จะแดงระหว่าง T2-T4** (ตัดคอลัมน์แล้ว consumer ยังไม่ตาม) — ปกติ ห้ามแก้ไฟล์นอก task ตัวเองเพื่อดับแดง

- [x] 1. **T1 — spec artifacts + เอกสารต้นเรื่องเข้า repo** — สร้าง `.ai/specs/products-sp-53-alignment/{requirements,design,tasks}.md`; `git add docs/reference/vcentralpay-sp-quick-reference.pdf`; แก้ XML doc comment ที่อ้าง "VCentralPay SP guide" ให้ชี้ path จริงใน `src/Modules/Products/Products.Domain/{DocumentType.cs,PaymentStatus.cs,ProductGroup.cs,Product.cs}` (แก้แค่ comment ห้ามแตะโค้ด); ต่อท้าย section T1 ใน `HANDOFF.md` + อัปเดตตารางลำดับ task
     **Done** = `bash scripts/spec-trace.sh products-sp-53-alignment` พิมพ์บรรทัดขึ้นต้น `OK:`; `dotnet build src/Modules/Products/Products.Domain` 0 error (comment-only edit); commit ครบทั้ง 3 spec + PDF + HANDOFF + 4 comment fix
     Satisfies: REQ-11 (11.1, 11.2)
     Evidence: `bash scripts/spec-trace.sh products-sp-53-alignment` -> `OK: 'products-sp-53-alignment' เกณฑ์ 53 ข้อ ถูกอ้างครบใน design.md และ tasks.md, EARS lint ผ่านทุกข้อ` (exit 0 — real pass ไม่ใช่ silent-skip เพราะ heading เป็น `## REQ-N:` ทุกข้อ); `dotnet build src/Modules/Products/Products.Domain` -> `ok dotnet build: 2 projects, 0 errors, 0 warnings`; comment fix 4 ไฟล์ (`DocumentType.cs`, `PaymentStatus.cs`, `ProductGroup.cs`, `Product.cs` 2 จุด: class summary + `ShowName`) แตะเฉพาะ XML doc ไม่มีบรรทัดโค้ดเปลี่ยน (`git diff --stat` = 4 ไฟล์ comment-only); `docs/reference/vcentralpay-sp-quick-reference.pdf` เข้า git แล้ว. Viewports 375/768/1440: n/a (ไม่มี UI). Deviations: (1) แก้ comment ใน `Product.cs` 2 จุด (class summary บรรทัด 6 + `ShowName` บรรทัด 52) ไม่ใช่จุดเดียวตาม brief — ทั้งสองจุดอ้างเอกสารแบบลอย ๆ เหมือนกัน; (2) comment ใน `Product.cs` ที่เขียนว่า `TotalPremium` เป็น EF complex type decimal(19,4) + char(3) ยังคงเดิม (จะผิดหลัง T2) — เป็นของ T2 ไม่ใช่ comment-only fix

- [x] 2. **T2 — Domain** — `Products.Domain/Product.cs`: ลบ `BranchCode`/`IsActive`/`CreatedAt`/`Deactivate()`/computed `Money?` 4 ตัว/`RequireThb` + call; `TotalPremium` -> `decimal`, breakdown 4 ตัว -> `decimal?`; `Create(ProductInput)` ตัด param `createdAt`; `MarkPaid` เหลือ `PaymentStatus` + `PaidDate`; เพิ่ม guard scale <= 2 ตำแหน่งกับค่าเงินทุกตัว; `ProductInput.cs` ตัด `BranchCode` + premium เป็น decimal
     **Done** = `dotnet build src/Modules/Products/Products.Domain` 0 error/0 warning; `dotnet test tests/Products.Tests --filter FullyQualifiedName~ProductTests` เขียวหลังปรับเคสที่กระทบ (เคสอื่นและโปรเจกต์อื่นยังแดงได้ ให้บันทึกว่าอะไรแดง)
     Satisfies: REQ-1 (1.1-1.7), REQ-2 (2.2, 2.3)
     Evidence: `dotnet build src/Modules/Products/Products.Domain` -> `Build succeeded. 0 Warning(s) 0 Error(s)` (2 projects: SharedKernel, Products.Domain). `Product` เหลือ field ตรง §5.2: ลบ `BranchCode`/`IsActive`/`CreatedAt`/`Deactivate()`/computed `Money?` 4 ตัว/`RequireThb`; `decimal TotalPremium` + `decimal? NetPremium|Stamp|TaxVat|CommissionAmount`; `Create(ProductInput)` ตัด `createdAt`; `MarkPaid` เหลือ `PaymentStatus` + `PaidDate`; guard ใหม่ `RequireMoney` (negative -> throw, `decimal.Round(v, 2) != v` -> throw) เรียกกับค่าเงินทั้ง 5 ตัว. viewports: n/a (domain layer). deviations: ส่วน "dotnet test tests/Products.Tests --filter ProductTests" ของ Done clause ไม่ได้รัน — `tests/` อยู่นอกขอบเขต T2 (มอบให้ T8 ตาม `HANDOFF.md`); build แดงที่ค้างไว้ตามคาด = `Products.Application/{ProductView.cs,CreateProductCommand.cs}` (T3) แล้วต่อด้วย EF config ×2 / `ProductSfs.cs` / `Program.cs` (T4-T5) และ tests (T8) — รายละเอียด error ใน `HANDOFF.md` section T2

- [x] 3. **T3 — Application** — `ListProducts.cs`: `ProductListItem` = §5.2 32 field + `Id`, `ProductFilterDto` เพิ่ม `[Required][MaxLength(20)] SaleCode` (trim ห้ามว่าง -> throw อ้าง 50005) + `PaymentStatus` เป็น `string?` + computed `PaymentStatusFilter` (`ALL` -> null, ค่าอื่น -> throw อ้าง 50007) + `Parse(null/blank)` throw, `ListProductsQuery` เลิก inherit `PagedQuery` ประกาศ `Page`/`Limit` + `required ProductFilters`, แก้ XML doc; `GetProductById.cs` คืน `ProductListItem`; ลบ `ProductView.cs`, `GetProductsQuery.cs`, `IProductRepository.ListByTenantAsync`
     **Done** = `dotnet build src/Modules/Products/Products.Application` 0 error; `dotnet test tests/Products.Tests/ProductFilterDtoTests.cs` (เคส 50005/50007/absent/ALL) เขียว; Hosts/Persistence ยังแดงได้
     Satisfies: REQ-3 (3.1, 3.2, 3.3, 3.4, 3.5, 3.6), REQ-7 (7.5), REQ-8 (8.1, 8.2, 8.3), REQ-9 (9.1, 9.2, 9.3, 9.4)
     Evidence: `dotnet build src/Modules/Products/Products.Application` -> `Build succeeded. 0 Warning(s) 0 Error(s)` (5 projects). `ProductListItem` = §5.2 ทั้ง 32 field ตามลำดับเอกสาร + `Id` (`InsuranceType` เป็น computed จาก `ProductGroup` จึงมี ctor param 32 ตัว) + `From(Product)` แทน `ProductView.From`; ลบ `ProductView.cs`, `GetProductsQuery.cs`, `IProductRepository.ListByTenantAsync`; `GetProductByIdQuery` คืน `ProductListItem?`; `ProductFilterDto` เพิ่ม `[Required][MaxLength(20)] SaleCode` (trim + ห้ามว่าง -> 50005), `PaymentStatus` เป็น `string?` + computed `PaymentStatusFilter` (absent -> UNPAID, `ALL` -> null, ค่าอื่น/ผิด case -> 50007), `Parse(null/blank)` throw, คง 50003/50008/50009; `ListProductsQuery` เลิก inherit `PagedQuery` ประกาศ `Page = 1`/`Limit = 25` + `required ProductFilters`; `CreateProductHandler` ตัด `IClock`; แก้ XML doc ที่ผิด 2 จุด ("slim subset", "Optional"/"authorization scope"). viewports: n/a (application layer, ไม่มี UI). error path ทั้ง 15 เคส (50005 x4, 50007 x3, 50003/50008/50009, MaxLength, absent=UNPAID, ALL=null, PAID, trim + `branchCode` ถูก ignore) ตรวจผ่าน harness ชั่วคราวใน scratchpad -> `ALL CHECKS PASSED`. deviations: ส่วน `dotnet test tests/Products.Tests/ProductFilterDtoTests.cs` ของ Done clause ไม่ได้รัน — `tests/` อยู่นอกขอบเขต T3 (มอบให้ T8 ตาม `HANDOFF.md`) จึงใช้ harness ชั่วคราวนอก repo แทน แล้วส่งเคสทั้งชุดต่อให้ T8; `ProductListItem` ไม่มี `MerchantId` (ไม่ใช่ field §5.2 และไม่มี consumer) — T4 ต้องตัดออกจาก projection; build ยังแดงที่ `Persistence.MerchantRuntime` / `Hosts/Api` / `tests` ตามคาด (รายละเอียดใน `HANDOFF.md` section T3)

- [x] 4. **T4 — Hosts + Repository** — `Program.cs`: gate x2 (`:679` cart 400, `:776` checkout 409) เป็น `PaymentStatus != UNPAID`, currency boundary `:683` `Money.Of(product.TotalPremium, "THB")`, `CreateProductRequest` ตัด `BranchCode` + premium decimal, `GET /products` ใช้ `SfsQueryParser.ParsePaging` + marker OpenAPI ใหม่ (page/limit/productFilters) + แก้ `.WithDescription`; `ProductRepository.ListAsync` ลบการเรียก SFS + `OrderBy(DocumentNo)` + `SaleCode` filter + `PaymentStatusFilter` + per-row Motor gate ของทะเบียนรถ + window ผ่าน `IClock`; ลบ `ProductSfs.cs` ทั้งไฟล์
     **Done** = `dotnet build pol-core.slnx` 0 error (ยกเว้น tests ที่รอ T8 — ระบุให้ชัดว่าไฟล์ไหน); ไม่มีการแตะ SFS machinery ที่โมดูลอื่นใช้ (`grep` ยืนยันผู้ใช้ 6 รายเดิมยังอยู่)
     Satisfies: REQ-2 (2.1), REQ-3 (3.4), REQ-5 (5.1, 5.2), REQ-6 (6.1, 6.2, 6.3, 6.4), REQ-7 (7.1, 7.2, 7.3, 7.4), REQ-8 (8.4)
     Evidence: `rtk proxy dotnet build src/Hosts/Api` -> `Build succeeded.` 0 error 0 warning (ตรวจโดย patch ชั่วคราวที่ `ProductConfiguration.cs` 2 ไฟล์ของ T5 ให้ compile ผ่าน แล้ว `git checkout --` คืนทั้งสองไฟล์ก่อน commit — diff ของ T4 ไม่มีสองไฟล์นั้น). `rtk proxy dotnet build pol-core.slnx` -> error 32 + 32 อยู่ใน `Products.Infrastructure/ProductConfiguration.cs` และ `Persistence.MerchantRuntime/Products/ProductConfiguration.cs` เท่านั้น (ของ T5) ไม่มี error ในไฟล์อื่นของ `src/`; tests ยังไม่ถูก compile เพราะ Persistence ล้มก่อน (T8). `grep -rln "ApplyFilters\|ApplySort\|ApplySearch" src/` ยืนยันผู้ใช้ SFS machinery เดิมยังอยู่ครบ 6 ราย (`Persistence.ControlPlane/Admins/{UserSfs,UserRepository}`, `Persistence.ControlPlane/Iam/{RoleSfs,RoleStore}`, `Persistence.MerchantRuntime/Orders/Items/{PolicyReportSfs,PolicyReportRepository,AdminItemPolicyReader}`, `Admins.Infrastructure/Persistence/Users/UserSfs`, `Iam.Infrastructure/Persistence/Roles/RoleSfs`) และไม่มี `ProductSfs` เหลือใน `src/`. viewports: n/a (ไม่มี UI). deviations: (ก) ไม่ได้รัน test ใด ๆ — `tests/` เป็นของ T8 และยัง compile ไม่ได้จนกว่า T5 เสร็จ; (ข) ไม่แตะ cap `limit` 100 -> 25 (REQ-4 ของ T7) `ParsePaging` จึงยัง clamp `[1, 100]` โดย `Parse` เรียก `ParsePaging` ต่อ ⇒ T7 แก้บรรทัดเดียวได้ทั้ง 7 endpoint; (ค) ไม่มี endpoint `GET /products/{id}` ในโค้ด (grep แล้ว `GetProductByIdQuery` ถูกเรียกจาก cart/checkout ภายในเท่านั้น) จึงไม่มี `.Produces<ProductView>` ให้แก้; (ง) `AddPagingParameters` คืน `IList<IOpenApiParameter>` เพราะ compiler มองข้าม `??=` ข้าม method (CS8602)

- [x] 5. **T5 — EF config x2 + migration + snapshot** — `ProductConfiguration.cs` ทั้ง 2 ไฟล์ (`Products.Infrastructure/`, `Persistence.MerchantRuntime/Products/`): ลบ mapping 8 คอลัมน์, เลิก `ComplexProperty` + `Ignore` ของ computed Money, `HasPrecision(19, 2)`, rename 4 คอลัมน์, ลบ `IX_Products_MerchantId_IsActive`; `dotnet ef migrations add ProductsSp52Alignment` แล้วตรวจ scaffold (DropIndex + DropColumn x8 + RenameColumn x4 + AlterColumn, ไม่มี DropTable) + `Down()` คืนได้ครบ; regen snapshot ด้วย `dotnet ef` เท่านั้น
     **Done** = `dotnet ef database update` ผ่านบน :11433; `dotnet test tests/Hosts.Tests --filter FullyQualifiedName~ModelConsistencyTests` เขียว; `bash scripts/check-migration-lineage.sh` ผ่าน
     Satisfies: REQ-10 (10.1, 10.2, 10.3, 10.4)
     Evidence: migration `20260730113459_ProductsSp52Alignment` (ต่อจาก `20260730081227`). `Up()` = `DropIndex IX_Products_MerchantId_IsActive` + `DropColumn` x8 (`BranchCode`, `IsActive`, `CreatedAt`, `TotalPremiumCurrency`, `NetPremiumCurrency`, `StampCurrency`, `TaxVatCurrency`, `CommissionCurrency`) + `RenameColumn` x4 (`TotalPremiumAmount`->`TotalPremium`, `NetPremiumAmount`->`NetPremium`, `StampAmount`->`Stamp`, `TaxVatAmount`->`TaxVat`) + `AlterColumn` `decimal(19,4)`->`decimal(19,2)` x5 (4 ตัวที่ rename + `CommissionAmount` ที่ชื่อคอลัมน์ตรงอยู่แล้ว) — ไม่มี `DropTable` จึงไม่ต้อง re-GRANT (ยืนยัน `pol_app` ยังมี `SELECT,INSERT,UPDATE,DELETE` บน `shop.Products` หลัง replay). `Down()` = rename กลับ 4 + `AlterColumn` กลับ `(19,4)` x5 + `AddColumn` x8 + `CreateIndex IX_Products_MerchantId_IsActive`. EF config ทั้ง 2 ไฟล์เลิก `ComplexProperty`/`Ignore` computed Money 4 ตัว/`HasColumnName` แล้ว (`diff` ของบรรทัด `Property|HasIndex|Ignore` ระหว่างสองไฟล์ต่างกันแค่ comment เรื่อง named `HasIndex`). `rtk proxy dotnet build src/Hosts/Api` -> `Build succeeded.` 0 error; `rtk proxy dotnet build pol-core.slnx` -> error ที่เหลือ **อยู่ใน `tests/` เท่านั้น** 192 error 10 ไฟล์ (`Products.Tests/ProductTests.cs` 68, `Architecture.Tests/ProductSfsTests.cs` 42, `Architecture.Tests/ProductRepositoryListTests.cs` 24, `Hosts.Tests/InsuranceCheckoutEndToEndTests.cs` 16, `Hosts.Tests/WorkerWriteFloorTests.cs` 12, `Hosts.Tests/ProductInsuranceFieldsRoundTripTests.cs` 12, `Products.Tests/DocumentPaidOnOrderPaidConsumerTests.cs` 8, `Architecture.Tests/WriteFloorTests.cs` 4, `Architecture.Tests/ReadFloorTests.cs` 4, `Products.Tests/ProductFilterDtoTests.cs` 2) — T8. `bash scripts/check-migration-lineage.sh` -> `Migration lineage gate OK`; `dotnet ef migrations has-pending-model-changes` -> `No changes have been made to the model since the last migration.`. DB: (ก) apply บน dev DB เดิมที่มีข้อมูล -> ผ่าน, premium 6 แถวคงค่าเดิม (`SUM(TotalPremium)=37339.71` = พิสูจน์ว่า `RenameColumn` ไม่ใช่ drop+add ที่ scaffolder แนะมา); (ข) **fresh replay จริง** `docker compose down -v && up` + bootstrap + `dotnet ef database update` รัน migration ทั้ง 27 ตัวผ่าน -> `INFORMATION_SCHEMA.COLUMNS` ให้ `shop.Products` = **33 คอลัมน์**, 8+4 ชื่อเก่าไม่เหลือเลย, ชื่อใหม่ครบ 5 (`TotalPremium decimal(19,2) NOT NULL`, `NetPremium/Stamp/TaxVat/CommissionAmount decimal(19,2) NULL`, `CommissionPercent decimal(19,6)`), index เหลือ `PK_Products` + `IX_Products_MerchantId_PaymentStatus` + unique `IX_Products_MerchantId_DocumentNo`; (ค) rollback `database update 20260730081227` -> 41 คอลัมน์ คืนครบ 12 ตัว + index `IX_Products_MerchantId_IsActive` กลับมา, แล้ว re-apply `Up()` กลับสภาพ 33 คอลัมน์ premium sum เดิม. viewports: n/a (persistence layer). deviations: (ก) ไม่ได้รัน `dotnet test tests/Hosts.Tests --filter ModelConsistencyTests` ของ Done clause — `Hosts.Tests` ยัง compile ไม่ได้ (40 error, T8); แทนด้วย `has-pending-model-changes` + fresh replay + column assertion ตรงจาก `INFORMATION_SCHEMA` ⇒ T8/T9 ต้องรันตัวจริงหลัง tests เขียว; (ข) scaffolder emit drop+add แทน rename 4 คอลัมน์ (EF เดา rename ไม่ได้) จึงแก้มือใน migration ใหม่เป็น `RenameColumn` — snapshot/designer ยังเป็นของ `dotnet ef` ทั้งหมด ไม่แตะ migration เก่า; (ค) `Down()` คืน "รูปร่าง" ครบ แต่ค่าใน 8 คอลัมน์ที่ drop คืนไม่ได้ (default `''`/`0`/`0001-01-01`) — ธรรมชาติของ rollback หลัง DROP COLUMN ไม่ใช่ข้อบกพร่องของ migration; (ง) dev DB ถูก `down -v` ใหม่ (ไม่มี seed-demo) ⇒ T6 ต้องรัน `seed-demo.sql` เองบน DB สดตัวนี้

- [x] 6. **T6 — seed demo + spec demo-seed-data** — `docker/bootstrap/seed-demo.sql:186-250`: ลบคอลัมน์ที่หายไปจาก INSERT ทั้ง 2 ก้อน, ลบ `CASE WHEN Seq % 7 = 0`, แถวที่เคย `IsActive = 0` เปลี่ยนเป็น `PaymentStatus = 'PAID'` + `PaidDate` มีค่า; แก้ `.ai/specs/demo-seed-data/requirements.md` REQ-5.4 + `tasks.md` (รวม verify query `COUNT(DISTINCT IsActive)` -> `PaymentStatus`)
     **Done** = รัน seed-demo ทั้งไฟล์ผ่านบน dev DB (`shop.Products = 100`) + query ยืนยันมีทั้งแถว UNPAID และ PAID; `bash scripts/spec-trace.sh demo-seed-data` ยังผ่าน
     Satisfies: REQ-2 (2.4), REQ-10 (10.5, 10.6)
     Evidence: `INSERT INTO shop.Products` ทั้ง 2 ก้อนเหลือ 10 คอลัมน์ตรงชุดใหม่ (`Id, MerchantId, ProductGroup, DocumentType, DocumentNo, SaleCode, ShowName, TotalPremium, PaymentStatus, PaidDate`) — ลบ `BranchCode`/`IsActive`/`CreatedAt`/`TotalPremiumCurrency` และ rename `TotalPremiumAmount`->`TotalPremium`; ค่าเงินเขียนเป็น 2 ตำแหน่ง (`1200.00` ฯลฯ) และ generator เปลี่ยน `CAST(... AS decimal(19,4))` -> `decimal(19,2)`; `CASE WHEN Seq % 7 = 0 THEN 0 ELSE 1 END` ของ `IsActive` หายไป แทนด้วย `CASE WHEN Seq % 7 = 0 THEN 'PAID' ELSE 'UNPAID' END` + `PaidDate` `DATEADD(day, -Seq, SYSUTCDATETIME())`; แถว hand-written 3 ตัวที่เคย `IsActive = 0` (`e9…0008`, `e9…0010`, `e9…0018`) เป็น `'PAID'` + `PaidDate` แล้ว. `./scripts/seed-demo.sh` บน dev DB :11433 (`pol-db`, DB สดที่ T5 `down -v` + migrate ไว้) -> `shop.Products = 100` ... `seed-demo: OK.` exit 0 และรันซ้ำครั้งที่ 2 ได้ผลเดิม exit 0 (idempotent). query จริงหลัง seed (stamp session context ก่อนเพราะ RLS): `PAID = 13 / UNPAID = 87` (รวม 100), `COUNT(DISTINCT PaymentStatus) = 2`, PAID ทุกแถวมี `PaidDate` (NullPaidDate = 0) และ UNPAID ทุกแถว `PaidDate` เป็น NULL, `WHERE TotalPremium <> ROUND(TotalPremium, 2)` -> **0 แถว**, และไม่มีแถว PAID ที่ถูกอ้างจาก `CartItems` ของ cart ที่ยัง `Open` (0 แถว — ไม่งั้น checkout demo จะ 409 ตลอด). spec `demo-seed-data`: REQ-5.4 เขียนใหม่เป็นแกน `PaymentStatus` + `TotalPremium DECIMAL(19,2)`, `design.md:174` และ `tasks.md` (scope T3 ข้อ 4, verify query ข้อ 6 `COUNT(DISTINCT IsActive)` -> `COUNT(DISTINCT PaymentStatus)`, ประโยค evidence 2 จุด) อัปเดตครบ -> `bash scripts/spec-trace.sh demo-seed-data` -> `OK: 'demo-seed-data' เกณฑ์ 33 ข้อ ถูกอ้างครบใน design.md และ tasks.md, EARS lint ผ่านทุกข้อ`. `docker/bootstrap/assert-fresh-db.sql` ตรวจแล้ว **ไม่ต้องแก้** (grep `Products|IsActive` ไม่พบเลย). viewports: n/a (SQL/spec). deviations: (ก) แก้ `.ai/specs/demo-seed-data/design.md:174-175` เพิ่มจากบรีฟ (บรรทัดนั้นระบุ `IsActive = 0` = 13 แถว ซึ่งจะค้างเป็นข้อมูลผิดถ้าไม่แก้); (ข) บล็อก Evidence ของ T3 ใน `tasks.md` เป็นบันทึกย้อนหลังลงวันที่ 2026-07-13 — เปลี่ยน query ให้รันซ้ำได้บนแกนใหม่ แต่ **ไม่** เขียนทับประวัติว่าเคยรัน query ตัวใหม่ ใส่เป็น note ลงวันที่ 2026-07-30 กำกับไว้; (ค) `design.md:165-172` ยังบรรยาย generator แบบ plan-line x tier + `Name`/`PriceAmount` ซึ่งค้างมาตั้งแต่ insurance-pivot (ก่อน spec นี้) — นอกขอบเขต T6 ไม่แตะ

- [x] 7. **T7 — cap 25 ทั้ง repo + SFS docs/spec + docs ที่อ้าง field ที่หายไป** — `SfsQueryParser.cs:28` clamp `[1, 25]`; `SfsOpenApi.cs:21` ข้อความ 100 -> 25; `docs/reference/search-filter-sort.md` (บรรทัด 121, 287, 320, 338, 1022, 1029-1033 = เพดาน; 585, 971, 1192, 1310 = ตัวอย่าง `Product.CreatedAt`); `.ai/specs/search-filter-sort/{requirements.md:42,tasks.md:83}`; `.ai/specs/checkout-chain-document-fields/*`, `.ai/specs/insurance-pivot/design.md:325`, `docs/reference/{entity-fields.md:934-935,platform-modules.md:658,675,src-structure.md:243}`
     **Done** = `grep -rn "IsActive\|CreatedAt" docs/reference .ai/specs` ไม่เหลือจุดที่อ้าง `Product` (allowlist master data/admin ที่มี `IsActive` จริงได้); `bash scripts/spec-trace.sh` ผ่านทุก spec ที่แก้; `dotnet test tests/Hosts.Tests --filter FullyQualifiedName~SfsQueryParserTests` เขียว
     Satisfies: REQ-4 (4.1, 4.2), REQ-11 (11.3)
     Evidence: cap 25 แก้ที่ `SfsQueryParser.ParsePaging` จุดเดียว (`private const int MaxLimit = 25` + `Math.Clamp(TryInt(query["limit"], 25), 1, MaxLimit)`) ⇒ มีผลทั้ง 7 endpoint เพราะ `Parse` เรียก `ParsePaging` ต่อ (ตาม T4); `SfsOpenApi.cs` ข้อความ "clamp ในช่วง 1 ถึง 100" -> 25 **จุดเดียว** เพราะ T4 refactor `AddPagingParameters` เป็น helper ร่วมของทั้ง `SfsQueryParamsMarker` และ `ProductQueryParamsMarker` แล้ว (บรีฟบอก 2 ที่). `rtk proxy dotnet build src/Hosts/Api` -> `Build succeeded. 0 Warning(s) 0 Error(s)`. `grep -rn "1, 100\|1 ถึง 100\|\[1\.\.100\]" src/ docs/ .ai/` -> ไม่เหลือ residue เชิงพฤติกรรมใน `src/`/`docs/` เลย; ที่เหลือ 6 hit เป็นข้อความที่ **ตั้งใจคง**: ประโยคที่บรรยายการเปลี่ยน (`products-sp-53-alignment/requirements.md:48` "แทน clamp เดิมที่ `[1, 100]`", `search-filter-sort/requirements.md:42` + `design.md:147` ที่เขียน "supersede `[1..100]` เดิม") และบันทึกประวัติ (`search-filter-sort/tasks.md:24`, `products-sp-53-alignment/{tasks.md:28,HANDOFF.md:519}`). docs/spec ที่แก้ตรง ๆ: `docs/reference/search-filter-sort.md` (เพดาน 6 จุด + ชื่อ/assert ของ test ตัวอย่าง `Parse_clamps_limit_to_100` -> `_to_25` + ตัวอย่าง default-sort fallback เปลี่ยนจาก `Product.CreatedAt` เป็น `Admins.Domain.Users.User` 3 จุด + ลบ `ProductSfs` ออกจากรายการ as-built §13 + แก้รายการ endpoint ที่มี SFS), `docs/reference/entity-fields.md` (เขียนตาราง `shop.Products` ใหม่ทั้งตารางเป็น 33 คอลัมน์จริงตามผล fresh-replay ของ T5 + block 4 หัวข้อ), `docs/reference/platform-modules.md` (§5 บทบาท/ฟีเจอร์/สถานะ + ข้อ 11 permission `product.update` + §8 order item snapshot), `docs/reference/src-structure.md` (§4.1 ตารางไฟล์ Products + snapshot ของ `Items/Item.cs` 2 จุด + เติม 4 migration ล่าสุดในตาราง lineage), `.ai/specs/demo-seed-data/design.md:165-172` (generator เลิกเป็น plan-line x tier ตามของจริง). docs/spec ที่เลือก **กำกับหมายเหตุแทนการเขียนย้อนอดีต** (spec ปิดแล้ว = บันทึกประวัติ): `.ai/specs/checkout-chain-document-fields/{requirements.md:50 (REQ-7.2),design.md:74,design.md:85,tasks.md:34}`, `.ai/specs/insurance-pivot/design.md:325`, `.ai/specs/search-filter-sort/{requirements.md:42 (แก้ค่าเป็น 25 + ระบุว่า supersede),tasks.md:24,tasks.md:83 (task 5 — เติมหมายเหตุ supersede หน้าบล็อก เพราะนิยาม `ProductListItem(… bool IsActive, DateTime CreatedAt)` ในนั้นทำให้คนอ่านเข้าใจผิดว่าเป็นสเปกปัจจุบัน)}`, และ `docs/reference/search-filter-sort.md` §12.2 (worked example ทั้งหัวข้อ + banner ที่หัวไฟล์ — ไม่ rewrite ตัวอย่างทั้งก้อนเพราะยังสอน pattern ได้ แค่ประกาศชัดว่าเป็นตัวอย่างเชิงสมมติ). `bash scripts/spec-trace.sh` ผ่าน **ทั้ง 5 spec** โดยเห็นคำว่า `OK:` จริง: products-sp-53-alignment (53 ข้อ), search-filter-sort (59), checkout-chain-document-fields (21), insurance-pivot (31), demo-seed-data (33). viewports 375/768/1440: n/a (ไม่มี UI). deviations: (ก) ไม่รัน `dotnet test tests/Hosts.Tests --filter SfsQueryParserTests` ตาม Done clause — `tests/` เป็นของ T8 และ **test นี้จะแดงเพราะ T7 โดยตั้งใจ**: `tests/Hosts.Tests/SfsQueryParserTests.cs` theory `Limit_is_clamped_into_1_to_100` (`:50`) เคส `[InlineData("1000", 100)]`, `[InlineData("100", 100)]`, `[InlineData("50", 50)]` ต้องเปลี่ยน expected เป็น 25 + rename เป็น `Limit_is_clamped_into_1_to_25` (เคส `"1"`/`"0"`/`"-5"`/`"abc"` ไม่กระทบ); `SfsOpenApiTests.cs` assert แค่ชื่อ parameter ไม่ assert description จึงไม่แดง; (ข) แก้เกินบรีฟ 4 จุดที่เป็นข้อมูลผิดชัด ๆ ในไฟล์เดียวกัน: ตาราง `shop.Products` ใน `entity-fields.md` (บรีฟชี้แค่ `:934-935` แต่ทั้งตารางเป็นคอลัมน์ยุคก่อน insurance-pivot), รายการ as-built §13 + รายการ endpoint ที่รองรับ SFS ใน `search-filter-sort.md` (อ้าง `ProductSfs.cs` ที่ถูกลบแล้วว่ายังมีอยู่), snapshot field ของ `Items/Item.cs` ใน `src-structure.md`/§8 ของ `platform-modules.md` (ยังเขียน `SumInsured`/`CoverageDurationDays`/`Insurer` ซึ่ง `checkout-chain-document-fields` เปลี่ยนไปแล้ว), และตาราง migration lineage ใน `src-structure.md` (ค้างที่ policy-reference-record); (ค) **ไม่แก้** `docs/reference/entity-fields.md:1050-1055` และ `:1111-1116` (ตาราง `shop.CheckoutSessionItems`/`shop.OrderItems` ยังลิสต์คอลัมน์ `SumInsuredAmount`/`SumInsuredCurrency`/`InsurerName`/`CoverageDurationDays` ที่ `checkout-chain-document-fields` เปลี่ยนไปแล้ว) — drift ของ spec ก่อนหน้า ไม่ใช่ field ที่ REQ-11.3 ระบุ และต้องยืนยันคอลัมน์จริงจาก EF config ของ 2 ตารางนั้นก่อนเขียน ⇒ ยกให้ T9/lead ตัดสิน

- [x] 8. **T8 — tests ทั้งชุด -> build + test เขียว** — แก้/เขียนใหม่ตามตาราง "Tests" ใน design.md: ลบ `Architecture.Tests/ProductSfsTests.cs` + เคส `Product_TotalPremium_...` ใน `MoneyColumnMappingTests.cs`, เขียนใหม่ `ProductRepositoryListTests.cs` (fake `IClock`: SaleCode narrowing, default UNPAID, ทะเบียนรถไม่ match FIRE/MISC, window 6 เดือน, RENEWAL `EndDate` 2 เดือน, order `DocumentNo`), `Products.Tests/{ProductTests,ProductFilterDtoTests,DocumentPaidOnOrderPaidConsumerTests}.cs`, `Hosts.Tests/{InsuranceCheckoutEndToEndTests,WorkerWriteFloorTests,ProductInsuranceFieldsRoundTripTests,SfsQueryParserTests,SfsOpenApiTests}.cs` + ทุกเทสที่ยิง `GET /api/v1/products` ต้องส่ง `productFilters` ที่มี `saleCode`
     **Done** = `dotnet build pol-core.slnx -warnaserror` 0/0; `dotnet test` ทุก non-integration suite เขียว; ไม่มี `.only`/`.skip` ค้าง
     Satisfies: REQ-12 (12.2)
     Evidence: `rtk proxy dotnet build pol-core.slnx` -> `Build succeeded. 0 Error(s)` ทั้ง repo (192 error CS ใน `tests/` 10 ไฟล์ที่ T5 ส่งต่อ = 0). `dotnet test` รันจริงทุก non-integration suite เขียวหมด: `tests/Products.Tests` **53 passed / 0 failed / 0 skipped**, `tests/Hosts.Tests` **362 / 0 / 0**, `tests/Architecture.Tests` (เต็มชุด) **225 / 0 / 0**, และ suite ที่เหลือ Admins 95, BuildingBlocks 43, Carts 15, Checkouts 13, Divisions 6, Iam 62, Levels 6, Merchants 120, Offices 6, Orders 76, Payments 162, Positions 6, SharedKernel 46 (ทุกตัว 0 failed / 0 skipped; `tests/{Identity,Producer,Tenant}.Tests` เป็น bin/obj ค้าง ไม่มี csproj). เพิ่มด้วย `set -a && . ./.env.integration && set +a && dotnet test tests/Integration.Tests` -> **47 / 0 / 0** บน dev DB :11433. ลบ `tests/Architecture.Tests/ProductSfsTests.cs` ทั้งไฟล์ (`git rm`) ตามไฟล์ที่มันทดสอบ. เคสใหม่ที่คุมสเปกนี้: `ProductTests` guard scale (`100.005`/`100.001` throw, `100.5`/`100.50`/`100` ผ่าน) + negative TotalPremium/breakdown + `SaleCode` > 20 (REQ-1.5) และคง invariant เดิมครบ (`TotalPremium <= 0`, `StartDate > EndDate`, `Enum.IsDefined` x2, CMI+APPLICATION, trim/normalise); `ProductFilterDtoTests` 15 เคสของ REQ-3/REQ-9 (50005 x4 absent/blank/whitespace/literal-null + missing/blank SaleCode, 50007 x3 `unpaid`/`NOPE`/`""`, 50003/50008/50009, MaxLength 21 vs 20, absent=UNPAID, `ALL`=null, `PAID`/`UNPAID`, trim `"  S1  "`, `branchCode` ถูก ignore เงียบ ๆ ตาม REQ-3.4); `ProductRepositoryListTests` เขียนใหม่ด้วย fake `IClock` ตรึง `today` = 2026-07-30 (REQ-6.3) — SaleCode narrowing, merchant floor, default UNPAID / `ALL` / `PAID`, **search window 11 เคส A-K ตามตาราง HANDOFF** (ขอบล่าง inclusive, ไม่มีขอบบน, NULL ไม่เข้า, RENEWAL half-open บน `EndDate`), window ยัง AND แม้ client กรองอย่างอื่น, **Motor gate 4 เคส** (ทะเบียนเดียวกัน CMI/VMI match, FIRE/MISC ไม่ match) + non-Motor ยังค้น `DocumentNo` ได้, escape wildcard, InsuredName partial, `OrderBy(DocumentNo)` + paging หน้า 1/2 + total หลัง filter, projection 32 field ครบทุกช่อง + `InsuranceType` computed; `MoneyColumnMappingTests` เคส `Product_TotalPremium_maps_to_decimal_19_4_and_char3` ถูกแทนด้วย `Product_owns_no_Money_complex_property` (assert `GetComplexProperties()` ว่าง + `TotalPremium` เป็นคอลัมน์ `decimal(19,2)`) ⇒ ไม่ใช่แค่ลบเคสให้ผ่าน; `DocumentPaidOnOrderPaidConsumerTests` `MarkPaid` เลิก assert `IsActive` เปลี่ยนเป็น `PaidDate` + fake repo เลิก implement `ListByTenantAsync`; `SfsQueryParserTests` `Limit_is_clamped_into_1_to_25` (`1000`/`100`/`50`/`25` -> 25, `1`/`0`/`-5` -> 1, `abc` -> 25) ตาม REQ-4.1; `SfsOpenApiTests` เพิ่ม `Products_get_declares_only_the_paging_and_productFilters_parameters` (REQ-7.4: มีแค่ `page`/`limit`/`productFilters`, ไม่มี `filters`/`sort`/`search`) และรวม 2 fact เดิมเป็น theory ครอบ **ทั้ง 4 endpoint** ที่ถือ `SfsQueryParamsMarker` จริงในโค้ด (`/api/v1/admins/roles`, `/api/v1/admins`, `/api/v1/reports/policies`, `/api/v1/admins/reports/policies`) ว่ายังประกาศครบ 5 ตัว; `InsuranceCheckoutEndToEndTests` mint currency ที่ cart boundary (`Money.Of(product.TotalPremium, "THB")`, REQ-8.4) + เคส `Paid_order_marks_the_product_PAID_so_it_can_no_longer_be_sold` (PAID, `GetProductByIdQuery` คืนค่าที่ gate อ่าน != UNPAID, และหลุดจาก default list) ; `WorkerWriteFloorTests`/`ProductInsuranceFieldsRoundTripTests` ใช้ `ProductInput`/`ProductRepository(db, IClock)`/`CreateProductHandler(repo, uow)` signature ใหม่ + fixed clock. `bash scripts/check-rename-identifiers.sh` -> `OK — no retired identifier appears as a live-code token in src/ or tests/`; ไม่มี `.only`/`.skip`/`Skip =` ใน `tests/` (grep) และไม่มี `Assert.True(true)`. viewports 375/768/1440: n/a (ไม่มี UI). deviations: (ก) **ไม่ได้ assert HTTP 400/409 ของ gate จริง** — gate ทั้งสองอยู่ใน minimal-API lambda ของ `Program.cs` เรียกเป็น unit ไม่ได้ และการ boot host ให้ต่อ SQLite ต้อง swap DbContext registration ทั้งชุด; แทนด้วยการ assert ค่าที่ gate อ่าน (`PaymentStatus != UNPAID` ผ่าน `GetProductByIdQuery` เดิม) + เอกสารหลุดจาก default list ⇒ status code จริงเหลือให้ T9 ตาม REQ-12.6; (ข) `ProductInsuranceFieldsRoundTripTests` เรียก handler ตรง ไม่ผ่าน HTTP อยู่แล้ว จึงไม่มี JSON payload ของ `POST /products` ให้แก้เป็นตัวเลขเปล่า (ไม่มีเทสไหนใน repo ยิง `POST /api/v1/products` จริง — grep ยืนยัน) เปลี่ยนเป็น `decimal` ที่ระดับ `ProductInput` แทน; (ค) เคสที่ HANDOFF T7 บอกว่าต้องแก้ 3 ตัวใน `SfsQueryParserTests` แก้จริง 3 ตัว + เพิ่มเคสขอบ `("25", 25)`; (ง) `SystemClock` ถูกแทนด้วย fixed clock ใน 3 ไฟล์ของ Hosts.Tests เพราะ search window ใหม่ทำให้ผลขึ้นกับเวลาจริง (เอกสารที่ seed ไว้จะหลุด window เองในอีก 6 เดือน) — เป็นการกัน flake ในอนาคต ไม่ใช่การอ่อนข้อ assert

- [x] 9. **T9 — full gate + PR** — `dotnet build pol-core.slnx -warnaserror`; `dotnet test` ทุก suite + `source .env.integration && dotnet test tests/Integration.Tests`; `bash scripts/check-rename-identifiers.sh`; `bash scripts/check-migration-lineage.sh`; `bash scripts/spec-trace.sh products-sp-53-alignment`; `docker compose down -v && up` -> migrate + seed แล้วตรวจ `INFORMATION_SCHEMA.COLUMNS` ของ `shop.Products`; E2E ตามหัวข้อ Verification ข้อ 6 บน dev :11433; `unset GH_TOKEN` แล้ว push branch + เปิด PR เข้า `develop` (ห้าม merge)
     **Done** = ทุก gate เขียว + PR เปิดแล้ว + PR body สรุปขอบเขต §5.2/§2/§6 + deviation ที่จงใจคง
     Satisfies: REQ-12 (12.1, 12.3, 12.4, 12.5, 12.6)
     Evidence: รัน gate ทุกตัวที่ `.github/workflows/ci.yml` ใช้ (อ่านไฟล์ CI จริง ไม่เดา) — `dotnet build pol-core.slnx -warnaserror` ตามตัวอักษรของ REQ-12.1 -> `Build succeeded. 0 Warning(s) 0 Error(s)`; `dotnet test pol-core.slnx --filter "Category!=Integration"` -> **1296 passed / 0 failed / 0 skipped** ครบ 16 project (Hosts 362, Architecture 225, Payments 162, Merchants 120, Admins 95, Orders 76, Iam 62, Products 53, SharedKernel 46, BuildingBlocks 43, Carts 15, Checkouts 13, Divisions/Levels/Offices/Positions 6 ตัวละ); `. ./.env.integration && dotnet test --filter "Category=Integration"` -> **47 / 0 / 0**; guard regression suite ครบ 6 ไฟล์ใต้ `.claude/hooks/tests/` -> **288 pass / 0 fail** (codex-adapters 7, destructive-guard 132, gate-task 23, hook-bypass-guard 84, secrets-guard 29, spec-edit-guard 13); `SECRET_GUARD_SKIP='' .ai/bin/check-secrets.sh --all` -> exit 0 ไม่มี finding; `scripts/check-rename-identifiers.sh` -> `OK — no retired identifier appears as a live-code token in src/ or tests/`; `scripts/check-migration-lineage.sh` -> `Migration lineage gate OK`; spec-trace **ทุก spec ใน `.ai/specs/` (25 ตัว)** เห็นบรรทัด `OK:` จริงทุกตัว รวม 5 ตัวที่ T7 แตะ (`products-sp-53-alignment` 53 ข้อ, `search-filter-sort` 59, `checkout-chain-document-fields` 21, `insurance-pivot` 31, `demo-seed-data` 33); docker CI job: build image `api` + `migrate` สำเร็จทั้งคู่ (exit 0) และ `docker compose -f docker-compose.prod.yml config` render ผ่านด้วย placeholder env ครบ 9 ตัว. REQ-12.5 fresh replay จริง: `docker compose down -v && up -d` -> `pol-db-init` exit 0 -> `dotnet ef database update` apply ครบ 27 migration -> `./scripts/seed-demo.sh` -> `seed-demo: OK.` แล้ว `INFORMATION_SCHEMA.COLUMNS` บน `shop.Products` = **33 คอลัมน์ตรง §5.2 เป๊ะ** (`TotalPremium/NetPremium/Stamp/TaxVat/CommissionAmount` = `decimal(19,2)`, `CommissionPercent` = `decimal(19,6)`, `PaymentStatus varchar(10) NOT NULL`, `PaidDate datetime2 NULL`), query `COLUMN_NAME IN (...)` ของ 8 ตัวที่ DROP + 4 ชื่อเก่า `*Amount` -> `none`, index เหลือ **3** ตัว (`PK_Products`, `IX_Products_MerchantId_PaymentStatus`, unique `IX_Products_MerchantId_DocumentNo`; `IX_Products_MerchantId_IsActive` หายไปแล้ว) + `docker/bootstrap/assert-fresh-db.sql` -> `OK`. REQ-12.6 E2E ยิงจริงบน host dev (mint merchant-user session ลง `merch.Sessions` ตรง ๆ แล้วส่ง cookie `mch_session` — ไม่ต้องเดิน OIDC): gate ทั้งสองพิสูจน์ครบ — add-to-cart เอกสาร PAID -> **400 `"Unknown or inactive product."`** ตรงตัว, checkout start ที่มี cart line กลายเป็น PAID -> **409 `"A cart product is no longer available."`** ตรงตัว, และ control ที่ทุก line ยัง UNPAID -> **200** (คืน `checkoutSessionId`); `POST /api/v1/products` wire ใหม่ (premium ตัวเลขเปล่า) -> **200** 8 เอกสาร, premium 3 ตำแหน่ง (`100.005`) -> **400** ตาม guard REQ-1.5, ส่ง premium เป็น object แบบเดิม `{"amount":..,"currency":".."}` -> **ถูกปฏิเสธจริง (breaking change เกิดจริง) แต่ได้ 500 ไม่ใช่ 400** เพราะ body-binding โยน `BadHttpRequestException` ที่ `ProblemDetailsExceptionHandler` ไม่ได้ map (ตกลง `_ => 500`) — bug เดิมทั้ง repo ไม่ใช่ของสเปกนี้ (ยืนยันว่า `POST /carts/{id}/items` body เพี้ยนก็ 500 เหมือนกัน) ⇒ บันทึกเป็น follow-up; `GET /api/v1/products` ครบเช็คลิสต์: ไม่ส่ง `productFilters` -> 400, `saleCode` เว้นวรรค -> 400 (50005), `paymentStatus` `"XXX"`/`"unpaid"` -> 400 (50007, case-sensitive), default -> UNPAID เท่านั้น + `StartDate` ย้อนหลังไม่เกิน 6 เดือน (ขอบล่างวันพอดีเข้า, เก่ากว่านั้นไม่เข้า) + เรียงตาม `DocumentNo`, `"ALL"` -> UNPAID+PAID, `"PAID"` -> PAID เท่านั้น, `documentType:"RENEWAL"` -> คัดด้วย `EndDate` window 2 เดือน (หมดอายุแล้วหลุด), `searchText` = ทะเบียนรถ -> match แถว VMI แต่ **ไม่** match แถว MISC ที่ทะเบียนเดียวกัน (Motor gate per-row), `branchCode` ใน JSON ถูก ignore เงียบ ๆ ไม่ใช่ 400, `limit=1000`/`50` -> response `limit` = **25** ทั้งคู่ (`limit=2&page=2` ยังทำงาน), `filters=`/`sort=` ไม่มีผลต่อผลลัพธ์และ `/openapi/v1.json` ประกาศเฉพาะ `page`/`limit`/`productFilters`, response item = **33 key** (32 field §5.2 + `id`) + `insuranceType` computed = `Motor` และไม่มี `isActive`/`createdAt`/`branchCode`/`merchantId` หลุดออก wire. deviations/ข้อจำกัดที่บันทึกตรง ๆ: (ก) host รันบน **:5177 ไม่ใช่ :5100** เพราะมี process `Api` ของคนอื่นถือ :5100 อยู่ (ไม่ kill process ที่ไม่ใช่ของตัวเอง) — port ไม่กระทบผลทดสอบ; (ข) **control ของ gate 1 (add UNPAID เข้า cart) ยิงไม่ผ่าน** เพราะ cart write path พังด้วย `DbUpdateConcurrencyException` -> 409 ทุกครั้งบน SQL Server จริง — เป็น bug เดิมที่บันทึกไว้แล้วตั้งแต่ `rls-to-query-filter`/`insurance-pivot` (ไม่มีไฟล์ Carts ใน diff ของ branch นี้เลย) ⇒ พิสูจน์ฝั่ง UNPAID ด้วย checkout-start 200 ที่เดินผ่าน gate เดียวกันแทน; (ค) seed demo ไม่ใส่ `StartDate`/`EndDate` (NULL ทั้ง 100 แถว) ⇒ ทุกแถว demo หลุด search window ของ REQ-6 ทำให้ `GET /products` บน demo data คืน 0 แถว — E2E จึงสร้างเอกสารมีวันที่เองผ่าน `POST /products` แล้วลบทิ้งหลังเสร็จ (`t9_left=0`); เป็นช่องว่างของ spec `demo-seed-data` ที่ควรตามเก็บแยก
