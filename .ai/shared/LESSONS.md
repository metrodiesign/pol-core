> Canonical source for ALL agents (Claude loads via .claude/rules stub; Codex/OpenCode/Pi read directly).
> แก้ที่นี่ที่เดียว — single source of truth.

# Lessons (promoted from retrospectives)

> Universal process / workflow / tooling lessons. Loaded for Claude via the always-on
> `.claude/rules/lessons.md` stub (every turn's prefix); Codex/OpenCode/Pi read this
> canonical file at session start per their adapter.
> Keep lean, prune stale. Stack-specific lessons live under the `.ai/shared/stack/`
> extension point (path-scoped: a stack file loads only when reading matching files).
> Full record in `retrospectives/`.

- **Pattern**: pure-logic-first — แยก logic ที่ทดสอบได้ (สูตร/validation) เป็น pure function ในไดเรกทอรีของโปรเจกต์ (co-located กับ logic ที่ทดสอบ) แล้ว unit test เขียวก่อนแตะ UI. — **Why**: ความถูกต้องไม่ปนกับ rendering, ปิด acceptance ที่เป็นตัวเลขก่อน wire UI.
- **Pattern**: /spec-analyze ก่อน design คุ้มเสมอสำหรับฟีเจอร์ที่มี logic — จับ conflict/gap ที่ทำให้ test เขียนไม่ได้ (หน่วย/วิธีปัดเศษ, trigger คำนวณ, empty state, requirement ที่ขัดกันเอง). — **Why**: แก้ที่ requirements ถูกกว่าแก้โค้ดทีหลัง.
- **Pattern**: audit vuln แยก **dev-only chain vs prod-core** ก่อนตัดสิน (ใช้คำสั่งของ package manager ที่ชี้ว่า dep ตัวไหนดึงมันเข้ามา) -> vuln ที่อยู่ใน dev/test chain ล้วน (ไม่ได้ bundle เข้า prod) แก้สะอาดด้วยการ bump major ของ dev tool นั้น; vuln ใน core/transitive ของ prod ให้ flag + ระบุเงื่อนไข resolve, ห้ามรัน auto-fix แบบ force. — **Why**: auto-fix แบบ force มัก downgrade core dep เป็น breaking — เลวกว่าตัว vuln; เหมารวมทุก vuln เป็น "ต้อง fix" จะ downgrade core ทั้งที่ตัวจริงเป็น dev dependency แก้ได้ด้วย bump major. core ที่ยังไม่มี fix ต้องรอ upstream bump. กลไกใน CI: gate เฉพาะ prod attack surface (audit ที่ละ dev dependency, ระดับ high ขึ้นไป) แล้วเปิด tracking issue สำหรับ dev vuln แยก — อย่า gate audit รวม dev dependency ทั้งหมด มันจะแดงทุก PR จาก dev dependency เก่า.
- **Pattern**: รัน long-running server ของโปรเจกต์ผ่าน `rtk proxy <start-command>` เพื่อเห็น raw log ("Ready"/error). — **Why**: rtk hook rewrite output เป็น summary ("Errors: 1 | Warnings: 0") บดบัง log จริง -> เข้าใจผิดว่า server พังทั้งที่ขึ้น 200.
- **Pattern**: cost จริงอ่านจาก ledger `.cost.total_cost_usd` เท่านั้น ห้าม recompute จาก transcript (overcount 1.6-3.7x) — รายละเอียด dedup/การปันส่วน/ข้อจำกัด ledger: `.claude/skills/spec-retro/references/cost-accounting.md`. — **Why**: ระบบคำนวณ cost ให้แล้วผ่าน statusline; recompute เองได้เลขผิดเสมอ.
- **Pattern**: headless `claude -p` buffer output จนงานจบ — pane ว่าง/CPU ต่ำ ≠ ค้าง. ก่อนฆ่า ตรวจ progress บน disk (`git status`, ไฟล์ใหม่, dependency ที่ install แล้ว) ไม่ใช่ดู pane. และ `--permission-mode acceptEdits` ครอบแค่ Edit ไม่ครอบ Bash -> คำสั่ง install/scaffold ติด permission เงียบใน headless (ต้อง `--dangerously-skip-permissions` หรือ `--allowedTools`). — **Why**: เคยฆ่า scaffold ที่กำลังทำงานเพราะ pane ว่าง.
- **Pattern**: หลัง command fail อ่าน output ให้จบก่อนยิงใหม่; แก้ script แล้ว **Read กลับยืนยันการแก้ (โดยเฉพาะ `cd`/path)** ก่อนรัน. error เดิมซ้ำ 2 ครั้ง = หยุด. — **Why**: ลืม `cd` ใน resume-pane ซ้ำ 4 รอบเพราะ re-fire background blind ไม่อ่านผลรอบก่อน + ประกาศ "แก้แล้ว" โดยไม่ได้แก้บรรทัดจริง.
- **Pattern**: `tasks.md` checkbox ขัดกับ git log (commit อ้าง task เสร็จ แต่ checkbox ยัง `[ ]`) เชื่อ **filesystem** เป็น ground truth ก่อนเริ่ม — list ไดเรกทอรี source / build manifest ของโปรเจกต์ดูว่ามี artifact จริงไหม. retrospective-only commit (docs ไม่มีโค้ดคู่) ทำให้ branch ดูคืบหน้าทั้งที่ว่าง. — **Why**: เกือบข้าม task 1 (scaffold) เพราะ log มี commit "task 2-9" แต่ source dir ไม่มีจริง; เชื่อ log = report ผิด/skip บนฐานที่ไม่มี.
- **Pattern**: `git diff --stat` / `--name-only` **ไม่เห็นไฟล์ใน path ที่ยัง untracked** (เคสจริง: source folder ทั้งโฟลเดอร์เป็น `??`) -> retro/handoff ที่ gather "Files Modified" จาก git stat อย่างเดียวจะ **ตกหล่นโค้ดจริง** (เห็นแต่ spec/docs ที่ tracked). เช็ก `git status` (เห็น `??`) ควบคู่ + จำจาก session เอง. — **Why**: รายงานว่า "ไม่มีโค้ดเปลี่ยน" ทั้งที่แก้ component จริง เพราะ stat เงียบเรื่อง untracked.
- **Pattern**: "assemble" task เป็นจุดจับ gap ของ requirement ระดับ section — ก่อนประกอบ cross-check **ทุก section ใน requirement ที่ list ลำดับ/องค์ประกอบ เทียบ component ที่มีจริง + anchor ใน nav data**. เคสจริง: section หนึ่ง (มี REQ ของตัวเอง) ไม่ถูก task ย่อยใด list เลย แต่ requirement ลำดับหน้าต้องมี + nav data มี anchor ของมัน -> assembly เป็นที่เดียวที่จับได้. component ที่ขาด = สร้างเป็น prerequisite ของ assembly (blocker ของ requirement ลำดับหน้า ไม่ใช่ scope creep) แล้ว **flag ดัง + บันทึก gap-fill ใน tasks.md** ไม่แก้เงียบ. — **Why**: decomposition ที่แบ่ง task ตาม verify-modality (typecheck-only vs interactive ใน browser) ทำ section ตกร่อง (section ที่ verify ด้วย typecheck ล้วน แต่ไม่อยู่ใน list ของ task นั้น); ไม่ cross-check ลำดับหน้า = ส่งหน้าขาด section ทั้งที่ทุก task ย่อยผ่านในขอบเขตตัวเอง.
- **Discovery**: multi-session แพงกว่า all-in-one ~30-40% สำหรับ coupled feature — แยก session เพื่อ accuracy เท่านั้น (conscious trade ไม่ใช่ cost win); kernel การตัดสินใจอยู่ที่ `.claude/commands/pane-loop.md` + spec-tasks/SKILL.md, ตัวเลข/วิธีคิด cost ของ multi run: `.claude/skills/spec-retro/references/cost-accounting.md`. — **Why**: เคยเชื่อกลับด้าน (แยก=ถูกกว่า) → default ผิด.
- **Pattern**: PreToolUse hook block (exit 2) ฆ่า compound command **ทั้งก้อน** — setup ที่มัดมาในก้อนเดียวกัน (chmod, mkdir, cp) ไม่รันเงียบๆ. แยก setup ออกจากคำสั่งที่เสี่ยงโดน block, หลังโดน block ให้ไล่เช็กว่าส่วนไหนของก้อนยังไม่ได้รัน, และก่อน commit ตรวจ mode ใน index (`git ls-files -s`) ของ script ทุกตัว. เคสจริง: `hook-bypass-guard.sh` เข้า commit เป็น 100644 เพราะ `chmod +x` อยู่ก้อนเดียวกับ test ที่ destructive-guard block — เจอตอน `git log --stat` แก้ด้วย amend ก่อน push. หมายเหตุประกอบ: hook ที่เพิ่ม/แก้ใน settings.json มีผล**ทันทีกลาง session** (ไม่ใช่ snapshot ตอนเริ่ม) — เหยื่อรายแรกของ guard ใหม่อาจเป็นคำสั่งทดสอบของตัวเอง; test harness ที่มี destructive string ใน argument ให้เขียนเป็นไฟล์ /tmp แล้วรัน ไม่ใช่ inline. — **Why**: เชื่อว่า chmod รันแล้วเพราะอยู่ในคำสั่งที่ "ส่งไปแล้ว" ทั้งที่ hook ตัดก่อน execute → ไฟล์ mode ผิดหลุดเข้า commit.
- **Pattern**: guard/regex hook เขียนเสร็จห้ามเชื่อจนกว่าผ่าน adversarial test เป็นไฟล์ test suite (bypass case + false-positive case ด้วยเคสจริง รันผ่าน stdin JSON) — regex ที่ผ่านตา+ผ่าน test ชุดที่ผู้เขียนนึกออก ยังโดน fresh-context reviewer เจาะได้ 8 ทาง (indented rm, /bin/rm, xargs, git clean แยก flag, combined short flag -nm, hooksPath override) และ false positive ข้ามคำสั่ง (grep -r ตามด้วย rm -f โดน AND ของ flag คนละคำสั่ง). — **Why**: ผู้เขียน test เฉพาะเคสที่ตัวเองนึกออก; reviewer ที่ถูกสั่งให้ "craft bypass แล้วรันจริง" จับ failure mode ที่ผู้เขียนมองไม่เห็นเสมอ.
- **Pattern**: guard ที่มอง command เป็น flat string = fail-open หลายทาง (`\rm`, `"rm"`, `sh -c 'rm -rf'`, `eval 'rm -rf'`, `HEAD:refs/heads/main` ที่ `/` นำหน้า branch) — normalize ลงสำเนา detection-only ที่ไม่ exec (strip backslash/quote) ก่อนหา POS anchor; block rule ที่ชนชื่อคำสั่งคนละโดเมน (SQL `TRUNCATE` vs coreutil `truncate -s`) ต้อง scope ด้วย next-token (ตามด้วย non-dash = SQL); จับ argv ทั้งหมด (`$*`) ไม่ใช่แค่ `$1`. ทุก block rule คู่กับ benign baseline ที่ต้องผ่าน. — **Why**: regex ที่ดูครบยังรอด escape idiom และ over-block coreutil/everyday code; ไม่มี benign baseline = ไม่เห็น false-positive ที่ขวางงานจริง.
- **Pattern**: hardening guard อาจ block การใช้งานชอบธรรมของตัวเอง — tamper-detection (`#9` ห้ามแก้ guard) block คำสั่ง read-only `git config core.hooksPath` (query เปล่า ไม่มี value) ตอน recon เอง; secret-scanner block test fixtures ของตัวเอง (Stripe/`ghp_` sample) จน commit ไม่ได้. แก้: tamper-detect เฉพาะ **write** (มี value / `--unset` / redirect) ไม่ใช่ read; scanner exclude fixtures dir (`.claude/hooks/tests/`); doc-example secret ใช้ค่า placeholder (`<password>` ที่เข้า `<.*>` rule) ให้ทั้งอ่านรู้เรื่องและไม่โดน flag. — **Why**: guard ที่ดักกว้างเกินกลายเป็น false-positive ที่ขวางแม้คนเขียน guard เอง — เจอตอน recon โดน block + commit fixtures โดน scan ตัวเอง.
- **Pattern**: multi-agent edit หลายไฟล์พร้อมกัน → partition ตาม **file-owner** (1 agent = ชุดไฟล์ disjoint ไม่ทับกัน) กัน write-conflict โดยไม่ต้อง worktree; แต่ agent ที่อ่าน shared tree กลางคันเห็น edit ที่ยังไม่เสร็จของกลุ่มอื่น → per-agent test count มั่ว. เชื่อ integration verify ที่รัน **หลังทุกกลุ่ม land** เท่านั้น (fullsuite ที่รันก่อน repair = stale). — **Why**: builder verify รายงาน test แดงจาก edit ครึ่งทางของกลุ่มอื่น ไม่ใช่ bug ตัวเอง; ตัดสินจาก snapshot กลางคัน = ตื่นตูม/พลาดของจริง.
- **Pattern**: ผล Workflow/subagent review เชื่อไม่ได้ถ้ามี `<failures>` — verifier ที่ตายกลางทาง (spend-limit / terminal error) คืน verdict null, partition แบบ `f.verdict?.isReal` ตีเป็น falsy แล้วตกเข้า `rejected` เงียบๆ ปนกับ finding ที่ถูก refute จริง. ก่อนเชื่อ rejected/confirmed: นับ `<failures>` เทียบจำนวน verifier ที่ควรรัน; verifier ไหนตาย ให้ re-verify finding นั้นด้วยมือกับไฟล์จริง อย่าถือ rejected = ไม่ใช่ปัญหา. ออกแบบ harness ให้ verdict ที่หายไป = "ยังไม่ตัดสิน" (ไม่ใช่ rejected) จะปลอดภัยกว่า. — **Why**: เคส Tier 4 review verifier 16/17 ตาย แต่ผลส่ง `confirmed:1, rejected:16` ดูเหมือนผ่านเกือบหมด ทั้งที่ rejected ส่วนใหญ่ไม่เคยถูกตรวจ — ถ้าเชื่อตามจะ ship 7 bug จริง.
- **Pattern**: flip `tasks.md` checkbox เป็น `[x]` + `Evidence:` block + bullet จริง ≥1 ต้องอยู่ **ใน edit ก้อนเดียวกัน** (old_string คร่อมตั้งแต่บรรทัด checkbox ถึง Evidence bullet แรก). task-gate hook (PostToolUse) ส่งเฉพาะ `new_string` ของ edit นั้นให้ engine แล้ว scope Evidence **per flipped `[x]` ในเนื้อ hunk** — flip checkbox กับ append Evidence คนละ edit = hunk ที่มี `[x]` ไม่มี Evidence → block เสมอ; pre-append Evidence ก่อนก็ไม่ช่วยเพราะ hunk ของ flip เป็นบรรทัดเดียว. — **Why**: เสีย 3-4 รอบ churn (revert→combined edit) ต่อ task เพราะ split edit; ทำ edit เดียวคร่อม checkbox→bullet ผ่านรอบแรก.
- **Pattern**: CI "Spec trace" loop **ทุก** spec ใต้ `.ai/specs/*/` ที่มี `requirements.md` (bugfix spec ไม่มี → skip) แล้วเก็บ fail รวม (`|| status=1`) exit 1 ท้าย — spec ที่ PR **ไม่ได้แตะ** ก็ทำ PR แดงได้. spec-trace แดง: รัน loop ทั้งชุด local หา culprit อย่าเดาว่าเป็น spec ที่เพิ่งแก้; เพิ่ม/แก้ criterion ใน `requirements.md` ต้องอ้างใน `design.md` (section `Requirement Traceability`) + `tasks.md` ทันที. — **Why**: PR #33 แดงเพราะ `producer-google-sso` REQ-4.7 ที่ commit อื่นเพิ่มใน requirements แต่ลืม design traceability — ไม่เกี่ยว SFS ที่กำลังทำ; เกือบไล่ผิดที่.
- **Pattern**: bugfix spec ที่ task แรกเป็น repro test (ต้อง RED ก่อนแก้): อย่า flip `[x]` ของ task test ก่อน fix land — task-gate รัน `SDD_TEST_CMD` ตอน flip แล้ว block ที่สถานะแดง. ทำ T-test + T-fix ให้เขียวก่อน แล้ว flip ตามลำดับ โดย capture output RED จริง (ชื่อ test + assertion message เช่น "Expected: Paid / Actual: AwaitingPayment") ลง Evidence ของ T-test. — **Why**: ลำดับ TDD ชนกับ code-green gate โดยธรรมชาติ; วิธีนี้คงหลักฐาน repro ครบโดยไม่ต้องผ่าน gate ที่สถานะแดง.
- **Pattern**: audit ว่า "ฟีเจอร์ X มีในโค้ดไหม" ห้ามสรุปจาก grep ชื่อ method ของ framework ตรงตัวใน Hosts — โปรเจกต์ห่อเป็น extension ชื่อของตัวเองใน BuildingBlocks (เคสจริง: หา `AddHealthChecks`/`MapHealthChecks` ไม่เจอ → บันทึก gap "ไม่มี health check" ลง canon ทั้งที่มี `AddReadinessHealthChecks()`/`MapPolHealthChecks()` + route `/health/live` อยู่ใน `BuildingBlocks.Web/HealthChecks.cs` wire ครบทั้ง Api/Worker — Codex จับบน PR #46). ค้นด้วย concept (route string, คำ domain) ทั้ง repo รวม BuildingBlocks ก่อนประกาศ "ยังไม่มี". — **Why**: gap ปลอมใน canon = ใบสั่งงานซ้ำซ้อน + mis-state ความพร้อม deploy.
- **Pattern**: rtk hook rewrite ครอบ `rg`/`grep` ด้วย — pattern `-P` (PCRE) fail เพราะข้างหลังเป็น BSD grep และ arg เช่น `-r '$1'` ถูก mangle; งานตรวจ regex หลายไฟล์ (emoji scan, anchor check, ref inventory) เขียนเป็น `python3 - <<'EOF'` ก้อนเดียว หรือ `rtk proxy` เมื่อต้องใช้ tool ตรง. **และ rtk บีบ stdout ของ `git diff` เป็น stat/summary ด้วย** — pipeline ที่ pipe ต่อ (`git diff | grep Assert`) ได้ 0 เงียบๆ ทั้งที่ raw diff มีหลายร้อยบรรทัด (เคสจริง: assertion-diff review ของ hierarchical-naming task 12 เจอ 0/382 รอบแรก); งาน audit/diff-walk ให้ dump raw ลงไฟล์ก่อน (`rtk proxy git diff ... > f` หรือ `git diff --output=f`) แล้ว parse จากไฟล์. — **Why**: retry หลายรอบกับ error ที่ไม่ได้มาจาก pattern ของเราเอง; ร้ายกว่านั้นคือ false-negative เงียบใน review gate — 0 match ที่แปลว่า "ไม่มีอะไรเปลี่ยน" ทั้งที่จริงแค่ output ถูกบีบ.
- **Pattern**: subagent misfire ได้ — จบผิดปกติเร็ว (~5s), `tool_uses: 0`, คืนข้อความ canned ไม่เกี่ยวกับ prompt. ก่อนเชื่อผล agent เช็ค usage metadata (tool calls / duration) ว่าสมเหตุสมผลกับงาน; misfire = relaunch (เพิ่มคำสั่งเปิดงานชัดๆ เช่น "start by running ls" ช่วยลดโอกาสซ้ำ). — **Why**: เอาผล misfire ไปใช้ = วางแผนบนข้อมูลว่าง.
- **Pattern**: secret-scanner heuristic (keyword token/secret/key/password ตามด้วยเครื่องหมาย assign แล้ว value ยาว 20+ ตัว, value-class ไม่รวม `.`) ติด **ชื่อ identifier** ไม่ใช่แค่ค่า secret จริง — บรรทัด assignment ที่ตัวแปรชื่อลงท้ายด้วย token/secret รับค่าจาก class ชื่อยาว (เคสจริง: class ชื่อ `ProducerSessionTokens` 21 ตัว assign ให้ตัวแปร `sessionToken` — หลังเครื่องหมาย assign เป็นชื่อยาวเกิน threshold เลยดูเหมือน secret) โดน flag. ถ้า CI secret-gate แดงบนบรรทัดที่ไม่มี secret จริง: เช็คว่า var/ชื่อ type มีคำ token/session/secret/key + ยาวเกิน threshold ไหม แล้วย่อชื่อ (`ProducerSessionTokens` 21 ตัว → `ProducerTokens` 14 ตัว) — อย่าแก้ scanner/อย่า bypass. หมายเหตุ: example ที่อธิบาย lesson นี้ก็ห้ามวาง keyword ชน assign-sign + ชื่อยาว ในบรรทัดเดียว ไม่งั้น doc trip scanner เอง (`--all` mode สแกนทั้ง repo). — **Why**: เสีย CI cycle ตามหา "secret หลุด" ที่ไม่มีจริง; twin ฝั่ง Admin (`AdminSessionTokens` 18 ตัว) รอดเพราะสั้นกว่า threshold เลยไม่เคยเจอตอนเขียน DUP.
- **Pattern**: GitHub REST create issue (`issue_write method=create` / `POST /issues`) **ignore `state`/`state_reason`** — issue เกิดเป็น `open` เสมอ. จะได้ `closed` ต้อง **create แล้ว `issue_write update(state=closed)` แยกอีก call** (หรือ verify state จริงหลัง create ก่อนบันทึก manifest). `/spec-sync-github` รอบแรกสร้าง task 1-6 (`[x]`) ด้วย `state: closed` → GitHub drop เงียบ → ทั้ง 6 โผล่ **open**, epic sub-issue progress เป็น 0/7 (ทั้งที่เสร็จ 6) + manifest บันทึก `"closed"` optimistic ผิด; re-sync (idempotent) จับ drift แล้ว close ให้ถูก 6/7. — **Why**: teammate เห็น epic ว่างานค้างทั้งที่เสร็จ; อย่าเชื่อ `state` param บน create — close ตามหลังเสมอ หรือ verify state จริงก่อนเขียน manifest (`subIssueLinked`/hash ก็ควร verify จาก response ไม่ใช่สมมติ).
- **Pattern**: code ใหม่ที่ mirror family ที่มีอยู่ (route group, EF `Like`/SFS, seed, config) → grep sibling จริง + เปิด `.ai/shared/stack/<stack>.md` หา pattern ที่ตัดสินไว้แล้ว (escape char, clean-path, tie-breaker) **ก่อนเขียน** ไม่ใช่รอ review จับ. เคสจริง: Codex จับ 2 P2 บน PR #66 ที่เป็น convention documented แล้วทั้งคู่ — trailing-slash `MapGroup`+empty-root (มีใน dotnet.md) และ LIKE ต้อง `SfsLike.Escape` (มีใน dotnet.md + sibling `AdminRoleSfs`/`ProductSfs`) — เขียนซ้ำผิดเพราะไม่ได้เปิด profile/sibling ก่อน. — **Why**: reuse up-front ถูกกว่า review round-trip (CI cycle + fix commit ฟรี); stack profile มีไว้กันพลาดแบบนี้โดยเฉพาะ แต่ช่วยไม่ได้ถ้าไม่อ่านตอนเขียนของใหม่.
- **Pattern**: wire contract ที่ flat/ไม่มี namespace (auth scheme id, permission key, OpenAPI security-scheme id, integration-event name — กลุ่ม L8) ต้องมี test **pin literal** อย่างน้อยหนึ่งจุด. เคสจริง: sweep rename บน branch hierarchical-naming เปลี่ยน scheme id `"PlatformUserSession"` → `"Session"` เงียบ (task 4) แล้ว suite เขียวยาว 6 tasks เพราะทุก consumer อ้าง const — เพิ่งเจอตอน task 10 land ค่า design; ปิดด้วย test เดียวที่ assert `components.securitySchemes` = `["AdminSession","MerchantUserSession"]`. — **Why**: contract ที่ทุก caller อ้างผ่าน const คือจุดบอดของ suite ทั้งระบบ — เปลี่ยนค่าแล้วไม่มีอะไรแดง; literal pin หนึ่งบรรทัดจับ mistake ทั้ง class นี้.
- **Pattern**: verify step ของ subagent ต้องสั่งให้ **รัน gate ที่ CI รันจริง** (`docker/bootstrap/assert-fresh-db.sql`, step ใน `.github/workflows/ci.yml`, `scripts/*.sh`) ห้ามสั่งแค่ "query แล้ววาง output" — query ที่ agent แต่งเองย่อมผ่าน เพราะมันเขียนตามสิ่งที่ตัวเองเพิ่งทำ (ยืนยันว่า "โลกตรงกับที่ฉันคิด" ไม่ใช่ "ตรงกับที่ระบบอื่นคาดหวัง"). เคสจริง (PR #109): teammate ย้าย 4 ตารางจาก `admin` ไป schema `cfg` แล้ว verify ด้วย SQL ของตัวเอง (ตาราง/grant/FK/seed ครบ เขียวหมด) แต่ fresh-DB gate ยังนับ `admin.Positions` → พังทุก fresh CI DB ด้วย `Invalid object name`. ผลพ่วง: rename/ย้าย DB object ต้อง grep ชื่อเก่าใน `docker/` + `.github/` ด้วย ไม่ใช่แค่ `src/`+`tests/`. — **Why**: คลาสเดียวกับ PR #79 (rf1) ที่ required check แดงเงียบตั้งแต่ commit แรก; self-authored verification เป็นจุดบอดถาวรของ agent ที่ทำงานเองแล้วตรวจงานตัวเอง.
- **Pattern**: gate ที่ **skip แล้ว exit 0 = false green** — ห้ามเชื่อ exit code อย่างเดียว ต้องอ่าน output ว่ามัน "ตรวจ" หรือ "ข้าม". เคสจริง: `scripts/spec_trace.py` รับเฉพาะ heading `## REQ-N:` + เกณฑ์ `- N.M ` + บรรทัด `Satisfies:` ใน tasks.md; requirements.md ที่เขียนด้วย `### REQ-N —` + `- **N.M**` ทำให้ script เข้า documented skip path แล้ว exit 0 ทั้งที่ไม่เคยตรวจสัก criterion — spec-trace เป็น required check ⇒ spec drift ผ่านได้เงียบทั้ง spec. — **Why**: false green อันตรายกว่าแดง เพราะไม่มีใครไปดู; เขียน artifact ใหม่แล้วให้รัน gate หนึ่งรอบ + อ่านข้อความ ("OK: เกณฑ์ N ข้อ") ยืนยันว่ามันนับได้จริง.
- **Pattern**: subagent/teammate ห้าม dump process env เต็มก้อน (`env`, `printenv`, `set`) — เคสจริง: teammate หนึ่งทำ env dump ระหว่าง source `.env.integration` แล้วค่า `GH_TOKEN`/`GITHUB_PERSONAL_ACCESS_TOKEN`/`GEMINI_API_KEY` จริงหลุดลง transcript file ใต้ `~/.claude/projects/**` (persist บนดิสก์ + ติด backup ได้). ตรวจการรั่วด้วย name-only check (`grep -c 'KEY=' transcript` — นับ occurrence ไม่ echo ค่า); รั่วแล้ว = แจ้ง user rotate ทันทีตามกฎ secrets + ชี้ไฟล์ transcript ให้ลบ. **main thread ก็ทำหลุดได้ด้วย typo เดียว: `env -u FOO` ที่ไม่มีคำสั่งต่อท้าย = `env` เปล่า = dump ทั้งก้อน** (เคสจริง: `env -u GH_TOKEN; sleep 60; gh pr checks` — `;` ทำให้ `env` กลายเป็นคำสั่งเดี่ยว, `GEMINI_API_KEY` หลุด). `env -u VAR` ต้องตามด้วยคำสั่งเสมอ ไม่ใช่ `;`. — **Why**: transcript คือ output ถาวรอีกช่องที่ secret หลุดได้โดยไม่แตะ git เลย; การ "ตรวจว่ารั่วไหม" ด้วยการ print ค่า = รั่วซ้ำใน transcript ของตัวเอง.
- **Pattern**: noise ซ้ำ ๆ ใน tool output ที่ "ดูไม่มีพิษภัย" ต้อง**อธิบายให้ได้หนึ่งครั้ง** ห้ามปัดทิ้งว่า harmless — "harmless" ที่ยังอธิบายไม่ได้คือสมมติฐาน ไม่ใช่ข้อสรุป. เคสจริง: `set -a && source .env` พ่น `command not found: User` ทุกครั้งตลอด session ประกาศว่าไม่เป็นไรแล้วเดินต่อ ที่จริงคือ shell อ่านค่าที่ **ไม่ได้ quote** แล้วตีความ `;` ในนั้นเป็นตัวจบคำสั่ง -> ตัวแปรถูกตัดเงียบ ๆ (`POL_DESIGN_SQL` เหลือแค่ `Server=localhost,11433`) แล้วไป surface อีก 5 ชั่วโมงถัดมาเป็น `pre-login handshake` error ของ `dotnet ef` ที่ดูไม่เกี่ยวกันเลย. — **Why**: ต้นทุนเช็คหนึ่งครั้ง (~1 คำสั่ง) ต่ำกว่าต้นทุนไล่อาการปลายทางเสมอ; noise ที่ถูกปัดทิ้งคือต้นเหตุที่ถูกฝัง.
- **Pattern**: ไฟล์ gitignored (`.env`, `appsettings.Development.json`, config เครื่องใคร) = **จุดบอดถาวรของ CI** — rename configuration key ที่ sweep ทั้ง repo แล้ว CI เขียวหมด ยังทิ้ง key เก่าค้างในไฟล์พวกนี้ของทุกเครื่อง dev โดยไม่มี gate ไหนจับ. เคสจริง: rf1 rename `ConnectionStrings:Producer` -> `App`; `.env`/`appsettings.Development.json` บนเครื่อง dev ยังเป็น `Producer` -> `GetConnectionString("App")` ตกไปหยิบ `appsettings.json` ที่ commit ไว้ซึ่ง password ว่างโดยตั้งใจ -> connection ของ `pol_app` ใช้ไม่ได้ทั้งตัว แต่ host ยัง boot ผ่าน (ไม่ fail-fast) เลยดูเหมือนปกติ. rename ที่แตะ configuration key ต้องแนบรายการ "ไฟล์ local ที่ต้องแก้ด้วยมือ" ใน PR description + README เสมอ. — **Why**: sweep + CI เขียว ให้ความมั่นใจปลอมว่า rename จบ ทั้งที่ทุกเครื่อง dev พังเงียบ — และเพราะไฟล์ gitignored จึงไม่มีใครเห็นว่าคนอื่นก็เจอเหมือนกัน.
- **Pattern**: กฎเดียวกันห้ามอยู่หลายไฟล์ — เลือก single source แล้วที่เหลือเป็น **pointer** (ชื่อไฟล์ + หัวข้อ) ไม่ใช่ copy เนื้อ. canon ปัจจุบัน: task sizing = `TASK_PROTOCOL.md`, EARS = `EARS.md`, test = `TESTING_PROTOCOL.md`, secret/branch = `SECURITY_RULES.md`. ที่ยังค้าง (แก้ตอนแตะไฟล์นั้นอยู่แล้ว): `spec-requirements/SKILL.md` inline EARS ครบ 5 patterns แทนที่จะชี้ `EARS.md`; `spec-quick/SKILL.md` ชี้ sizing ไปที่ skill `spec-tasks` ไม่ใช่ canon; `ARCHITECTURE.md` §Anti-Patterns ถือกฎของ TASK_PROTOCOL (mark `[x]` ต้องมี Evidence) กับ TESTING_PROTOCOL (assert พฤติกรรมที่สังเกตได้) ไว้เอง. — **Why**: กฎที่อยู่ 2-3 ที่จะ drift คนละทิศเงียบ ๆ แล้ว agent อ่านเจอฉบับเก่าก่อน — การยุบ canon เข้า `.ai/shared/` ปิดก้อนใหญ่ไปแล้ว แต่ copy ที่ตกค้างใน skill คือแหล่ง drift รอบถัดไป.
- **Pattern**: promote lesson ใหม่ = **merge กับข้อเดิมที่ชนกัน** ไม่ใช่ append ท้ายเสมอ — ก่อนเพิ่ม grep หัวข้อเดียวกันใน `LESSONS.md` / `.ai/shared/stack/<stack>.md`; เจอข้อที่ขัดกันให้เขียนรวมเป็นข้อเดียวพร้อมเงื่อนไขว่าอันไหนใช้เมื่อไร. เคสจริง: `stack/nextjs.md` (ถอดออกแล้ว) เคยมีสองบรรทัดสั่งกลับด้านกันเรื่องชดเชย scrollbar ("+15 เสมอ" vs "headless = 0px") อยู่ร่วมไฟล์เดียวกัน. `/spec-retro` สั่ง prune เฉพาะ stale/duplicate — **ไม่ครอบ contradictory**. คู่กับ budget: `LESSONS.md` อยู่ใน always-on prefix ทุก turn (ตอนนี้ ~34KB) ข้อใหม่ที่ไม่ merge = ต้นทุนถาวรทุก session. — **Why**: lesson ที่ขัดกันเองแย่กว่าไม่มี lesson — agent หยิบข้อไหนก็ได้แล้วอ้างว่าทำตาม canon.
- **Pattern**: decision ตอน implement ที่เบี่ยงจาก `design.md` (pin เวอร์ชัน, เปลี่ยน data shape, เลือก library) ต้อง **sync กลับขึ้น `design.md` ในงานเดียวกัน** ไม่ใช่จบที่ `deviations:` ใน Evidence ของ `tasks.md`. ตอนนี้ propagation เดินทางเดียว (requirements → design → tasks ผ่าน sync-mode ของ /spec-design + /spec-tasks) ส่วนขา implement → design ไม่มีใครทำ; `/spec-retro` step 4 sync แค่ CODING_STANDARDS/ARCHITECTURE ไม่แตะ design.md. — **Why**: `design.md` คือสิ่งที่ session ถัดไปอ่านเพื่อ re-acquire context — ถ้ามันค้างที่แผนเดิม decision ที่ตัดสินไปแล้วจะถูก re-litigate หรือถูก implement กลับด้านใน task ถัดไป.
- **Pattern**: doc ที่อ้างตำแหน่ง config ต้องระบุว่าเป็น **global (`~/.claude/settings.json`) หรือ project (`.claude/settings.json`)** เสมอ — ชื่อไฟล์ซ้ำกัน. เคสจริง: `.claude/commands/pane-loop.md` บอก hands-free มาจาก "`settings.json` (`defaultMode: bypassPermissions`)" แต่ project settings.json มีแต่ `hooks` ไม่มี `defaultMode` — ค่าอยู่ที่ global เท่านั้น ⇒ เครื่องใหม่/clone ใหม่รัน pane-loop แล้วค้างรอ permission prompt โดยไม่รู้ว่าขาดอะไร. — **Why**: config ที่อยู่ global ไม่ติดไปกับ repo; doc ที่ไม่บอกว่าอยู่ไฟล์ไหน ทำให้คน setup ใหม่ไปไล่หาในที่ที่ไม่มีวันเจอ.
- **Pattern**: verify step ของ task หนึ่งอาจ **พิสูจน์ไม่ได้จนกว่า task ถัดไปจะมีข้อมูล** — สั่ง agent ให้รายงานตรง ๆ เมื่อ verify ไม่ applicable ดีกว่าปล่อยให้แต่ง evidence ที่ "ผ่าน". เคสจริง: T1 ของ demo-seed ถูกสั่งพิสูจน์ว่า "ลบก่อน stamp RLS context = PK collision" แต่ T1 ยังไม่มี INSERT ลงตาราง merchant-scoped เลย -> comment step ออกแล้วสคริปต์ยัง exit 0; T1 รายงานตรงว่าพิสูจน์ไม่ได้ + ทำ standalone experiment แทน + ชี้ว่าของจริงจะเห็นตอน T2 — T2 ได้ `Msg 2627 Violation of PRIMARY KEY constraint` จริง. — **Why**: agent ที่เจอ verify ที่พิสูจน์ไม่ได้มักเลือกแต่ง evidence ผ่านมากกว่ายอมรับว่าทำไม่ได้; evidence ปลอมแพงกว่า verify ที่เลื่อนไป task ถัดไป.
